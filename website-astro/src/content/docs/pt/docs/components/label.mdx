---
slug: pt/docs/components/label
title: Etiqueta (Label)
sidebar_label: Label
---

import CodeSampleWithSource from "../../../../../components/CodeSampleWithSource.astro";

O componente `Label` é um componente simples e versátil usado para exibir conteúdo de texto com estilo de rótulo.
Geralmente é usado para exibir um rótulo para um campo de entrada, como um checkbox ou botão de rádio.

## Uso

Para usar o componente `Label`, importe-o dos seus componentes. Você pode passar uma prop `border` para adicionar uma borda ao redor do rótulo.
Você também pode passar qualquer uma das props HTML `label`, como `htmlFor`, `className` e `style`.

## Label com Checkbox

Exemplo simples de uso do componente `Label` para criar uma superfície básica para conteúdo.

<CodeSampleWithSource path="/src/content/docs/samples/components/label/label_checkbox.tsx"/>

## Label com botão de rádio

Este é um exemplo de uso do componente `Label` com um botão de rádio.

<CodeSampleWithSource path="/src/content/docs/samples/components/label/label_radio_group.tsx"/>

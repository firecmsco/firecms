---
title: Getting started
sidebar_label: Getting started
slug: docs/index
description: Welcome to Rebase, the open-source Postgres admin panel and headless CMS built for developers. Define your schema as TypeScript collections, and Rebase generates a full CRUD UI with tables, forms, relations, and validation — all backed by real-time WebSocket sync.
---

<video className="intro_video" loop autoPlay muted>
    <source src="/img/full_screen_dark.mp4" type="video/mp4"/>
</video>

:::important
Rebase is an **open-source Postgres admin panel and headless CMS** built with React and TypeScript. It connects directly to your **PostgreSQL** database to provide a powerful, intuitive, and type-safe interface for managing your data.
:::

Rebase is designed to be the fastest way to build a production-ready back-office on top of Postgres. It generates full CRUD (Create, Read, Update, Delete) views from your TypeScript collection definitions, supports **relations**, **Row Level Security (RLS)**, and can be set up in minutes.

## Getting Started

Scaffold a new project with the CLI:

```bash
npx create-rebase-app
```

This generates a project with three packages:

- **`frontend/`** — React SPA (Vite + TypeScript) with the Rebase admin UI
- **`backend/`** — Node.js + Express server with PostgreSQL, Drizzle ORM, and WebSocket sync
- **`shared/`** — TypeScript collection definitions shared between frontend and backend

Configure your database in `.env`:

```bash
DATABASE_URL=postgresql://username:password@localhost:5432/your_database
```

Start the dev servers:

```bash
pnpm dev
```

That's it. Your admin panel is running. Follow the [**Quickstart guide**](/docs/self) for the full walkthrough.

## What Can You Build?

Rebase is flexible enough to be the admin panel for any Postgres-backed project. Common use cases include:

  * **Custom Admin Panels:** Create a type-safe UI for managing your application's data (e.g., users, products, orders) without the frontend hassle.
  * **Internal Tools:** Build bespoke tools for your operations, sales, or support teams to manage workflows, track data, and run internal processes.
  * **Headless CMS:** Power your websites, mobile apps, and marketing content from a single, powerful admin interface backed by Postgres.

## Core Features

### Schema as Code

Define your data models as TypeScript collections. Each collection maps to a Postgres table and supports typed properties, relations, enums, and security rules. Your code becomes the single source of truth, enabling version control, type-safety, and a seamless git-based workflow.

### Instant CRUD UI

Rebase reads your schema and automatically generates a full-featured, performant, and beautiful CRUD interface with tables, forms, validation, relations, and more.

### Real-time Sync

Rebase uses WebSockets to provide real-time data synchronization. When data changes in Postgres, your admin panel updates instantly.

### Radical Extensibility

If you can build it in React, you can build it in Rebase. Create custom form fields, entirely new top-level views (like SQL consoles or RLS editors), or replace any part of the UI with your own components. No black boxes.

### Built-in Tools

Every Rebase project includes:

- **Collection Editor** — create and edit collections through a visual UI
- **Data Import/Export** — import JSON, CSV, and Excel; export to JSON and CSV
- **User & Role Management** — role-based access control stored in your database
- **Entity History** — track every change with full audit trail
- **SQL Console** — run queries directly against your database
- **RLS Policy Editor** — manage Row Level Security policies visually
- **REST & GraphQL API** — auto-generated from your collections
- **AI Data Enhancement** — LLM-powered autocompletion for your fields

-----

## Next Steps

  * **🚀 Quickstart:** Follow the [**Quickstart guide**](/docs/self) to get your project running locally with `npx`.
  * **📖 Template Walkthrough:** Understand the generated code with the [**Template Walkthrough**](/docs/pro/template_walkthrough).
  * **📚 Collections:** Learn how Rebase maps [**Collections**](/docs/collections) to your Postgres tables.
  * **🗄️ Database Setup:** Set up your PostgreSQL database with the [**Database Setup guide**](/docs/database_setup).

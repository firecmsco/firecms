---
slug: "docs/api/variables/iconSynonyms"
title: "iconSynonyms"
---

[**@firecms/core**](../README)

***

[@firecms/core](../README) / iconSynonyms

# Variable: iconSynonyms

> `const` **iconSynonyms**: `object`

Defined in: [util/icon\_synonyms.ts:1](https://github.com/firecmsco/firecms/blob/main/packages/firecms_core/src/util/icon_synonyms.ts)

## Type Declaration

### abc

> **abc**: `string` = `"alphabet character font letter symbol text type"`

### ac\_unit

> **ac\_unit**: `string` = `"air cold conditioner freeze snowflake temperature weather winter"`

### access\_alarm

> **access\_alarm**: `string` = `"clock time"`

### access\_alarms

> **access\_alarms**: `string` = `"clock time"`

### access\_time

> **access\_time**: `string` = `"clock time"`

### accessibility

> **accessibility**: `string` = `"accessible body handicap help human people person user"`

### accessibility\_new

> **accessibility\_new**: `string` = `"accessible arms body handicap help human people person user"`

### accessible

> **accessible**: `string` = `"accessibility body handicap help human people person user wheelchair"`

### accessible\_forward

> **accessible\_forward**: `string` = `"accessibility body handicap help human people person wheelchair"`

### account\_balance

> **account\_balance**: `string` = `"bank bill building card cash coin commerce court credit currency dollars finance money online payment structure temple transaction"`

### account\_balance\_wallet

> **account\_balance\_wallet**: `string` = `"bank bill card cash coin commerce credit currency dollars finance money online payment transaction"`

### account\_box

> **account\_box**: `string` = `"avatar face human people person profile square thumbnail user"`

### account\_circle

> **account\_circle**: `string` = `"avatar face human people person profile thumbnail user"`

### account\_tree

> **account\_tree**: `string` = `"analytics chart connect data diagram flow infographic measure metrics process project sitemap square statistics structure tracking"`

### ad\_units

> **ad\_units**: `string` = `"Android banner cell device hardware iOS mobile notifications phone tablet top"`

### adb

> **adb**: `string` = `"android bridge debug"`

### add

> **add**: `string` = `"+ create item new plus symbol"`

### add\_a\_photo

> **add\_a\_photo**: `string` = `"+ camera lens new photography picture plus symbol"`

### add\_alarm

> **add\_alarm**: `string` = `"clock plus time"`

### add\_alert

> **add\_alert**: `string` = `"+ active alarm announcement bell callout chime information new notifications notify plus reminder ring sound symbol"`

### add\_box

> **add\_box**: `string` = `"create new plus square symbol"`

### add\_business

> **add\_business**: `string` = `"+ bill building card cash coin commerce company credit currency dollars market money new online payment plus retail shopping storefront symbol"`

### add\_card

> **add\_card**: `string` = `"+ bill cash coin commerce cost credit currency dollars finance money new online payment plus price shopping symbol"`

### add\_chart

> **add\_chart**: `string` = `"+ analytics bars data diagram infographic measure metrics new plus statistics symbol tracking"`

### add\_circle

> **add\_circle**: `string` = `"+ create new plus"`

### add\_circle\_outline

> **add\_circle\_outline**: `string` = `"+ create new plus"`

### add\_comment

> **add\_comment**: `string` = `"+ bubble chat communicate feedback message new plus speech symbol"`

### add\_ic\_call

> **add\_ic\_call**: `string` = `"+ cell contact device hardware mobile new plus symbol telephone"`

### add\_link

> **add\_link**: `string` = `"attach clip new plus symbol"`

### add\_location

> **add\_location**: `string` = `"+ destination direction gps maps new pin place plus stop symbol"`

### add\_location\_alt

> **add\_location\_alt**: `string` = `"+ destination direction maps new pin place plus stop symbol"`

### add\_moderator

> **add\_moderator**: `string` = `"+ certified new plus privacy private protection security shield symbol verified"`

### add\_photo\_alternate

> **add\_photo\_alternate**: `string` = `"+ image landscape mountains new photography picture plus symbol"`

### add\_road

> **add\_road**: `string` = `"+ destination direction highway maps new plus stop street symbol traffic"`

### add\_shopping\_cart

> **add\_shopping\_cart**: `string` = `"card cash checkout coin commerce credit currency dollars money online payment plus"`

### add\_task

> **add\_task**: `string` = `"+ approve check circle completed increase mark ok plus select tick yes"`

### add\_to\_drive

> **add\_to\_drive**: `string` = `"+ app backup cloud data files folders gdrive google plus recovery storage"`

### add\_to\_home\_screen

> **add\_to\_home\_screen**: `string` = `"Android add arrow cell device hardware iOS mobile phone tablet to up"`

### add\_to\_photos

> **add\_to\_photos**: `string` = `"collection image landscape mountains photography picture plus"`

### add\_to\_queue

> **add\_to\_queue**: `string` = `"+ Android backlog chrome desktop device display hardware iOS lineup mac monitor new plus screen symbol television watch web window"`

### adf\_scanner

> **adf\_scanner**: `string` = `"document feeder machine office"`

### adjust

> **adjust**: `string` = `"alter center circles control dot edit filter fix image mix move setting slider sort switch target tune"`

### admin\_panel\_settings

> **admin\_panel\_settings**: `string` = `"account avatar certified face human people person privacy private profile protection security shield user verified"`

### agriculture

> **agriculture**: `string` = `"automobile cars cultivation farm harvest maps tractor transport travel truck vehicle"`

### air

> **air**: `string` = `"blowing breeze flow wave weather wind"`

### airline\_seat\_flat

> **airline\_seat\_flat**: `string` = `"bed body business class first human people person rest sleep travel"`

### airline\_seat\_flat\_angled

> **airline\_seat\_flat\_angled**: `string` = `"bed body business class first human people person rest sleep travel"`

### airline\_seat\_individual\_suite

> **airline\_seat\_individual\_suite**: `string` = `"bed body business class first human people person rest sleep travel"`

### airline\_seat\_legroom\_extra

> **airline\_seat\_legroom\_extra**: `string` = `"body feet human people person sitting space travel"`

### airline\_seat\_legroom\_normal

> **airline\_seat\_legroom\_normal**: `string` = `"body feet human people person sitting space travel"`

### airline\_seat\_legroom\_reduced

> **airline\_seat\_legroom\_reduced**: `string` = `"body feet human people person sitting space travel"`

### airline\_seat\_recline\_extra

> **airline\_seat\_recline\_extra**: `string` = `"body feet human legroom people person sitting space travel"`

### airline\_seat\_recline\_normal

> **airline\_seat\_recline\_normal**: `string` = `"body extra feet human legroom people person sitting space travel"`

### airline\_stops

> **airline\_stops**: `string` = `"arrow destination direction layover location maps place transportation travel trip"`

### airlines

> **airlines**: `string` = `"airplane airport flight transportation travel trip"`

### airplane\_ticket

> **airplane\_ticket**: `string` = `"airport boarding flight fly maps pass transportation travel"`

### airplanemode\_active

> **airplanemode\_active**: `string` = `"flight flying on signal"`

### airplanemode\_inactive

> **airplanemode\_inactive**: `string` = `"airport disabled enabled flight flying maps offline slash transportation travel"`

### airplay

> **airplay**: `string` = `"apple arrow cast connect control desktop device display monitor screen signal television tv"`

### airport\_shuttle

> **airport\_shuttle**: `string` = `"automobile bus cars commercial delivery direction maps mini public transportation travel truck van vehicle"`

### alarm

> **alarm**: `string` = `"alart bell clock countdown date notification schedule time"`

### alarm\_add

> **alarm\_add**: `string` = `"+ alart bell clock countdown date new notification plus schedule symbol time"`

### alarm\_off

> **alarm\_off**: `string` = `"alart bell clock disabled duration enabled notification slash stop timer watch"`

### alarm\_on

> **alarm\_on**: `string` = `"alart bell checkmark clock disabled duration enabled notification off ready slash start timer watch"`

### album

> **album**: `string` = `"artist audio bvb cd computer data disk file music play record sound storage track vinyl"`

### align\_horizontal\_center

> **align\_horizontal\_center**: `string` = `"alignment format layout lines paragraph rules style text"`

### align\_horizontal\_left

> **align\_horizontal\_left**: `string` = `"alignment format layout lines paragraph rules style text"`

### align\_horizontal\_right

> **align\_horizontal\_right**: `string` = `"alignment format layout lines paragraph rules style text"`

### align\_vertical\_bottom

> **align\_vertical\_bottom**: `string` = `"alignment format layout lines paragraph rules style text"`

### align\_vertical\_center

> **align\_vertical\_center**: `string` = `"alignment format layout lines paragraph rules style text"`

### align\_vertical\_top

> **align\_vertical\_top**: `string` = `"alignment format layout lines paragraph rules style text"`

### all\_inbox

> **all\_inbox**: `string` = `"Inbox delivered delivery email letter message post send"`

### all\_inclusive

> **all\_inclusive**: `string` = `"endless forever infinite infinity loop mobius neverending strip sustainability sustainable"`

### all\_out

> **all\_out**: `string` = `"arrows circle directional expand shape"`

### alt\_route

> **alt\_route**: `string` = `"alternate alternative arrows direction maps navigation options other routes split symbol"`

### alternate\_email

> **alternate\_email**: `string` = `"@ address contact tag"`

### analytics

> **analytics**: `string` = `"assessment bar chart data diagram infographic measure metrics statistics tracking"`

### anchor

> **anchor**: `string` = `"google logo"`

### android

> **android**: `string` = `"brand character logo mascot operating system toy"`

### animation

> **animation**: `string` = `"circles film motion movement movie moving sequence video"`

### announcement

> **announcement**: `string` = `"! alert attention balloon bubble caution chat comment communicate danger error exclamation feedback important mark message news notification speech symbol warning"`

### aod

> **aod**: `string` = `"Android always device display hardware homescreen iOS mobile phone tablet"`

### apartment

> **apartment**: `string` = `"accommodation architecture building city company estate flat home house office places real residence residential shelter units workplace"`

### api

> **api**: `string` = `"developer development enterprise software"`

### app\_blocking

> **app\_blocking**: `string` = `"Android applications cancel cell device hardware iOS mobile phone stopped tablet"`

### app\_registration

> **app\_registration**: `string` = `"apps edit pencil register"`

### app\_settings\_alt

> **app\_settings\_alt**: `string` = `"Android applications cell device gear hardware iOS mobile phone tablet"`

### app\_shortcut

> **app\_shortcut**: `string` = `"bookmarked favorite highlight important mobile saved software special star"`

### approval

> **approval**: `string` = `"apply approvals approve certificate certification disapproval drive file impression ink mark postage stamp"`

### apps

> **apps**: `string` = `"all applications circles collection components dots grid homescreen icons interface squares ui ux"`

### apps\_outage

> **apps\_outage**: `string` = `"all applications circles collection components dots grid interface squares ui ux"`

### architecture

> **architecture**: `string` = `"art compass design drawing engineering geometric tool"`

### archive

> **archive**: `string` = `"inbox mail store"`

### arrow\_back

> **arrow\_back**: `string` = `"application components direction interface left navigation previous screen ui ux website"`

### arrow\_back\_ios

> **arrow\_back\_ios**: `string` = `"application chevron components direction interface left navigation previous screen ui ux website"`

### arrow\_back\_ios\_new

> **arrow\_back\_ios\_new**: `string` = `"application chevron components direction interface left navigation previous screen ui ux website"`

### arrow\_circle\_down

> **arrow\_circle\_down**: `string` = `"direction navigation"`

### arrow\_circle\_up

> **arrow\_circle\_up**: `string` = `"direction navigation"`

### arrow\_downward

> **arrow\_downward**: `string` = `"application components direction interface navigation screen ui ux website"`

### arrow\_drop\_down

> **arrow\_drop\_down**: `string` = `"application components direction interface navigation screen ui ux website"`

### arrow\_drop\_down\_circle

> **arrow\_drop\_down\_circle**: `string` = `"application components direction interface navigation screen ui ux website"`

### arrow\_drop\_up

> **arrow\_drop\_up**: `string` = `"application components direction interface navigation screen ui ux website"`

### arrow\_forward

> **arrow\_forward**: `string` = `"application arrows components direction interface navigation right screen ui ux website"`

### arrow\_forward\_ios

> **arrow\_forward\_ios**: `string` = `"application chevron components direction interface navigation next right screen ui ux website"`

### arrow\_left

> **arrow\_left**: `string` = `"application backstack backward components direction interface navigation previous screen ui ux website"`

### arrow\_right

> **arrow\_right**: `string` = `"application components continue direction forward interface navigation screen ui ux website"`

### arrow\_right\_alt

> **arrow\_right\_alt**: `string` = `"arrows direction east navigation pointing shape"`

### arrow\_upward

> **arrow\_upward**: `string` = `"application components direction interface navigation screen submit ui ux website"`

### art\_track

> **art\_track**: `string` = `"album artist audio display format image insert music photography picture sound tracks"`

### article

> **article**: `string` = `"clarify document file news page paper text writing"`

### aspect\_ratio

> **aspect\_ratio**: `string` = `"expand image monitor resize resolution scale screen square"`

### assessment

> **assessment**: `string` = `"analytics bars chart data diagram infographic measure metrics report statistics tracking"`

### assignment

> **assignment**: `string` = `"article clipboard document task text writing"`

### assignment\_ind

> **assignment\_ind**: `string` = `"account clipboard document face people person profile task user"`

### assignment\_late

> **assignment\_late**: `string` = `"! alert announcement attention caution clipboard danger document error exclamation important mark notification symbol task warning"`

### assignment\_return

> **assignment\_return**: `string` = `"arrow back clipboard document left point retun task"`

### assignment\_returned

> **assignment\_returned**: `string` = `"arrow clipboard document down point task"`

### assignment\_turned\_in

> **assignment\_turned\_in**: `string` = `"approve checkmark clipboard complete document done finished ok select task tick validate verified yes"`

### assistant

> **assistant**: `string` = `"bubble chat comment communicate feedback message recommendation speech star suggestion twinkle"`

### assistant\_direction

> **assistant\_direction**: `string` = `"destination location maps navigate navigation pin place right stop"`

### assistant\_photo

> **assistant\_photo**: `string` = `"flag recommendation smart star suggestion"`

### assured\_workload

> **assured\_workload**: `string` = `"compliance confidential federal government regulatory secure sensitive"`

### atm

> **atm**: `string` = `"alphabet automated bill card cart cash character coin commerce credit currency dollars font letter machine money online payment shopping symbol teller text type"`

### attach\_email

> **attach\_email**: `string` = `"attachment clip compose envelop letter link message send"`

### attach\_file

> **attach\_file**: `string` = `"add item link mail media paperclip"`

### attach\_money

> **attach\_money**: `string` = `"bill card cash coin commerce cost credit currency dollars finance online payment price profit sale symbol"`

### attachment

> **attachment**: `string` = `"compose file image item link paperclip"`

### attractions

> **attractions**: `string` = `"amusement entertainment ferris fun maps park places wheel"`

### attribution

> **attribution**: `string` = `"attribute body copyright copywriter human people person"`

### audio\_file

> **audio\_file**: `string` = `"document key music note sound track"`

### audiotrack

> **audiotrack**: `string` = `"key music note sound"`

### auto\_awesome

> **auto\_awesome**: `string` = `"adjust editing enhance filter image photography photos setting stars"`

### auto\_awesome\_mosaic

> **auto\_awesome\_mosaic**: `string` = `"adjust collage editing enhance filter grid image layout photographs photography photos pictures setting"`

### auto\_awesome\_motion

> **auto\_awesome\_motion**: `string` = `"adjust animation collage editing enhance filter image live photographs photography photos pictures setting video"`

### auto\_delete

> **auto\_delete**: `string` = `"bin can clock date garbage remove schedule time trash"`

### auto\_fix\_high

> **auto\_fix\_high**: `string` = `"adjust editing enhance erase magic modify pen stars tool wand"`

### auto\_fix\_normal

> **auto\_fix\_normal**: `string` = `"edit erase magic modify stars wand"`

### auto\_fix\_off

> **auto\_fix\_off**: `string` = `"disabled edit enabled erase magic modify on slash stars wand"`

### auto\_graph

> **auto\_graph**: `string` = `"analytics chart data diagram infographic line measure metrics stars statistics tracking"`

### auto\_mode

> **auto\_mode**: `string` = `"around arrows direction inprogress loading navigation nest refresh renew rotate turn"`

### auto\_stories

> **auto\_stories**: `string` = `"audiobook flipping pages reading story"`

### autofps\_select

> **autofps\_select**: `string` = `"A alphabet character font frame frequency letter per rate seconds symbol text type"`

### autorenew

> **autorenew**: `string` = `"around arrows cached direction inprogress loader loading navigation pending refresh rotate status turn"`

### av\_timer

> **av\_timer**: `string` = `"clock countdown duration minutes seconds stopwatch"`

### baby\_changing\_station

> **baby\_changing\_station**: `string` = `"babies bathroom body children father human infant kids mother newborn people person toddler wc young"`

### backpack

> **backpack**: `string` = `"bookbag knapsack storage travel"`

### backspace

> **backspace**: `string` = `"arrow cancel clear correct delete erase remove"`

### backup

> **backup**: `string` = `"arrow cloud data drive files folders point storage submit upload"`

### backup\_table

> **backup\_table**: `string` = `"drive files folders format layout stack storage"`

### badge

> **badge**: `string` = `"account avatar card certified employee face human identification name people person profile security user work"`

### bakery\_dining

> **bakery\_dining**: `string` = `"bread breakfast brunch croissant food"`

### balance

> **balance**: `string` = `"equal equilibrium equity impartiality justice parity stability. steadiness symmetry"`

### balcony

> **balcony**: `string` = `"architecture doors estate home house maps outside place real residence residential stay terrace window"`

### ballot

> **ballot**: `string` = `"bullet bulllet election list point poll vote"`

### bar\_chart

> **bar\_chart**: `string` = `"analytics anlytics data diagram infographic measure metrics statistics tracking"`

### batch\_prediction

> **batch\_prediction**: `string` = `"bulb idea light"`

### bathroom

> **bathroom**: `string` = `"closet home house place plumbing shower sprinkler wash water wc"`

### bathtub

> **bathtub**: `string` = `"bathing bathroom clean home hotel human person shower travel"`

### battery\_alert

> **battery\_alert**: `string` = `"! attention caution cell charge danger error exclamation important mark mobile notification power symbol warning"`

### battery\_charging\_full

> **battery\_charging\_full**: `string` = `"cell charge lightening lightning mobile power thunderbolt"`

### battery\_full

> **battery\_full**: `string` = `"cell charge mobile power"`

### battery\_saver

> **battery\_saver**: `string` = `"+ add charge charging new plus power symbol"`

### battery\_std

> **battery\_std**: `string` = `"cell charge mobile plus power standard"`

### battery\_unknown

> **battery\_unknown**: `string` = `"? assistance cell charge help information mark mobile power punctuation question support symbol"`

### beach\_access

> **beach\_access**: `string` = `"parasol places summer sunny umbrella"`

### bed

> **bed**: `string` = `"bedroom double full furniture home hotel house king night pillows queen rest size sleep"`

### bedroom\_baby

> **bedroom\_baby**: `string` = `"babies children home horse house infant kid newborn rocking toddler young"`

### bedroom\_child

> **bedroom\_child**: `string` = `"children furniture home hotel house kid night pillows rest size sleep twin young"`

### bedroom\_parent

> **bedroom\_parent**: `string` = `"double full furniture home hotel house king master night pillows queen rest sizem sleep"`

### bedtime

> **bedtime**: `string` = `"nightime sleep"`

### bedtime\_off

> **bedtime\_off**: `string` = `"nightime sleep"`

### beenhere

> **beenhere**: `string` = `"approve archive bookmark checkmark complete done favorite label library reading remember ribbon save select tag tick validate verified yes"`

### bento

> **bento**: `string` = `"box dinner food lunch meal restaurant takeout"`

### bike\_scooter

> **bike\_scooter**: `string` = `"automobile cars maps transportation vehicle vespa"`

### biotech

> **biotech**: `string` = `"chemistry laboratory microscope research science technology test"`

### blender

> **blender**: `string` = `"appliance cooking electric juicer kitchen machine vitamix"`

### blinds\_closed

> **blinds\_closed**: `string` = `"cover curtains nest shutter sunshade"`

### block

> **block**: `string` = `"allowed avoid banned cancel close disable entry exit not prohibited quit remove stop"`

### bloodtype

> **bloodtype**: `string` = `"donate droplet emergency hospital medicine negative positive water"`

### bluetooth

> **bluetooth**: `string` = `"cast connection device network paring streaming symbol wireless"`

### bluetooth\_audio

> **bluetooth\_audio**: `string` = `"connection device music signal sound symbol"`

### bluetooth\_connected

> **bluetooth\_connected**: `string` = `"cast connection device network paring streaming symbol wireless"`

### bluetooth\_disabled

> **bluetooth\_disabled**: `string` = `"cast connection device enabled network offline paring slash streaming symbol wireless"`

### bluetooth\_drive

> **bluetooth\_drive**: `string` = `"automobile cars cast connection device maps paring streaming symbol transportation travel vehicle wireless"`

### bluetooth\_searching

> **bluetooth\_searching**: `string` = `"connection device network paring symbol wireless"`

### blur\_circular

> **blur\_circular**: `string` = `"circle dots editing effect enhance filter"`

### blur\_linear

> **blur\_linear**: `string` = `"dots editing effect enhance filter"`

### blur\_off

> **blur\_off**: `string` = `"disabled dots editing effect enabled enhance on slash"`

### blur\_on

> **blur\_on**: `string` = `"disabled dots editing effect enabled enhance filter off slash"`

### bolt

> **bolt**: `string` = `"electric energy fast flash lightning power thunderbolt"`

### book

> **book**: `string` = `"blog bookmark favorite label library reading remember ribbon save tag"`

### book\_online

> **book\_online**: `string` = `"Android admission appointment cell device event hardware iOS mobile pass phone reservation tablet ticket"`

### bookmark

> **bookmark**: `string` = `"archive favorite follow label library reading remember ribbon save tag"`

### bookmark\_add

> **bookmark\_add**: `string` = `"+ favorite plus remember ribbon save symbol"`

### bookmark\_added

> **bookmark\_added**: `string` = `"approve check complete done favorite remember save select tick validate verified yes"`

### bookmark\_border

> **bookmark\_border**: `string` = `"archive favorite label library outline reading remember ribbon save tag"`

### bookmark\_remove

> **bookmark\_remove**: `string` = `"delete favorite minus remember ribbon save subtract"`

### bookmarks

> **bookmarks**: `string` = `"favorite label layers library multiple reading remember ribbon save stack tag"`

### border\_all

> **border\_all**: `string` = `"doc editing editor spreadsheet stroke text type writing"`

### border\_bottom

> **border\_bottom**: `string` = `"doc editing editor spreadsheet stroke text type writing"`

### border\_clear

> **border\_clear**: `string` = `"doc editing editor spreadsheet stroke text type writing"`

### border\_color

> **border\_color**: `string` = `"all create doc editing editor marker pencil spreadsheet stroke text type writing"`

### border\_horizontal

> **border\_horizontal**: `string` = `"doc editing editor spreadsheet stroke text type writing"`

### border\_inner

> **border\_inner**: `string` = `"doc editing editor spreadsheet stroke text type writing"`

### border\_left

> **border\_left**: `string` = `"doc editing editor spreadsheet stroke text type writing"`

### border\_outer

> **border\_outer**: `string` = `"doc editing editor spreadsheet stroke text type writing"`

### border\_right

> **border\_right**: `string` = `"doc editing editor spreadsheet stroke text type writing"`

### border\_style

> **border\_style**: `string` = `"color doc editing editor spreadsheet stroke text type writing"`

### border\_top

> **border\_top**: `string` = `"doc editing editor spreadsheet stroke text type writing"`

### border\_vertical

> **border\_vertical**: `string` = `"doc editing editor spreadsheet stroke text type writing"`

### boy

> **boy**: `string` = `"body gender human male people person social symbol"`

### branding\_watermark

> **branding\_watermark**: `string` = `"components copyright design emblem format identity interface layout logo screen stamp ui ux website window"`

### breakfast\_dining

> **breakfast\_dining**: `string` = `"bakery bread butter food toast"`

### brightness\_1

> **brightness\_1**: `string` = `"circle control crescent cresent level moon screen"`

### brightness\_2

> **brightness\_2**: `string` = `"circle control crescent cresent level moon night screen"`

### brightness\_3

> **brightness\_3**: `string` = `"circle control crescent cresent level moon night screen"`

### brightness\_4

> **brightness\_4**: `string` = `"circle control crescent cresent dark level moon night screen sun"`

### brightness\_5

> **brightness\_5**: `string` = `"circle control crescent cresent level moon screen sun"`

### brightness\_6

> **brightness\_6**: `string` = `"circle control crescent cresent level moon screen sun"`

### brightness\_7

> **brightness\_7**: `string` = `"circle control crescent cresent level light moon screen sun"`

### brightness\_auto

> **brightness\_auto**: `string` = `"A control display level mobile monitor phone screen"`

### brightness\_high

> **brightness\_high**: `string` = `"auto control mobile monitor phone"`

### brightness\_low

> **brightness\_low**: `string` = `"auto control mobile monitor phone"`

### brightness\_medium

> **brightness\_medium**: `string` = `"auto control mobile monitor phone"`

### broken\_image

> **broken\_image**: `string` = `"corrupt error landscape mountains photography picture torn"`

### browser\_not\_supported

> **browser\_not\_supported**: `string` = `"disabled enabled internet off on page screen slash website www"`

### browser\_updated

> **browser\_updated**: `string` = `"Android arrow chrome desktop device display download hardware iOS mac monitor screen web window"`

### brunch\_dining

> **brunch\_dining**: `string` = `"breakfast champagne champaign drink food lunch meal"`

### brush

> **brush**: `string` = `"art design draw editing painting tool"`

### bubble\_chart

> **bubble\_chart**: `string` = `"analytics bars data diagram infographic measure metrics statistics tracking"`

### bug\_report

> **bug\_report**: `string` = `"animal file fix insect issue problem testing ticket virus warning"`

### build

> **build**: `string` = `"adjust fix repair spanner tool wrench"`

### build\_circle

> **build\_circle**: `string` = `"adjust fix repair tool wrench"`

### bungalow

> **bungalow**: `string` = `"architecture cottage estate home house maps place real residence residential stay traveling"`

### burst\_mode

> **burst\_mode**: `string` = `"image landscape mountains multiple photography picture"`

### bus\_alert

> **bus\_alert**: `string` = `"! attention automobile cars caution danger error exclamation important maps mark notification symbol transportation vehicle warning"`

### business

> **business**: `string` = `"address apartment architecture building company estate flat home office place real residence residential shelter structure"`

### business\_center

> **business\_center**: `string` = `"baggage briefcase places purse suitcase work"`

### cabin

> **cabin**: `string` = `"architecture camping cottage estate home house log maps place real residence residential stay traveling wood"`

### cable

> **cable**: `string` = `"connection device electronics usb wire"`

### cached

> **cached**: `string` = `"around arrows inprogress loader loading refresh reload renew rotate"`

### cake

> **cake**: `string` = `"baked birthday candles celebration dessert food frosting party pastries pastry pie social sweet"`

### calculate

> **calculate**: `string` = `"+ - = calculator count finance math"`

### calendar\_today

> **calendar\_today**: `string` = `"date event month remember reminder schedule week"`

### calendar\_view\_day

> **calendar\_view\_day**: `string` = `"date event format grid layout month remember reminder schedule today week"`

### calendar\_view\_month

> **calendar\_view\_month**: `string` = `"date event format grid layout schedule today"`

### calendar\_view\_week

> **calendar\_view\_week**: `string` = `"date event format grid layout month schedule today"`

### call

> **call**: `string` = `"cell contact device hardware mobile talk telephone"`

### call\_end

> **call\_end**: `string` = `"cell contact device hardware mobile talk telephone"`

### call\_made

> **call\_made**: `string` = `"arrow device mobile"`

### call\_merge

> **call\_merge**: `string` = `"arrow device mobile"`

### call\_missed

> **call\_missed**: `string` = `"arrow device mobile"`

### call\_missed\_outgoing

> **call\_missed\_outgoing**: `string` = `"arrow device mobile"`

### call\_received

> **call\_received**: `string` = `"arrow device mobile"`

### call\_split

> **call\_split**: `string` = `"arrow device mobile"`

### call\_to\_action

> **call\_to\_action**: `string` = `"alert bar components cta design information interface layout message notification screen ui ux website window"`

### camera

> **camera**: `string` = `"album aperture lens photography picture record screenshot shutter"`

### camera\_alt

> **camera\_alt**: `string` = `"image photography picture"`

### camera\_enhance

> **camera\_enhance**: `string` = `"important lens photography picture quality special star"`

### camera\_front

> **camera\_front**: `string` = `"body human lens mobile person phone photography portrait selfie"`

### camera\_indoor

> **camera\_indoor**: `string` = `"architecture building estate filming home house image inside motion nest picture place real residence residential shelter videography"`

### camera\_outdoor

> **camera\_outdoor**: `string` = `"architecture building estate filming home house image motion nest outside picture place real residence residential shelter videography"`

### camera\_rear

> **camera\_rear**: `string` = `"front lens mobile phone photography picture portrait selfie"`

### camera\_roll

> **camera\_roll**: `string` = `"film image library photography"`

### cameraswitch

> **cameraswitch**: `string` = `"arrows flip rotate swap view"`

### campaign

> **campaign**: `string` = `"alert announcement loud megaphone microphone notification speaker"`

### cancel

> **cancel**: `string` = `"circle close cross disable exit status stop"`

### cancel\_presentation

> **cancel\_presentation**: `string` = `"close device exit no quit remove screen share slide stop website window"`

### cancel\_schedule\_send

> **cancel\_schedule\_send**: `string` = `"email no quit remove share stop x"`

### candlestick\_chart

> **candlestick\_chart**: `string` = `"analytics data diagram finance infographic measure metrics statistics tracking"`

### car\_rental

> **car\_rental**: `string` = `"automobile cars key maps transportation vehicle"`

### car\_repair

> **car\_repair**: `string` = `"automobile cars maps transportation vehicle"`

### card\_giftcard

> **card\_giftcard**: `string` = `"account balance bill cart cash certificate coin commerce creditcard currency dollars money online payment present shopping"`

### card\_membership

> **card\_membership**: `string` = `"bill bookmark cash certificate coin commerce cost creditcard currency dollars finance loyalty money online payment shopping subscription"`

### card\_travel

> **card\_travel**: `string` = `"bill cash coin commerce cost creditcard currency dollars finance membership miles money online payment trip"`

### carpenter

> **carpenter**: `string` = `"building construction cutting handyman repair saw tool"`

### cases

> **cases**: `string` = `"baggage briefcase business purse suitcase"`

### casino

> **casino**: `string` = `"dice dots entertainment gamble gambling games luck places"`

### cast

> **cast**: `string` = `"Android airplay chromecast connect desktop device display hardware iOS mac monitor screencast streaming television tv web window wireless"`

### cast\_connected

> **cast\_connected**: `string` = `"Android airplay chromecast desktop device display hardware iOS mac monitor screencast streaming television tv web window wireless"`

### cast\_for\_education

> **cast\_for\_education**: `string` = `"Android airplay chrome connect desktop device display hardware iOS learning lessons mac monitor screencast streaming teaching television tv web window wireless"`

### category

> **category**: `string` = `"categories circle collection items product sort square triangle"`

### celebration

> **celebration**: `string` = `"activity birthday event fun party"`

### cell\_tower

> **cell\_tower**: `string` = `"broadcast casting network signal transmitting wireless"`

### cell\_wifi

> **cell\_wifi**: `string` = `"connection data internet mobile network phone service signal wireless"`

### center\_focus\_strong

> **center\_focus\_strong**: `string` = `"camera image lens photography zoom"`

### center\_focus\_weak

> **center\_focus\_weak**: `string` = `"camera image lens photography zoom"`

### chair

> **chair**: `string` = `"comfort couch decoration furniture home house living lounging loveseat room seating sofa"`

### chair\_alt

> **chair\_alt**: `string` = `"cahir furniture home house kitchen lounging seating table"`

### chalet

> **chalet**: `string` = `"architecture cottage estate home house maps place real residence residential stay traveling"`

### change\_circle

> **change\_circle**: `string` = `"around arrows direction navigation rotate"`

### change\_history

> **change\_history**: `string` = `"shape triangle"`

### charging\_station

> **charging\_station**: `string` = `"Android battery cell device electric hardware iOS lightning mobile phone tablet thunderbolt"`

### chat

> **chat**: `string` = `"bubble comment communicate feedback message speech talk text"`

### chat\_bubble

> **chat\_bubble**: `string` = `"comment communicate feedback message speech talk text"`

### chat\_bubble\_outline

> **chat\_bubble\_outline**: `string` = `"comment communicate feedback message speech talk text"`

### check

> **check**: `string` = `"checkmark complete confirm correct done enter okay purchased select success tick yes"`

### check\_box

> **check\_box**: `string` = `"approved button checkmark component control form ok selected selection square success tick toggle ui yes"`

### check\_box\_outline\_blank

> **check\_box\_outline\_blank**: `string` = `"button checkmark component control deselected empty form selection square tick toggle ui"`

### check\_circle

> **check\_circle**: `string` = `"approve checkmark complete done download finished ok select success tick upload validate verified yes"`

### check\_circle\_outline

> **check\_circle\_outline**: `string` = `"approve checkmark complete done finished ok select success tick validate verified yes"`

### checkroom

> **checkroom**: `string` = `"check closet clothes coat hanger"`

### chevron\_left

> **chevron\_left**: `string` = `"arrows back direction triangle"`

### chevron\_right

> **chevron\_right**: `string` = `"arrows direction forward triangle"`

### child\_care

> **child\_care**: `string` = `"babies baby children face infant kids newborn toddler young"`

### child\_friendly

> **child\_friendly**: `string` = `"baby care carriage children infant kid newborn stroller toddler young"`

### chrome\_reader\_mode

> **chrome\_reader\_mode**: `string` = `"text"`

### circle

> **circle**: `string` = `"bullet button dot full geometry moon period radio"`

### circle\_notifications

> **circle\_notifications**: `string` = `"active alarm alert bell chime notify reminder ring sound"`

### class

> **class**: `string` = `"archive bookmark category favorite item label library reading remember ribbon save tag"`

### clean\_hands

> **clean\_hands**: `string` = `"bacteria disinfect germs gesture sanitizer"`

### cleaning\_services

> **cleaning\_services**: `string` = `"dust sweep"`

### clear

> **clear**: `string` = `"allowed back cancel correct cross delete disable erase exit not times"`

### clear\_all

> **clear\_all**: `string` = `"delete document erase format lines list notifications wipe"`

### close

> **close**: `string` = `"allowed cancel cross disable exit not status stop times"`

### close\_fullscreen

> **close\_fullscreen**: `string` = `"action arrows collapse direction minimize"`

### closed\_caption

> **closed\_caption**: `string` = `"accessible alphabet character decoder font language letter media movies subtitles symbol text tv type"`

### closed\_caption\_disabled

> **closed\_caption\_disabled**: `string` = `"accessible alphabet character decoder enabled font language letter media movies off slash subtitles symbol text tv type"`

### closed\_caption\_off

> **closed\_caption\_off**: `string` = `"accessible alphabet character decoder font language letter media movies outline subtitles symbol text tv type"`

### cloud

> **cloud**: `string` = `"connection internet network sky upload weather"`

### cloud\_circle

> **cloud\_circle**: `string` = `"application backup connection drive files folders internet network sky storage upload"`

### cloud\_done

> **cloud\_done**: `string` = `"application approve backup checkmark complete connection drive files folders internet network ok select sky storage tick upload validate verified yes"`

### cloud\_download

> **cloud\_download**: `string` = `"application arrow backup connection drive files folders internet network sky storage upload"`

### cloud\_off

> **cloud\_off**: `string` = `"application backup connection disabled drive enabled files folders internet network offline sky slash storage upload"`

### cloud\_queue

> **cloud\_queue**: `string` = `"connection internet network sky upload"`

### cloud\_sync

> **cloud\_sync**: `string` = `"application around backup connection drive files folders inprogress internet loading network refresh renew rotate sky storage turn upload"`

### cloud\_upload

> **cloud\_upload**: `string` = `"application arrow backup connection download drive files folders internet network sky storage"`

### co\_present

> **co\_present**: `string` = `"arrow co-present presentation screen share slides togather website"`

### co2

> **co2**: `string` = `"carbon dioxide gas"`

### code

> **code**: `string` = `"brackets css developer engineering html parenthesis platform"`

### code\_off

> **code\_off**: `string` = `"brackets css developer disabled enabled engineering html on platform slash"`

### coffee

> **coffee**: `string` = `"beverage cup drink mug plate set tea"`

### coffee\_maker

> **coffee\_maker**: `string` = `"appliances beverage cup drink machine mug"`

### collections

> **collections**: `string` = `"album gallery image landscape library mountains photography picture stack"`

### collections\_bookmark

> **collections\_bookmark**: `string` = `"album archive favorite gallery label library reading remember ribbon save stack tag"`

### color\_lens

> **color\_lens**: `string` = `"art paint pallet"`

### colorize

> **colorize**: `string` = `"color dropper extract eye picker pipette tool"`

### comment

> **comment**: `string` = `"bubble chat communicate document feedback message note outline speech"`

### comment\_bank

> **comment\_bank**: `string` = `"archive bookmark bubble cchat communicate favorite label library message remember ribbon save speech tag"`

### comments\_disabled

> **comments\_disabled**: `string` = `"bubble chat communicate enabled feedback message offline on slash speech"`

### commit

> **commit**: `string` = `"accomplish bind circle dedicate execute line perform pledge"`

### commute

> **commute**: `string` = `"automobile car direction maps public train transportation trip vehicle"`

### compare

> **compare**: `string` = `"adjustment editing edits enhance fix images photography photos scan settings"`

### compare\_arrows

> **compare\_arrows**: `string` = `"collide directional facing left pointing pressure push right together"`

### compass\_calibration

> **compass\_calibration**: `string` = `"connection internet location maps network refresh service signal wifi wireless"`

### compress

> **compress**: `string` = `"arrows collide pressure push together"`

### computer

> **computer**: `string` = `"Android chrome desktop device hardware iOS laptop mac monitor pc web window"`

### confirmation\_number

> **confirmation\_number**: `string` = `"admission entertainment event ticket"`

### connect\_without\_contact

> **connect\_without\_contact**: `string` = `"communicating distance people signal socialize"`

### connected\_tv

> **connected\_tv**: `string` = `"Android airplay chrome desktop device display hardware iOS mac monitor screencast streaming television web window wireless"`

### connecting\_airports

> **connecting\_airports**: `string` = `"airplanes flight transportation travel trip"`

### construction

> **construction**: `string` = `"build carpenter equipment fix hammer improvement industrial industry repair tools wrench"`

### contact\_mail

> **contact\_mail**: `string` = `"account address avatar communicate email face human information message people person profile user"`

### contact\_page

> **contact\_page**: `string` = `"account avatar data document drive face folders human people person profile sheet slide storage user writing"`

### contact\_phone

> **contact\_phone**: `string` = `"account avatar call communicate face human information message mobile number people person profile user"`

### contact\_support

> **contact\_support**: `string` = `"? alert announcement bubble chat comment communicate help information mark message punctuation speech symbol vquestion"`

### contactless

> **contactless**: `string` = `"applepay bluetooth cash connection connectivity credit device finance payment signal tap transaction wifi wireless"`

### contacts

> **contacts**: `string` = `"account address avatar call cell face human information mobile number people person phone profile user"`

### content\_copy

> **content\_copy**: `string` = `"copy document duplicate file multiple past"`

### content\_cut

> **content\_cut**: `string` = `"cut document file past scissors trim"`

### content\_paste

> **content\_paste**: `string` = `"clipboard copy cut document file multiple"`

### content\_paste\_go

> **content\_paste\_go**: `string` = `"clipboard disabled document enabled file slash"`

### content\_paste\_off

> **content\_paste\_off**: `string` = `"clipboard disabled document enabled file slash"`

### content\_paste\_search

> **content\_paste\_search**: `string` = `"clipboard document file find trace track"`

### contrast

> **contrast**: `string` = `"black editing effect filter grayscale images photography pictures settings white"`

### control\_camera

> **control\_camera**: `string` = `"adjust arrows center direction left move right"`

### control\_point

> **control\_point**: `string` = `"+ add circle plus"`

### control\_point\_duplicate

> **control\_point\_duplicate**: `string` = `"+ add circle multiple new plus symbol"`

### copy\_all

> **copy\_all**: `string` = `"content cut document file multiple page paper past"`

### copyright

> **copyright**: `string` = `"alphabet character circle emblem font legal letter owner symbol text"`

### coronavirus

> **coronavirus**: `string` = `"19 bacteria covid disease germs illness sick social"`

### corporate\_fare

> **corporate\_fare**: `string` = `"architecture building business estate organization place real residence residential shelter"`

### cottage

> **cottage**: `string` = `"architecture beach estate home house lake lodge maps place real residence residential stay traveling"`

### countertops

> **countertops**: `string` = `"home house kitchen sink table"`

### create

> **create**: `string` = `"compose editing input item new pencil write writing"`

### create\_new\_folder

> **create\_new\_folder**: `string` = `"+ add data directory document drive file plus sheet slide storage symbol"`

### credit\_card

> **credit\_card**: `string` = `"bill cash charge coin commerce cost creditcard currency dollars finance information money online payment price shopping symbol"`

### credit\_card\_off

> **credit\_card\_off**: `string` = `"charge commerce cost disabled enabled finance money online payment slash"`

### credit\_score

> **credit\_score**: `string` = `"approve bill card cash check coin commerce complete cost currency dollars done finance loan mark money ok online payment select symbol tick validate verified yes"`

### crib

> **crib**: `string` = `"babies baby bassinet bed children cradle infant kid newborn sleeping toddler"`

### crop

> **crop**: `string` = `"adjustments area editing frame images photos rectangle settings size square"`

### crop\_din

> **crop\_din**: `string` = `"adjustments area editing frame images photos picture rectangle settings size square"`

### crop\_free

> **crop\_free**: `string` = `"adjustments barcode editing focus frame image photos qrcode settings size square zoom"`

### crop\_landscape

> **crop\_landscape**: `string` = `"adjustments area editing frame images photos picture settings size square"`

### crop\_original

> **crop\_original**: `string` = `"adjustments area editing frame images photos picture settings size square"`

### crop\_portrait

> **crop\_portrait**: `string` = `"adjustments area editing frame images photos picture rectangle settings size square"`

### crop\_rotate

> **crop\_rotate**: `string` = `"adjustments area arrows editing frame images photos settings size turn"`

### crop\_square

> **crop\_square**: `string` = `"adjustments area editing frame images photos rectangle settings size"`

### css

> **css**: `string` = `"alphabet brackets character code developer engineering font html letter platform symbol text type"`

### currency\_exchange

> **currency\_exchange**: `string` = `"360 around arrows cash coin commerce direction dollars inprogress money pay renew rotate sync turn universal"`

### currency\_franc

> **currency\_franc**: `string` = `"bill card cash coin commerce cost credit dollars finance money online payment price shopping symbol"`

### currency\_lira

> **currency\_lira**: `string` = `"bill card cash coin commerce cost credit dollars finance money online payment price shopping symbol"`

### currency\_pound

> **currency\_pound**: `string` = `"bill card cash coin commerce cost credit dollars finance money online payment price shopping symbol"`

### currency\_ruble

> **currency\_ruble**: `string` = `"bill card cash coin commerce cost credit dollars finance money online payment price shopping symbol"`

### currency\_rupee

> **currency\_rupee**: `string` = `"bill card cash coin commerce cost credit dollars finance money online payment price shopping symbol"`

### currency\_yen

> **currency\_yen**: `string` = `"bill card cash coin commerce cost credit dollars finance money online payment price shopping symbol"`

### currency\_yuan

> **currency\_yuan**: `string` = `"bill card cash coin commerce cost credit dollars finance money online payment price shopping symbol"`

### curtains

> **curtains**: `string` = `"blinds cover nest open shutter sunshade"`

### curtains\_closed

> **curtains\_closed**: `string` = `"blinds cover nest shutter sunshade"`

### dangerous

> **dangerous**: `string` = `"broken fix no sign stop update warning wrong"`

### dark\_mode

> **dark\_mode**: `string` = `"application device interface moon night silent theme ui ux website"`

### dashboard

> **dashboard**: `string` = `"cards format layout rectangle shapes square website"`

### dashboard\_customize

> **dashboard\_customize**: `string` = `"cards format layout rectangle shapes square website"`

### data\_saver\_off

> **data\_saver\_off**: `string` = `"analytics bars chart diagram donut infographic measure metrics ring statistics tracking"`

### data\_saver\_on

> **data\_saver\_on**: `string` = `"+ add analytics chart diagram infographic measure metrics new plus ring statistics symbol tracking"`

### data\_thresholding

> **data\_thresholding**: `string` = `"hidden privacy thresold"`

### data\_usage

> **data\_usage**: `string` = `"analytics chart circle diagram infographic measure metrics statistics tracking"`

### date\_range

> **date\_range**: `string` = `"agenda calendar event month remember reminder schedule time today week"`

### deblur

> **deblur**: `string` = `"adjust editing enhance face image lines photography sharpen"`

### deck

> **deck**: `string` = `"chairs furniture garden home house outdoors outside patio social terrace umbrella yard"`

### dehaze

> **dehaze**: `string` = `"adjust editing enhance image lines photography remove"`

### delete

> **delete**: `string` = `"bin garbage junk recycle remove trashcan"`

### delete\_forever

> **delete\_forever**: `string` = `"bin cancel exit garbage junk recycle remove trashcan"`

### delete\_outline

> **delete\_outline**: `string` = `"bin can garbage remove trash"`

### delete\_sweep

> **delete\_sweep**: `string` = `"bin garbage junk recycle remove trashcan"`

### density\_large

> **density\_large**: `string` = `"horizontal lines rules"`

### density\_medium

> **density\_medium**: `string` = `"horizontal lines rules"`

### density\_small

> **density\_small**: `string` = `"horizontal lines rules"`

### departure\_board

> **departure\_board**: `string` = `"automobile bus cars clock maps public schedule time transportation travel vehicle"`

### description

> **description**: `string` = `"article bill data document drive file folders invoice item notes page paper sheet slide text writing"`

### desktop\_access\_disabled

> **desktop\_access\_disabled**: `string` = `"Android apple chrome device display enabled hardware iOS mac monitor offline pc screen slash web window"`

### desktop\_mac

> **desktop\_mac**: `string` = `"Android apple chrome device display hardware iOS monitor pc screen web window"`

### desktop\_windows

> **desktop\_windows**: `string` = `"Android chrome device display hardware iOS mac monitor pc screen television tv web"`

### details

> **details**: `string` = `"editing enhance image photography sharpen triangle"`

### developer\_board

> **developer\_board**: `string` = `"computer development devkit hardware microchip processor"`

### developer\_board\_off

> **developer\_board\_off**: `string` = `"computer development disabled enabled hardware microchip on processor slash"`

### developer\_mode

> **developer\_mode**: `string` = `"Android bracket cell code development device engineer hardware iOS mobile phone tablet"`

### device\_hub

> **device\_hub**: `string` = `"Android circle computer desktop hardware iOS laptop mobile monitor phone square tablet triangle watch wearable web"`

### device\_thermostat

> **device\_thermostat**: `string` = `"celsius fahrenheit temperature thermometer"`

### device\_unknown

> **device\_unknown**: `string` = `"? Android assistance cell hardware help iOS information mark mobile phone punctuation question support symbol tablet"`

### devices

> **devices**: `string` = `"Android computer desktop hardware iOS laptop mobile monitor phone tablet watch wearable web"`

### devices\_other

> **devices\_other**: `string` = `"Android cell chrome desktop gadget hardware iOS ipad mac mobile monitor phone smartwatch tablet vr wearables window"`

### dialer\_sip

> **dialer\_sip**: `string` = `"alphabet call cell character contact device font hardware initiation internet letter mobile over protocol routing session symbol telephone text type voice"`

### dialpad

> **dialpad**: `string` = `"buttons call contact device dots mobile numbers phone"`

### diamond

> **diamond**: `string` = `"fashion gems jewelry logo retail valuables"`

### difference

> **difference**: `string` = `"compare content copy cut document duplicate file multiple past"`

### dining

> **dining**: `string` = `"cafeteria cutlery diner eating fork room spoon"`

### dinner\_dining

> **dinner\_dining**: `string` = `"breakfast food fork lunch meal restaurant spaghetti utensils"`

### directions

> **directions**: `string` = `"arrow maps naviate right route sign traffic"`

### directions\_bike

> **directions\_bike**: `string` = `"bicycle human maps person public route transportation"`

### directions\_boat

> **directions\_boat**: `string` = `"automobile cars ferry maps public transportation vehicle"`

### directions\_boat\_filled

> **directions\_boat\_filled**: `string` = `"automobile cars ferry maps public transportation vehicle"`

### directions\_bus

> **directions\_bus**: `string` = `"automobile cars maps public transportation vehicle"`

### directions\_bus\_filled

> **directions\_bus\_filled**: `string` = `"automobile cars maps public transportation vehicle"`

### directions\_car

> **directions\_car**: `string` = `"automobile cars maps public transportation vehicle"`

### directions\_car\_filled

> **directions\_car\_filled**: `string` = `"automobile cars maps public transportation vehicle"`

### directions\_off

> **directions\_off**: `string` = `"arrow disabled enabled maps right route sign slash traffic"`

### directions\_railway

> **directions\_railway**: `string` = `"automobile cars maps public train transportation vehicle"`

### directions\_railway\_filled

> **directions\_railway\_filled**: `string` = `"automobile cars maps public train transportation vehicle"`

### directions\_run

> **directions\_run**: `string` = `"body health human jogging maps people person route running walk"`

### directions\_subway

> **directions\_subway**: `string` = `"automobile cars maps public rail train transportation vehicle"`

### directions\_subway\_filled

> **directions\_subway\_filled**: `string` = `"automobile cars maps public rail train transportation vehicle"`

### directions\_transit

> **directions\_transit**: `string` = `"automobile cars maps metro public rail subway train transportation vehicle"`

### directions\_transit\_filled

> **directions\_transit\_filled**: `string` = `"automobile cars maps public rail subway train transportation vehicle"`

### directions\_walk

> **directions\_walk**: `string` = `"body human jogging maps people person route run"`

### dirty\_lens

> **dirty\_lens**: `string` = `"camera photography picture splat"`

### disabled\_by\_default

> **disabled\_by\_default**: `string` = `"box cancel close exit no quit remove square stop"`

### disc\_full

> **disc\_full**: `string` = `"! alert attention caution cd danger error exclamation important mark music notification storage symbol vinyl warning"`

### display\_settings

> **display\_settings**: `string` = `"Android application change chrome desktop details device gear hardware iOS information mac monitor options personal screen service web window"`

### dns

> **dns**: `string` = `"address bars domain information ip list lookup name network server system"`

### do\_disturb

> **do\_disturb**: `string` = `"cancel close denied deny remove silence stop"`

### do\_disturb\_alt

> **do\_disturb\_alt**: `string` = `"cancel close denied deny remove silence stop"`

### do\_disturb\_off

> **do\_disturb\_off**: `string` = `"cancel close denied deny disabled enabled on remove silence slash stop"`

### do\_disturb\_on

> **do\_disturb\_on**: `string` = `"cancel close denied deny disabled enabled off remove silence slash stop"`

### do\_not\_disturb

> **do\_not\_disturb**: `string` = `"cancel close denied deny remove silence stop"`

### do\_not\_disturb\_alt

> **do\_not\_disturb\_alt**: `string` = `"cancel close denied deny remove silence stop"`

### do\_not\_disturb\_off

> **do\_not\_disturb\_off**: `string` = `"cancel close denied deny disabled enabled on remove silence slash stop"`

### do\_not\_disturb\_on

> **do\_not\_disturb\_on**: `string` = `"cancel close denied deny disabled enabled off remove silence slash stop"`

### do\_not\_disturb\_on\_total\_silence

> **do\_not\_disturb\_on\_total\_silence**: `string` = `"busy mute on quiet total"`

### do\_not\_step

> **do\_not\_step**: `string` = `"boot disabled enabled feet foot off on shoe slash sneaker"`

### do\_not\_touch

> **do\_not\_touch**: `string` = `"disabled enabled fingers gesture hand off on slash"`

### dock

> **dock**: `string` = `"Android cell charger charging connector device hardware iOS mobile phone power station tablet"`

### document\_scanner

> **document\_scanner**: `string` = `"article data drive file folders notes page paper sheet slide text writing"`

### domain

> **domain**: `string` = `"apartment architecture building business estate home place real residence residential shelter web www"`

### domain\_add

> **domain\_add**: `string` = `"+ apartment architecture building business estate home new place plus real residence residential shelter symbol web www"`

### domain\_disabled

> **domain\_disabled**: `string` = `"apartment architecture building business company enabled estate home internet maps office offline on place real residence residential slash website"`

### domain\_verification

> **domain\_verification**: `string` = `"application approve check complete design desktop done interface internet layout mark ok screen select tick ui ux validate verified website window www yes"`

### done

> **done**: `string` = `"approve checkmark complete finished ok select success tick validate verified yes"`

### done\_all

> **done\_all**: `string` = `"approve checkmark complete finished layers multiple ok select stack success tick validate verified yes"`

### done\_outline

> **done\_outline**: `string` = `"all approve checkmark complete finished ok select success tick validate verified yes"`

### donut\_large

> **donut\_large**: `string` = `"analytics chart circle complete data diagram infographic inprogress, measure metrics pie statistics tracking"`

### donut\_small

> **donut\_small**: `string` = `"analytics chart circle data diagram infographic inprogress measure metrics pie statistics tracking"`

### door\_back

> **door\_back**: `string` = `"closed doorway entrance exit home house"`

### door\_front

> **door\_front**: `string` = `"closed doorway entrance exit home house"`

### door\_sliding

> **door\_sliding**: `string` = `"automatic doorway double entrance exit glass home house two"`

### doorbell

> **doorbell**: `string` = `"alarm home house ringing"`

### double\_arrow

> **double\_arrow**: `string` = `"arrows chevron direction multiple navigation right"`

### downhill\_skiing

> **downhill\_skiing**: `string` = `"athlete athletic body entertainment exercise hobby human people person ski snow social sports travel winter"`

### download

> **download**: `string` = `"arrow downloads drive install upload"`

### download\_done

> **download\_done**: `string` = `"arrows check downloads drive installed ok tick upload"`

### download\_for\_offline

> **download\_for\_offline**: `string` = `"arrow circle for install offline upload"`

### downloading

> **downloading**: `string` = `"arrow circle downloads install pending progress upload"`

### drafts

> **drafts**: `string` = `"document email envelope file letter message read"`

### drag\_handle

> **drag\_handle**: `string` = `"application components design interface layout lines menu move screen ui ux website window"`

### drag\_indicator

> **drag\_indicator**: `string` = `"application circles components design dots drop interface layout mobile monitor move phone screen shape shift tablet ui ux website window"`

### drive\_eta

> **drive\_eta**: `string` = `"automobile cars destination direction estimate maps public transportation travel trip vehicle"`

### drive\_file\_move

> **drive\_file\_move**: `string` = `"arrows data direction document folders right sheet side slide storage"`

### drive\_file\_rename\_outline

> **drive\_file\_rename\_outline**: `string` = `"compose create draft editing input pencil write writing"`

### drive\_folder\_upload

> **drive\_folder\_upload**: `string` = `"arrow data document file sheet slide storage"`

### dry

> **dry**: `string` = `"air bathroom dryer fingers gesture hand wc"`

### dry\_cleaning

> **dry\_cleaning**: `string` = `"hanger hotel laundry places service towel"`

### duo

> **duo**: `string` = `"call chat conference device video"`

### dvr

> **dvr**: `string` = `"Android audio chrome computer desktop device display electronic hardware iOS laptop list mac monitor recorder screen tv video web window"`

### dynamic\_feed

> **dynamic\_feed**: `string` = `"layer live multiple post refresh update"`

### dynamic\_form

> **dynamic\_form**: `string` = `"code electric fast lightning lists questionnaire thunderbolt"`

### e\_mobiledata

> **e\_mobiledata**: `string` = `"alphabet font letter text type"`

### earbuds

> **earbuds**: `string` = `"accessory audio earphone headphone listen music sound"`

### earbuds\_battery

> **earbuds\_battery**: `string` = `"accessory audio charging earphone headphone listen music sound"`

### east

> **east**: `string` = `"arrow directional maps navigation right"`

### edgesensor\_high

> **edgesensor\_high**: `string` = `"Android cell device hardware iOS mobile move phone sensitivity tablet vibrate"`

### edgesensor\_low

> **edgesensor\_low**: `string` = `"Android cell device hardware iOS mobile move phone sensitivity tablet vibrate"`

### edit

> **edit**: `string` = `"compose create editing input new pencil write writing"`

### edit\_attributes

> **edit\_attributes**: `string` = `"approve attribution check complete done mark ok select tick validate verified yes"`

### edit\_location

> **edit\_location**: `string` = `"destination direction gps maps pencil pin place stop write"`

### edit\_location\_alt

> **edit\_location\_alt**: `string` = `"pencil pin"`

### edit\_notifications

> **edit\_notifications**: `string` = `"active alarm alert bell chime compose create draft editing input new notify pencil reminder ring sound write writing"`

### edit\_off

> **edit\_off**: `string` = `"compose create disabled draft editing enabled input new offline on pencil slash write writing"`

### edit\_road

> **edit\_road**: `string` = `"destination direction highway maps pencil street traffic"`

### egg

> **egg**: `string` = `"breakfast brunch food"`

### egg\_alt

> **egg\_alt**: `string` = `"breakfast brunch food"`

### eject

> **eject**: `string` = `"arrow disc drive dvd player remove triangle up usb"`

### elderly

> **elderly**: `string` = `"body cane human old people person senior"`

### elderly\_woman

> **elderly\_woman**: `string` = `"body cane female gender girl human lady old people person senior social symbol women"`

### electric\_bike

> **electric\_bike**: `string` = `"automobile cars electricity maps scooter transportation travel vehicle vespa"`

### electric\_bolt

> **electric\_bolt**: `string` = `"energy fast lightning nest thunderbolt"`

### electric\_car

> **electric\_car**: `string` = `"automobile cars electricity maps transportation travel vehicle"`

### electric\_meter

> **electric\_meter**: `string` = `"energy fast lightning measure nest thunderbolt usage voltage volts"`

### electric\_moped

> **electric\_moped**: `string` = `"automobile bike cars maps scooter transportation travel vehicle vespa"`

### electric\_rickshaw

> **electric\_rickshaw**: `string` = `"automobile cars india maps transportation truck vehicle"`

### electric\_scooter

> **electric\_scooter**: `string` = `"automobile bike cars maps transportation vehicle vespa"`

### electrical\_services

> **electrical\_services**: `string` = `"charge cord plug power wire"`

### elevator

> **elevator**: `string` = `"body down human people person up"`

### email

> **email**: `string` = `"envelope letter message note post receive send write"`

### emoji\_emotions

> **emoji\_emotions**: `string` = `"emoticon expressions face feelings glad happiness happy like mood person pleased smiley smiling social survey"`

### emoji\_events

> **emoji\_events**: `string` = `"achievement award chalice champion cup first prize reward sport trophy winner"`

### emoji\_food\_beverage

> **emoji\_food\_beverage**: `string` = `"coffee cup dring drink mug plate set tea"`

### emoji\_nature

> **emoji\_nature**: `string` = `"animal bee daisy flower honey insect ladybug petals spring summer"`

### emoji\_objects

> **emoji\_objects**: `string` = `"creative idea lamp lightbulb solution thinking"`

### emoji\_people

> **emoji\_people**: `string` = `"arm body greeting human person social wave waving"`

### emoji\_symbols

> **emoji\_symbols**: `string` = `"ampersand character hieroglyph music note percent sign"`

### emoji\_transportation

> **emoji\_transportation**: `string` = `"architecture automobile building cars commute company direction estate maps office place public real residence residential shelter travel vehicle"`

### energy\_savings\_leaf

> **energy\_savings\_leaf**: `string` = `"eco leaves nest usage"`

### engineering

> **engineering**: `string` = `"body cogs cogwheel construction fixing gears hat helmet human maintenance people person setting worker"`

### enhanced\_encryption

> **enhanced\_encryption**: `string` = `"+ add locked new password plus privacy private protection safety secure security symbol"`

### equalizer

> **equalizer**: `string` = `"adjustment analytics chart data graph measure metrics music noise sound static statistics tracking volume"`

### error

> **error**: `string` = `"! alert announcement attention caution circle danger exclamation feedback important mark notification problem symbol warning"`

### error\_outline

> **error\_outline**: `string` = `"! alert announcement attention caution circle danger exclamation feedback important mark notification problem symbol warning"`

### escalator

> **escalator**: `string` = `"down staircase up"`

### escalator\_warning

> **escalator\_warning**: `string` = `"body child human kid parent people person"`

### euro

> **euro**: `string` = `"bill card cash coin commerce cost credit currency dollars euros finance money online payment price profit shopping symbol"`

### euro\_symbol

> **euro\_symbol**: `string` = `"bill card cash coin commerce cost credit currency dollars finance money online payment price profit"`

### ev\_station

> **ev\_station**: `string` = `"automobile cars charge charging electricity filling fuel gasoline maps places power station transportation vehicle"`

### event

> **event**: `string` = `"agenda calendar date item mark month range remember reminder today week"`

### event\_available

> **event\_available**: `string` = `"agenda approve calendar check complete done item mark ok schedule select tick time validate verified yes"`

### event\_busy

> **event\_busy**: `string` = `"agenda calendar cancel close date exit item no remove schedule stop time unavailable"`

### event\_note

> **event\_note**: `string` = `"agenda calendar date item schedule text time writing"`

### event\_repeat

> **event\_repeat**: `string` = `"around calendar date day inprogress loading month refresh renew rotate schedule turn"`

### event\_seat

> **event\_seat**: `string` = `"assigned bench chair furniture reservation row section sit"`

### exit\_to\_app

> **exit\_to\_app**: `string` = `"application arrow back components design export interface layout leave login logout mobile monitor move output phone pointing quit register right screen signin signout signup tablet ux website window"`

### expand

> **expand**: `string` = `"arrows compress enlarge grow move push together"`

### expand\_circle\_down

> **expand\_circle\_down**: `string` = `"arrows chevron collapse direction expandable list more"`

### expand\_less

> **expand\_less**: `string` = `"arrows chevron collapse direction expandable list up"`

### expand\_more

> **expand\_more**: `string` = `"arrows chevron collapse direction down expandable list"`

### explicit

> **explicit**: `string` = `"adult alphabet character content font language letter media movies music parent rating supervision symbol text type"`

### explore

> **explore**: `string` = `"compass destination direction east location maps needle north south travel west"`

### explore\_off

> **explore\_off**: `string` = `"compass destination direction disabled east enabled location maps needle north slash south travel west"`

### exposure

> **exposure**: `string` = `"add brightness contrast editing effect image minus photography picture plus settings subtract"`

### extension

> **extension**: `string` = `"add-ons app extended game item jigsaw piece plugin puzzle shape"`

### extension\_off

> **extension\_off**: `string` = `"disabled enabled extended jigsaw piece puzzle shape slash"`

### face

> **face**: `string` = `"account avatar emoji eyes human login logout people person profile recognition security social thumbnail unlock user"`

### face\_retouching\_natural

> **face\_retouching\_natural**: `string` = `"editing effect emoji emotion faces image photography settings star tag"`

### face\_retouching\_off

> **face\_retouching\_off**: `string` = `"disabled editing effect emoji emotion enabled faces image natural photography settings slash tag"`

### fact\_check

> **fact\_check**: `string` = `"approve complete done list mark ok select tick validate verified yes"`

### factory

> **factory**: `string` = `"industry manufacturing warehouse"`

### family\_restroom

> **family\_restroom**: `string` = `"bathroom children father kids mother parents wc"`

### fast\_forward

> **fast\_forward**: `string` = `"control ff media music play speed time tv video"`

### fast\_rewind

> **fast\_rewind**: `string` = `"back control media music play speed time tv video"`

### fastfood

> **fastfood**: `string` = `"drink hamburger maps meal places"`

### favorite

> **favorite**: `string` = `"appreciate health heart like love remember save shape success"`

### favorite\_border

> **favorite\_border**: `string` = `"health heart like love outline remember save shape success"`

### fax

> **fax**: `string` = `"machine office phone send"`

### featured\_play\_list

> **featured\_play\_list**: `string` = `"audio collection highlighted item music playlist recommended"`

### featured\_video

> **featured\_video**: `string` = `"advertisement advertisment highlighted item play recommended watch,advertised"`

### feed

> **feed**: `string` = `"article headline information newspaper public social timeline"`

### feedback

> **feedback**: `string` = `"! alert announcement attention bubble caution chat comment communicate danger error exclamation important mark message notification speech symbol warning"`

### female

> **female**: `string` = `"gender girl lady social symbol woman women"`

### fence

> **fence**: `string` = `"backyard barrier boundaries boundary home house protection"`

### festival

> **festival**: `string` = `"circus event local maps places tent tour travel"`

### fiber\_dvr

> **fiber\_dvr**: `string` = `"alphabet character digital electronics font letter network recorder symbol text tv type video"`

### fiber\_manual\_record

> **fiber\_manual\_record**: `string` = `"circle dot play watch"`

### fiber\_new

> **fiber\_new**: `string` = `"alphabet character font letter network symbol text type"`

### fiber\_pin

> **fiber\_pin**: `string` = `"alphabet character font letter network symbol text type"`

### fiber\_smart\_record

> **fiber\_smart\_record**: `string` = `"circle dot play watch"`

### file\_copy

> **file\_copy**: `string` = `"bill clone content cut document duplicate invoice item multiple page past"`

### file\_download

> **file\_download**: `string` = `"arrows downloads drive export install upload"`

### file\_download\_done

> **file\_download\_done**: `string` = `"arrows check downloads drive installed tick upload"`

### file\_download\_off

> **file\_download\_off**: `string` = `"arrow disabled drive enabled export install on save slash upload"`

### file\_open

> **file\_open**: `string` = `"arrow document drive left page paper"`

### file\_present

> **file\_present**: `string` = `"clip data document drive folders note paper reminder sheet slide storage writing"`

### file\_upload

> **file\_upload**: `string` = `"arrows download drive export"`

### filter

> **filter**: `string` = `"editing effect image landscape mountains photography picture settings"`

### filter\_1

> **filter\_1**: `string` = `"digit editing effect images multiple number photography pictures settings stack symbol"`

### filter\_2

> **filter\_2**: `string` = `"digit editing effect images multiple number photography pictures settings stack symbol"`

### filter\_3

> **filter\_3**: `string` = `"digit editing effect images multiple number photography pictures settings stack symbol"`

### filter\_4

> **filter\_4**: `string` = `"digit editing effect images multiple number photography pictures settings stack symbol"`

### filter\_5

> **filter\_5**: `string` = `"digit editing effect images multiple number photography pictures settings stack symbol"`

### filter\_6

> **filter\_6**: `string` = `"digit editing effect images multiple number photography pictures settings stack symbol"`

### filter\_7

> **filter\_7**: `string` = `"digit editing effect images multiple number photography pictures settings stack symbol"`

### filter\_8

> **filter\_8**: `string` = `"digit editing effect images multiple number photography pictures settings stack symbol"`

### filter\_9

> **filter\_9**: `string` = `"digit editing effect images multiple number photography pictures settings stack symbol"`

### filter\_9\_plus

> **filter\_9\_plus**: `string` = `"+ digit editing effect images multiple number photography pictures settings stack symbol"`

### filter\_alt

> **filter\_alt**: `string` = `"edit funnel options refine sift"`

### filter\_alt\_off

> **filter\_alt\_off**: `string` = `"[offline] disabled edit funnel options refine sift slash"`

### filter\_b\_and\_w

> **filter\_b\_and\_w**: `string` = `"black contrast editing effect grayscale images photography pictures settings white"`

### filter\_center\_focus

> **filter\_center\_focus**: `string` = `"camera dot edit image photography picture"`

### filter\_drama

> **filter\_drama**: `string` = `"camera cloud editing effect image photography picture sky"`

### filter\_frames

> **filter\_frames**: `string` = `"boarders border camera center editing effect filters focus image options photography picture"`

### filter\_hdr

> **filter\_hdr**: `string` = `"camera editing effect image mountains photography picture"`

### filter\_list

> **filter\_list**: `string` = `"lines organize sort"`

### filter\_list\_off

> **filter\_list\_off**: `string` = `"[offline] alt disabled edit options refine sift slash"`

### filter\_none

> **filter\_none**: `string` = `"multiple stack"`

### filter\_tilt\_shift

> **filter\_tilt\_shift**: `string` = `"blur center editing effect focus images photography pictures"`

### filter\_vintage

> **filter\_vintage**: `string` = `"editing effect flower images photography pictures"`

### find\_in\_page

> **find\_in\_page**: `string` = `"data document drive file folders glass look magnifying paper search see sheet slide writing"`

### find\_replace

> **find\_replace**: `string` = `"around arrows glass inprogress loading look magnifying refresh renew rotate search see"`

### fingerprint

> **fingerprint**: `string` = `"biometrics identification identity reader thumbprint touchid verification"`

### fire\_extinguisher

> **fire\_extinguisher**: `string` = `"emergency water"`

### fireplace

> **fireplace**: `string` = `"chimney flame home house living pit room warm winter"`

### first\_page

> **first\_page**: `string` = `"arrow back chevron left rewind"`

### fit\_screen

> **fit\_screen**: `string` = `"enlarge format layout reduce scale size"`

### fitness\_center

> **fitness\_center**: `string` = `"athlete dumbbell exercise gym health hobby places sport weights workout"`

### flag

> **flag**: `string` = `"country goal mark nation report start"`

### flag\_circle

> **flag\_circle**: `string` = `"country goal mark nation report round start"`

### flaky

> **flaky**: `string` = `"approve check close complete contrast done exit mark no ok options select stop tick verified yes"`

### flare

> **flare**: `string` = `"bright editing effect images lensflare light photography pictures shine sparkle star sun"`

### flash\_auto

> **flash\_auto**: `string` = `"camera electric fast lightning thunderbolt"`

### flash\_off

> **flash\_off**: `string` = `"camera disabled electric enabled fast lightning on slash thunderbolt"`

### flash\_on

> **flash\_on**: `string` = `"camera disabled electric enabled fast lightning off slash thunderbolt"`

### flashlight\_off

> **flashlight\_off**: `string` = `"disabled enabled on slash"`

### flashlight\_on

> **flashlight\_on**: `string` = `"disabled enabled off slash"`

### flatware

> **flatware**: `string` = `"cafeteria cutlery diner dining eating fork room spoon"`

### flight

> **flight**: `string` = `"airplane airport flying transportation travel trip"`

### flight\_class

> **flight\_class**: `string` = `"airplane business first seat transportation travel trip window"`

### flight\_land

> **flight\_land**: `string` = `"airplane airport arrival arriving flying landing transportation travel"`

### flight\_takeoff

> **flight\_takeoff**: `string` = `"airplane airport departed departing flying landing transportation travel"`

### flip

> **flip**: `string` = `"editing image orientation scanning"`

### flip\_camera\_android

> **flip\_camera\_android**: `string` = `"center editing front image mobile orientation rear reverse rotate turn"`

### flip\_camera\_ios

> **flip\_camera\_ios**: `string` = `"android editing front image mobile orientation rear reverse rotate turn"`

### flip\_to\_back

> **flip\_to\_back**: `string` = `"arrangement format front layout move order sort"`

### flip\_to\_front

> **flip\_to\_front**: `string` = `"arrangement back format layout move order sort"`

### flutter\_dash

> **flutter\_dash**: `string` = `"bird mascot"`

### fmd\_bad

> **fmd\_bad**: `string` = `"! alert attention caution danger destination direction error exclamation important location maps mark notification pin place symbol warning"`

### fmd\_good

> **fmd\_good**: `string` = `"destination direction location maps pin place stop"`

### folder

> **folder**: `string` = `"data directory document drive file folders sheet slide storage"`

### folder\_delete

> **folder\_delete**: `string` = `"bin can data document drive file folders garbage remove sheet slide storage trash"`

### folder\_off

> **folder\_off**: `string` = `"[online] data disabled document drive enabled file folders sheet slash slide storage"`

### folder\_open

> **folder\_open**: `string` = `"data directory document drive file folders sheet slide storage"`

### folder\_shared

> **folder\_shared**: `string` = `"account collaboration data directory document drive face human people person profile sheet slide storage team user"`

### folder\_special

> **folder\_special**: `string` = `"bookmark data directory document drive favorite file highlight important marked saved shape sheet slide star storage"`

### folder\_zip

> **folder\_zip**: `string` = `"compress data document drive file folders open sheet slide storage"`

### follow\_the\_signs

> **follow\_the\_signs**: `string` = `"arrow body directional human people person right social"`

### font\_download

> **font\_download**: `string` = `"A alphabet character letter square symbol text type"`

### font\_download\_off

> **font\_download\_off**: `string` = `"alphabet character disabled enabled letter slash square symbol text type"`

### food\_bank

> **food\_bank**: `string` = `"architecture building charity eat estate fork house knife meal place real residence residential shelter utensils"`

### forest

> **forest**: `string` = `"jungle nature plantation plants trees woodland"`

### fork\_left

> **fork\_left**: `string` = `"arrows directions maps navigation path route sign traffic"`

### fork\_right

> **fork\_right**: `string` = `"arrows directions maps navigation path route sign traffic"`

### format\_align\_center

> **format\_align\_center**: `string` = `"alignment doc editing editor lines spreadsheet text type writing"`

### format\_align\_justify

> **format\_align\_justify**: `string` = `"alignment density doc editing editor extra lines small spreadsheet text type writing"`

### format\_align\_left

> **format\_align\_left**: `string` = `"alignment doc editing editor lines spreadsheet text type writing"`

### format\_align\_right

> **format\_align\_right**: `string` = `"alignment doc editing editor lines spreadsheet text type writing"`

### format\_bold

> **format\_bold**: `string` = `"B alphabet character doc editing editor font letter spreadsheet styles symbol text type writing"`

### format\_clear

> **format\_clear**: `string` = `"T alphabet character disabled doc editing editor enabled font letter off slash spreadsheet style symbol text type writing"`

### format\_color\_fill

> **format\_color\_fill**: `string` = `"bucket doc editing editor paint spreadsheet style text type writing"`

### format\_color\_reset

> **format\_color\_reset**: `string` = `"clear disabled doc droplet editing editor enabled fill liquid off on paint slash spreadsheet style text type water writing"`

### format\_color\_text

> **format\_color\_text**: `string` = `"doc editing editor fill paint spreadsheet style type writing"`

### format\_indent\_decrease

> **format\_indent\_decrease**: `string` = `"alignment doc editing editor indentation paragraph spreadsheet text type writing"`

### format\_indent\_increase

> **format\_indent\_increase**: `string` = `"alignment doc editing editor indentation paragraph spreadsheet text type writing"`

### format\_italic

> **format\_italic**: `string` = `"alphabet character doc editing editor font letter spreadsheet style symbol text type writing"`

### format\_line\_spacing

> **format\_line\_spacing**: `string` = `"alignment doc editing editor spreadsheet text type writing"`

### format\_list\_bulleted

> **format\_list\_bulleted**: `string` = `"alignment doc editing editor notes spreadsheet task text todo type writing"`

### format\_list\_numbered

> **format\_list\_numbered**: `string` = `"alignment digit doc editing editor notes spreadsheet symbol task text todo type writing"`

### format\_list\_numbered\_rtl

> **format\_list\_numbered\_rtl**: `string` = `"alignment digit doc editing editor notes spreadsheet symbol task text todo type writing"`

### format\_overline

> **format\_overline**: `string` = `"alphabet character doc editing editor font letter spreadsheet style symbol text type under writing"`

### format\_paint

> **format\_paint**: `string` = `"brush color doc editing editor fill paintroller spreadsheet style text type writing"`

### format\_quote

> **format\_quote**: `string` = `"doc editing editor quotation spreadsheet text type writing"`

### format\_shapes

> **format\_shapes**: `string` = `"alphabet character color doc editing editor fill font letter paint spreadsheet style symbol text type writing"`

### format\_size

> **format\_size**: `string` = `"alphabet character color doc editing editor fill font letter paint spreadsheet style symbol text type writing"`

### format\_strikethrough

> **format\_strikethrough**: `string` = `"alphabet character doc editing editor font letter spreadsheet style symbol text type writing"`

### format\_textdirection\_l\_to\_r

> **format\_textdirection\_l\_to\_r**: `string` = `"alignment doc editing editor ltr paragraph spreadsheet type writing"`

### format\_textdirection\_r\_to\_l

> **format\_textdirection\_r\_to\_l**: `string` = `"alignment doc editing editor paragraph rtl spreadsheet type writing"`

### format\_underlined

> **format\_underlined**: `string` = `"alphabet character doc editing editor font letter spreadsheet style symbol text type writing"`

### forum

> **forum**: `string` = `"bubble chat comment communicate community conversation feedback hub messages speech talk"`

### forward

> **forward**: `string` = `"arrow mail message playback right sent"`

### forward\_10

> **forward\_10**: `string` = `"arrow circle controls digit fast music number play rotate seconds speed symbol time video"`

### forward\_30

> **forward\_30**: `string` = `"arrow circle controls digit fast music number rotate seconds speed symbol time video"`

### forward\_5

> **forward\_5**: `string` = `"10 arrow circle controls digit fast music number rotate seconds speed symbol time video"`

### forward\_to\_inbox

> **forward\_to\_inbox**: `string` = `"arrow email envelop letter message send"`

### foundation

> **foundation**: `string` = `"architecture base basis building construction estate home house real residential"`

### free\_breakfast

> **free\_breakfast**: `string` = `"beverage cafe coffee cup drink mug tea"`

### fullscreen

> **fullscreen**: `string` = `"adjust application components interface size ui ux view website"`

### fullscreen\_exit

> **fullscreen\_exit**: `string` = `"adjust application components interface size ui ux view website"`

### functions

> **functions**: `string` = `"average calculate count doc editing editor math sigma spreadsheet style sum text type writing"`

### g\_mobiledata

> **g\_mobiledata**: `string` = `"alphabet character font letter network service symbol text type"`

### g\_translate

> **g\_translate**: `string` = `"emblem google language logo mark speaking speech translator words"`

### gamepad

> **gamepad**: `string` = `"buttons console controller device gaming playstation video"`

### games

> **games**: `string` = `"adjust arrows controller direction dpad gaming left move nintendo playstation right xbox"`

### garage

> **garage**: `string` = `"automobile automotive cars direction maps transportation travel vehicle"`

### gas\_meter

> **gas\_meter**: `string` = `"droplet energy measure nest usage water"`

### gavel

> **gavel**: `string` = `"agreement contract court document government hammer judge law mallet official police rules terms"`

### gesture

> **gesture**: `string` = `"drawing finger gestures hand line motion"`

### get\_app

> **get\_app**: `string` = `"arrows downloads export install play pointing retrieve upload"`

### gif

> **gif**: `string` = `"alphabet animated animation bitmap character font format graphics interchange letter symbol text type"`

### gif\_box

> **gif\_box**: `string` = `"alphabet animated animation bitmap character font format graphics interchange letter symbol text type"`

### girl

> **girl**: `string` = `"body female gender human lady people person social symbol woman women"`

### gite

> **gite**: `string` = `"architecture estate home hostel house maps place real residence residential stay traveling"`

### golf\_course

> **golf\_course**: `string` = `"athlete athletic ball club entertainment flag golfer golfing hobby hole places putt sports"`

### gpp\_bad

> **gpp\_bad**: `string` = `"cancel certified close error exit no privacy private protection remove security shield sim stop verified"`

### gpp\_good

> **gpp\_good**: `string` = `"certified check ok pass security shield sim tick"`

### gpp\_maybe

> **gpp\_maybe**: `string` = `"! alert attention caution certified danger error exclamation important mark notification privacy private protection security shield sim symbol verified warning"`

### gps\_fixed

> **gps\_fixed**: `string` = `"destination direction location maps pin place pointer stop tracking"`

### gps\_not\_fixed

> **gps\_not\_fixed**: `string` = `"destination direction disabled enabled fixed location maps not off online place pointer slash tracking"`

### gps\_off

> **gps\_off**: `string` = `"destination direction disabled enabled fixed location maps not offline place pointer slash tracking"`

### grade

> **grade**: `string` = `"achievement important likes marked rated rating reward saved shape special star"`

### gradient

> **gradient**: `string` = `"color editing effect filter images photography pictures"`

### grading

> **grading**: `string` = `"approve check complete document done feedback grade mark ok reviewed select tick validate verified writing yes"`

### grain

> **grain**: `string` = `"dots editing effect filter images photography pictures"`

### graphic\_eq

> **graphic\_eq**: `string` = `"audio equalizer music recording sound voice"`

### grass

> **grass**: `string` = `"backyard fodder ground home lawn plant turf"`

### grid\_goldenratio

> **grid\_goldenratio**: `string` = `"layout lines space"`

### grid\_off

> **grid\_off**: `string` = `"collage disabled enabled image layout on slash view"`

### grid\_on

> **grid\_on**: `string` = `"collage disabled enabled image layout off sheet slash view"`

### grid\_view

> **grid\_view**: `string` = `"application blocks components dashboard design interface layout screen square tiles ui ux website window"`

### group

> **group**: `string` = `"accounts committee face family friends humans network people persons profiles social team users"`

### group\_add

> **group\_add**: `string` = `"accounts committee face family friends humans increase more network people persons plus profiles social team users"`

### group\_remove

> **group\_remove**: `string` = `"accounts committee face family friends humans network people persons profiles social team users"`

### group\_work

> **group\_work**: `string` = `"alliance circle collaboration film partnership reel teamwork together"`

### groups

> **groups**: `string` = `"body club collaboration crowd gathering human meeting people person social teams"`

### h\_mobiledata

> **h\_mobiledata**: `string` = `"alphabet character font letter network service symbol text type"`

### h\_plus\_mobiledata

> **h\_plus\_mobiledata**: `string` = `"+ alphabet character font letter network service symbol text type"`

### hail

> **hail**: `string` = `"body human people person pick public stop taxi transportation"`

### handyman

> **handyman**: `string` = `"build construction fix hammer repair screwdriver tools"`

### hardware

> **hardware**: `string` = `"break construction hammer nail repair tool"`

### hd

> **hd**: `string` = `"alphabet character definition display font high letter movies quality resolution screen symbol text tv type video"`

### hdr\_auto

> **hdr\_auto**: `string` = `"A alphabet camera character circle dynamic font high letter photo range symbol text type"`

### hdr\_auto\_select

> **hdr\_auto\_select**: `string` = `"+ A alphabet camera character circle dynamic font high letter photo range symbol text type"`

### hdr\_enhanced\_select

> **hdr\_enhanced\_select**: `string` = `"add alphabet character dynamic font high letter plus range symbol text type"`

### hdr\_off

> **hdr\_off**: `string` = `"alphabet character disabled dynamic enabled enhance font high letter range select slash symbol text type"`

### hdr\_off\_select

> **hdr\_off\_select**: `string` = `"alphabet camera character circle disabled dynamic enabled font high letter photo range slash symbol text type"`

### hdr\_on

> **hdr\_on**: `string` = `"add alphabet character dynamic enhance font high letter plus range select symbol text type"`

### hdr\_on\_select

> **hdr\_on\_select**: `string` = `"+ alphabet camera character circle dynamic font high letter photo range symbol text type"`

### hdr\_plus

> **hdr\_plus**: `string` = `"+ add alphabet character circle dynamic enhance font high letter range select symbol text type"`

### hdr\_strong

> **hdr\_strong**: `string` = `"circles dots dynamic enhance high range"`

### hdr\_weak

> **hdr\_weak**: `string` = `"circles dots dynamic enhance high range"`

### headphones

> **headphones**: `string` = `"accessory audio device earphone headset listen music sound"`

### headphones\_battery

> **headphones\_battery**: `string` = `"accessory audio charging device earphone headset listen music sound"`

### headset

> **headset**: `string` = `"accessory audio device earbuds earmuffs earphone headphones listen music sound"`

### headset\_mic

> **headset\_mic**: `string` = `"accessory audio chat device earphone headphones listen music sound talk"`

### headset\_off

> **headset\_off**: `string` = `"accessory audio chat device disabled earphone enabled headphones listen mic music slash sound talk"`

### healing

> **healing**: `string` = `"bandage bandaid editing emergency fix health hospital image medicine"`

### health\_and\_safety

> **health\_and\_safety**: `string` = `"+ add certified plus privacy private protection security shield symbol verified"`

### hearing

> **hearing**: `string` = `"accessibility accessible aid handicap help impaired listen sound volume"`

### hearing\_disabled

> **hearing\_disabled**: `string` = `"accessibility accessible aid enabled handicap help impaired listen off on slash sound volume"`

### heart\_broken

> **heart\_broken**: `string` = `"break core crush health nucleus split"`

### heat\_pump

> **heat\_pump**: `string` = `"air conditioner cool energy furnance nest usage"`

### height

> **height**: `string` = `"arrows color doc down editing editor fill format paint resize spreadsheet stretch style text type up writing"`

### help

> **help**: `string` = `"? alert announcement assistance circle information mark punctuation question shape support symbol"`

### help\_center

> **help\_center**: `string` = `"? assistance information mark punctuation question support symbol"`

### help\_outline

> **help\_outline**: `string` = `"? alert announcement assistance circle information mark punctuation question shape support symbol"`

### hevc

> **hevc**: `string` = `"alphabet character coding efficiency font high letter symbol text type video"`

### hexagon

> **hexagon**: `string` = `"shape sides six"`

### hide\_image

> **hide\_image**: `string` = `"disabled enabled landscape mountains off on photography picture slash"`

### hide\_source

> **hide\_source**: `string` = `"circle disabled enabled offline on shape slash"`

### high\_quality

> **high\_quality**: `string` = `"alphabet character definition display font hq letter movies resolution screen symbol text tv type"`

### highlight

> **highlight**: `string` = `"color doc editing editor emphasize fill flashlight format marker paint spreadsheet style text type writing"`

### highlight\_off

> **highlight\_off**: `string` = `"cancel circle clear click close delete disable exit focus no quit remove stop target times"`

### hiking

> **hiking**: `string` = `"backpacking bag climbing duffle mountain social sports stick trail travel walking"`

### history

> **history**: `string` = `"arrow backwards clock date refresh renew reverse revert rotate schedule time turn undo"`

### history\_edu

> **history\_edu**: `string` = `"document education feather letter paper pen quill school tools write writing"`

### history\_toggle\_off

> **history\_toggle\_off**: `string` = `"clock date schedule time"`

### hls

> **hls**: `string` = `"alphabet character developer engineering font letter platform symbol text type"`

### hls\_off

> **hls\_off**: `string` = `"[offline] alphabet character developer disabled enabled engineering font letter platform slash symbol text type"`

### holiday\_village

> **holiday\_village**: `string` = `"architecture beach camping cottage estate home house lake lodge maps place real residence residential stay traveling vacation"`

### home

> **home**: `string` = `"address application--house architecture building components design estate homepage interface layout place real residence residential screen shelter structure unit ux website window"`

### home\_max

> **home\_max**: `string` = `"device gadget hardware internet iot nest smart things"`

### home\_mini

> **home\_mini**: `string` = `"Internet device gadget hardware iot nest smart things"`

### home\_repair\_service

> **home\_repair\_service**: `string` = `"equipment fix kit mechanic repairing toolbox tools workshop"`

### home\_work

> **home\_work**: `string` = `"architecture building estate house office place real residence residential shelter"`

### horizontal\_rule

> **horizontal\_rule**: `string` = `"gmail line novitas"`

### horizontal\_split

> **horizontal\_split**: `string` = `"bars format layout lines stacked"`

### hot\_tub

> **hot\_tub**: `string` = `"bathing bathroom bathtub hotel human jacuzzi person shower spa steam travel water"`

### hotel

> **hotel**: `string` = `"bed body human people person sleep stay travel trip"`

### hourglass\_bottom

> **hourglass\_bottom**: `string` = `"countdown half loading minutes time waiting"`

### hourglass\_disabled

> **hourglass\_disabled**: `string` = `"clock countdown empty enabled loading minutes off on slash time waiting"`

### hourglass\_empty

> **hourglass\_empty**: `string` = `"countdown loading minutes start time waiting"`

### hourglass\_full

> **hourglass\_full**: `string` = `"countdown loading minutes time waiting"`

### hourglass\_top

> **hourglass\_top**: `string` = `"countdown half loading minutes time waiting"`

### house

> **house**: `string` = `"architecture building estate family homepage places real residence residential shelter"`

### house\_siding

> **house\_siding**: `string` = `"architecture building construction estate exterior facade home real residential"`

### houseboat

> **houseboat**: `string` = `"architecture beach estate floating home maps place real residence residential sea stay traveling vacation"`

### how\_to\_reg

> **how\_to\_reg**: `string` = `"approve ballot check complete done election mark ok poll register registration select tick to validate verified vote yes"`

### how\_to\_vote

> **how\_to\_vote**: `string` = `"ballot election poll"`

### html

> **html**: `string` = `"alphabet brackets character code css developer engineering font letter platform symbol text type"`

### http

> **http**: `string` = `"alphabet character font internet letter network symbol text transfer type url website"`

### https

> **https**: `string` = `"connection encrypt internet key locked network password privacy private protection safety secure security ssl web"`

### hub

> **hub**: `string` = `"center connection core focal network nucleus point topology"`

### hvac

> **hvac**: `string` = `"air conditioning heating ventilation"`

### ice\_skating

> **ice\_skating**: `string` = `"athlete athletic entertainment exercise hobby shoe skates social sports travel"`

### icecream

> **icecream**: `string` = `"dessert food snack"`

### image

> **image**: `string` = `"disabled enabled frame hide landscape mountains off on photography picture slash"`

### image\_aspect\_ratio

> **image\_aspect\_ratio**: `string` = `"photography picture rectangle square"`

### image\_not\_supported

> **image\_not\_supported**: `string` = `"disabled enabled landscape mountains off on photography picture slash"`

### image\_search

> **image\_search**: `string` = `"find glass landscape look magnifying mountains photography picture see"`

### imagesearch\_roller

> **imagesearch\_roller**: `string` = `"art paint"`

### import\_contacts

> **import\_contacts**: `string` = `"address book friends information magazine open"`

### import\_export

> **import\_export**: `string` = `"arrows direction down explort up"`

### important\_devices

> **important\_devices**: `string` = `"Android cell computer desktop hardware iOS mobile monitor phone star tablet web"`

### inbox

> **inbox**: `string` = `"archive email incoming message"`

### indeterminate\_check\_box

> **indeterminate\_check\_box**: `string` = `"application button components control design form interface minus screen selected selection square toggle ui undetermined ux website"`

### info

> **info**: `string` = `"about alert announcement announcment assistance bubble circle details help information service support"`

### input

> **input**: `string` = `"arrow box download login move right"`

### insert\_chart

> **insert\_chart**: `string` = `"analytics barchart bars data diagram infographic measure metrics statistics tracking"`

### insert\_chart\_outlined

> **insert\_chart\_outlined**: `string` = `"analytics bars data diagram infographic measure metrics statistics tracking"`

### insert\_comment

> **insert\_comment**: `string` = `"add bubble chat feedback message"`

### insert\_drive\_file

> **insert\_drive\_file**: `string` = `"bill document format invoice item sheet slide"`

### insert\_emoticon

> **insert\_emoticon**: `string` = `"account emoji face happy human like people person profile sentiment smiley user"`

### insert\_invitation

> **insert\_invitation**: `string` = `"agenda calendar date event mark month range remember reminder today week"`

### insert\_link

> **insert\_link**: `string` = `"add anchor attach clip file mail media"`

### insert\_page\_break

> **insert\_page\_break**: `string` = `"document file paper"`

### insert\_photo

> **insert\_photo**: `string` = `"image landscape mountains photography picture wallpaper"`

### insights

> **insights**: `string` = `"analytics bars chart data diagram infographic measure metrics stars statistics tracking"`

### install\_desktop

> **install\_desktop**: `string` = `"Android chrome device display fix hardware iOS mac monitor place pwa screen web window"`

### install\_mobile

> **install\_mobile**: `string` = `"Android cell device hardware iOS phone pwa tablet"`

### integration\_instructions

> **integration\_instructions**: `string` = `"brackets clipboard code css developer document engineering html platform"`

### interests

> **interests**: `string` = `"circle heart shapes social square triangle"`

### interpreter\_mode

> **interpreter\_mode**: `string` = `"language microphone person speaking symbol"`

### inventory

> **inventory**: `string` = `"archive box buy check clipboard document e-commerce file list organize packages product purchase shop stock store supply"`

### invert\_colors

> **invert\_colors**: `string` = `"droplet editing hue inverted liquid palette tone water"`

### invert\_colors\_off

> **invert\_colors\_off**: `string` = `"disabled droplet enabled hue inverted liquid offline opacity palette slash tone water"`

### ios\_share

> **ios\_share**: `string` = `"arrows button direction export internet link send sharing social up website"`

### iron

> **iron**: `string` = `"appliance clothes electric ironing machine object"`

### iso

> **iso**: `string` = `"add editing effect image minus photography picture plus sensor shutter speed subtract"`

### javascript

> **javascript**: `string` = `"alphabet brackets character code css developer engineering font html letter platform symbol text type"`

### join\_full

> **join\_full**: `string` = `"circle combine command left outter right sql"`

### join\_inner

> **join\_inner**: `string` = `"circle command matching sql values"`

### join\_left

> **join\_left**: `string` = `"circle command matching sql values"`

### join\_right

> **join\_right**: `string` = `"circle command matching sql values"`

### kayaking

> **kayaking**: `string` = `"athlete athletic body canoe entertainment exercise hobby human lake paddle paddling people person rafting river row social sports summer travel water"`

### key

> **key**: `string` = `"blackout password restricted secret unlock"`

### key\_off

> **key\_off**: `string` = `"[offline] disabled enabled on password slash unlock"`

### keyboard

> **keyboard**: `string` = `"computer device hardware input keypad letter office text type"`

### keyboard\_alt

> **keyboard\_alt**: `string` = `"computer device hardware input keypad letter office text type"`

### keyboard\_arrow\_down

> **keyboard\_arrow\_down**: `string` = `"arrows chevron open"`

### keyboard\_arrow\_left

> **keyboard\_arrow\_left**: `string` = `"arrows chevron"`

### keyboard\_arrow\_right

> **keyboard\_arrow\_right**: `string` = `"arrows chevron open start"`

### keyboard\_arrow\_up

> **keyboard\_arrow\_up**: `string` = `"arrows chevron submit"`

### keyboard\_backspace

> **keyboard\_backspace**: `string` = `"arrow left"`

### keyboard\_capslock

> **keyboard\_capslock**: `string` = `"arrow up"`

### keyboard\_command\_key

> **keyboard\_command\_key**: `string` = `"button command control key"`

### keyboard\_control\_key

> **keyboard\_control\_key**: `string` = `"control key"`

### keyboard\_double\_arrow\_down

> **keyboard\_double\_arrow\_down**: `string` = `"arrows direction multiple navigation"`

### keyboard\_double\_arrow\_left

> **keyboard\_double\_arrow\_left**: `string` = `"arrows direction multiple navigation"`

### keyboard\_double\_arrow\_right

> **keyboard\_double\_arrow\_right**: `string` = `"arrows direction multiple navigation"`

### keyboard\_double\_arrow\_up

> **keyboard\_double\_arrow\_up**: `string` = `"arrows direction multiple navigation"`

### keyboard\_hide

> **keyboard\_hide**: `string` = `"arrow computer device down hardware input keypad text"`

### keyboard\_option\_key

> **keyboard\_option\_key**: `string` = `"alt key modifier"`

### keyboard\_return

> **keyboard\_return**: `string` = `"arrow back left"`

### keyboard\_tab

> **keyboard\_tab**: `string` = `"arrow next right"`

### keyboard\_voice

> **keyboard\_voice**: `string` = `"microphone noise recorder speaker"`

### king\_bed

> **king\_bed**: `string` = `"bedroom double furniture home hotel house night pillows queen rest sleep"`

### kitchen

> **kitchen**: `string` = `"appliance cabinet cold food freezer fridge home house ice places refrigerator storage"`

### kitesurfing

> **kitesurfing**: `string` = `"athlete athletic beach body entertainment exercise hobby human people person social sports travel water"`

### label

> **label**: `string` = `"badge favorite indent item library mail remember save stamp sticker tag"`

### label\_important

> **label\_important**: `string` = `"badge favorite important. indent item library mail remember save stamp sticker tag wing"`

### label\_off

> **label\_off**: `string` = `"disabled enabled favorite indent library mail on remember save slash stamp sticker tag wing"`

### lan

> **lan**: `string` = `"computer connection data internet network service"`

### landscape

> **landscape**: `string` = `"image mountains nature photography picture"`

### language

> **language**: `string` = `"country earth globe i18n internet l10n planet website world www"`

### laptop

> **laptop**: `string` = `"Android chrome computer connect desktop device display hardware iOS link mac monitor smart tv web windows"`

### laptop\_chromebook

> **laptop\_chromebook**: `string` = `"Android chromebook device display hardware iOS mac monitor screen web window"`

### laptop\_mac

> **laptop\_mac**: `string` = `"Android apple chrome device display hardware iOS monitor screen web window"`

### laptop\_windows

> **laptop\_windows**: `string` = `"Android chrome device display hardware iOS mac monitor screen web"`

### last\_page

> **last\_page**: `string` = `"application arrow chevron components end forward interface right screen ui ux website"`

### launch

> **launch**: `string` = `"application arrow box components core interface internal new open screen ui ux website window"`

### layers

> **layers**: `string` = `"arrange disabled enabled interaction maps off overlay pages slash stack"`

### layers\_clear

> **layers\_clear**: `string` = `"arrange delete disabled enabled interaction maps off overlay pages slash"`

### leaderboard

> **leaderboard**: `string` = `"analytics bars chart data diagram infographic measure metrics statistics tracking"`

### leak\_add

> **leak\_add**: `string` = `"connection data link network service signals synce wireless"`

### leak\_remove

> **leak\_remove**: `string` = `"connection data disabled enabled link network offline service signals slash synce wireless"`

### legend\_toggle

> **legend\_toggle**: `string` = `"analytics chart data diagram infographic measure metrics monitoring stackdriver statistics tracking"`

### lens

> **lens**: `string` = `"circle full geometry moon"`

### lens\_blur

> **lens\_blur**: `string` = `"camera dim dot effect foggy fuzzy image photo soften"`

### library\_add

> **library\_add**: `string` = `"+ collection layers multiple music new plus save stacked symbol video"`

### library\_add\_check

> **library\_add\_check**: `string` = `"approve collection complete done layers mark multiple music ok select stacked tick validate verified video yes"`

### library\_books

> **library\_books**: `string` = `"add album audio collection reading"`

### library\_music

> **library\_music**: `string` = `"add album audio collection song sounds"`

### light

> **light**: `string` = `"bulb ceiling hanging inside interior lamp lighting pendent room"`

### light\_mode

> **light\_mode**: `string` = `"brightness day device lighting morning mornng sky sunny"`

### lightbulb

> **lightbulb**: `string` = `"alert announcement idea information learning mode"`

### lightbulb\_circle

> **lightbulb\_circle**: `string` = `"alert announcement idea information"`

### line\_axis

> **line\_axis**: `string` = `"dash horizontal stroke vertical"`

### line\_style

> **line\_style**: `string` = `"dash dotted editor rule spacing"`

### line\_weight

> **line\_weight**: `string` = `"editor height size spacing style thickness"`

### linear\_scale

> **linear\_scale**: `string` = `"application components design interface layout measure menu screen slider ui ux website window"`

### link

> **link**: `string` = `"anchor chain clip connection external hyperlink linked links multimedia unlisted url"`

### link\_off

> **link\_off**: `string` = `"anchor attached chain clip connection disabled enabled linked links multimedia slash unlink url"`

### linked\_camera

> **linked\_camera**: `string` = `"connection lens network photography picture signals sync wireless"`

### liquor

> **liquor**: `string` = `"alcohol bar bottle club cocktail drink food party store wine"`

### list

> **list**: `string` = `"editor file format index menu options playlist task todo"`

### list\_alt

> **list\_alt**: `string` = `"box contained editor format lines reorder sheet stacked task title todo"`

### live\_help

> **live\_help**: `string` = `"? alert announcement assistance bubble chat comment communicate faq information mark message punctuation question speech support symbol"`

### live\_tv

> **live\_tv**: `string` = `"Android antennas chrome desktop device hardware iOS mac monitor movie play stream television web window"`

### living

> **living**: `string` = `"chair comfort couch decoration furniture home house lounging loveseat room seating sofa"`

### local\_activity

> **local\_activity**: `string` = `"event star things ticket"`

### local\_airport

> **local\_airport**: `string` = `"airplane flight flying transportation travel trip"`

### local\_atm

> **local\_atm**: `string` = `"bill card cart cash coin commerce credit currency dollars financial money online payment price profit shopping symbol"`

### local\_bar

> **local\_bar**: `string` = `"alcohol bottle club cocktail drink food liquor martini wine"`

### local\_cafe

> **local\_cafe**: `string` = `"bottle coffee cup drink food mug restaurant tea"`

### local\_car\_wash

> **local\_car\_wash**: `string` = `"automobile cars maps transportation travel vehicle"`

### local\_convenience\_store

> **local\_convenience\_store**: `string` = `"-- 24 bill building business card cash coin commerce company credit currency dollars maps market money new online payment plus shopping storefront symbol"`

### local\_dining

> **local\_dining**: `string` = `"cutlery eat food fork knife meal restaurant spoon"`

### local\_drink

> **local\_drink**: `string` = `"cup droplet glass liquid park water"`

### local\_fire\_department

> **local\_fire\_department**: `string` = `"911 firefighter flame hot"`

### local\_florist

> **local\_florist**: `string` = `"flower shop"`

### local\_gas\_station

> **local\_gas\_station**: `string` = `"auto car filling fuel gasoline oil station vehicle"`

### local\_grocery\_store

> **local\_grocery\_store**: `string` = `"market shop"`

### local\_hospital

> **local\_hospital**: `string` = `"911 aid cross doctor emergency first health medical medicine plus"`

### local\_hotel

> **local\_hotel**: `string` = `"bed body human people person sleep stay travel trip"`

### local\_laundry\_service

> **local\_laundry\_service**: `string` = `"cleaning clothing dryer hotel washer"`

### local\_library

> **local\_library**: `string` = `"book community learning person read"`

### local\_mall

> **local\_mall**: `string` = `"bill building business buy card cart cash coin commerce credit currency dollars handbag money online payment shopping storefront"`

### local\_offer

> **local\_offer**: `string` = `"deal discount price shopping store tag"`

### local\_parking

> **local\_parking**: `string` = `"alphabet auto car character font garage letter symbol text type vehicle"`

### local\_pharmacy

> **local\_pharmacy**: `string` = `"911 aid cross emergency first food hospital medicine places"`

### local\_phone

> **local\_phone**: `string` = `"booth call telecommunication"`

### local\_pizza

> **local\_pizza**: `string` = `"drink fastfood meal"`

### local\_police

> **local\_police**: `string` = `"911 badge law officer protection security shield"`

### local\_post\_office

> **local\_post\_office**: `string` = `"delivery email envelop letter message package parcel postal send stamp"`

### local\_printshop

> **local\_printshop**: `string` = `"draft fax ink machine office paper printer send"`

### local\_see

> **local\_see**: `string` = `"camera lens photography picture"`

### local\_shipping

> **local\_shipping**: `string` = `"automobile cars delivery letter mail maps office package parcel postal semi send shopping stamp transportation truck vehicle"`

### local\_taxi

> **local\_taxi**: `string` = `"automobile cab call cars direction lyft maps public transportation uber vehicle yellow"`

### location\_city

> **location\_city**: `string` = `"apartments architecture buildings business company estate home landscape place real residence residential shelter town urban"`

### location\_disabled

> **location\_disabled**: `string` = `"destination direction enabled maps off pin place pointer slash stop tracking"`

### location\_off

> **location\_off**: `string` = `"destination direction disabled enabled gps maps pin place room slash stop"`

### location\_on

> **location\_on**: `string` = `"destination direction disabled enabled gps maps off pin place room slash stop"`

### location\_searching

> **location\_searching**: `string` = `"destination direction maps pin place pointer stop tracking"`

### lock

> **lock**: `string` = `"connection key locked logout padlock password privacy private protection safety secure security signout"`

### lock\_clock

> **lock\_clock**: `string` = `"date locked password privacy private protection safety schedule secure security time"`

### lock\_open

> **lock\_open**: `string` = `"connection key login padlock password privacy private protection register safety secure security signin signup unlocked"`

### lock\_reset

> **lock\_reset**: `string` = `"around inprogress loading locked password privacy private protection refresh renew rotate safety secure security turn"`

### login

> **login**: `string` = `"access application arrow components design enter interface left screen ui ux website"`

### logo\_dev

> **logo\_dev**: `string` = `"dev.to"`

### logout

> **logout**: `string` = `"application arrow components design exit interface leave login right screen ui ux website"`

### looks

> **looks**: `string` = `"circle half rainbow"`

### looks\_3

> **looks\_3**: `string` = `"digit numbers square symbol"`

### looks\_4

> **looks\_4**: `string` = `"digit numbers square symbol"`

### looks\_5

> **looks\_5**: `string` = `"digit numbers square symbol"`

### looks\_6

> **looks\_6**: `string` = `"digit numbers square symbol"`

### looks\_one

> **looks\_one**: `string` = `"1 digit numbers square symbol"`

### looks\_two

> **looks\_two**: `string` = `"2 digit numbers square symbol"`

### loop

> **loop**: `string` = `"around arrows direction inprogress loader loading music navigation refresh renew repeat rotate turn"`

### loupe

> **loupe**: `string` = `"+ add details focus glass magnifying new plus symbol"`

### low\_priority

> **low\_priority**: `string` = `"arrange arrow backward bottom list move order task todo"`

### loyalty

> **loyalty**: `string` = `"badge card credit heart love membership miles points program sale subscription tag travel trip"`

### lte\_mobiledata

> **lte\_mobiledata**: `string` = `"alphabet character font internet letter network speed symbol text type wifi wireless"`

### lte\_plus\_mobiledata

> **lte\_plus\_mobiledata**: `string` = `"+ alphabet character font internet letter network speed symbol text type wifi wireless"`

### luggage

> **luggage**: `string` = `"airport baggage carry flight hotel on suitcase travel trip"`

### lunch\_dining

> **lunch\_dining**: `string` = `"breakfast dinner drink fastfood hamburger meal"`

### lyrics

> **lyrics**: `string` = `"audio bubble chat comment communicate feedback key message music note song sound speech track"`

### mail

> **mail**: `string` = `"email envelope inbox letter message send"`

### mail\_lock

> **mail\_lock**: `string` = `"email envelop letter locked message password privacy private protection safety secure security send"`

### mail\_outline

> **mail\_outline**: `string` = `"email envelope letter message note post receive send write"`

### male

> **male**: `string` = `"boy gender man social symbol"`

### man

> **man**: `string` = `"boy gender male social symbol"`

### manage\_accounts

> **manage\_accounts**: `string` = `"change details face gear options people person profile service-human settings user"`

### manage\_search

> **manage\_search**: `string` = `"glass history magnifying text"`

### map

> **map**: `string` = `"destination direction location maps pin place route stop travel"`

### maps\_home\_work

> **maps\_home\_work**: `string` = `"building house office"`

### maps\_ugc

> **maps\_ugc**: `string` = `"+ add bubble comment communicate feedback message new plus speech symbol"`

### margin

> **margin**: `string` = `"design layout padding size square"`

### mark\_as\_unread

> **mark\_as\_unread**: `string` = `"envelop letter mail postal receive send"`

### mark\_chat\_read

> **mark\_chat\_read**: `string` = `"approve bubble check comment communicate complete done message ok select sent speech tick verified yes"`

### mark\_chat\_unread

> **mark\_chat\_unread**: `string` = `"bubble circle comment communicate message notification speech"`

### mark\_email\_read

> **mark\_email\_read**: `string` = `"approve check complete done envelop letter message note ok select send sent tick yes"`

### mark\_email\_unread

> **mark\_email\_unread**: `string` = `"check circle envelop letter message note notification send"`

### markunread

> **markunread**: `string` = `"email envelope letter message send"`

### markunread\_mailbox

> **markunread\_mailbox**: `string` = `"deliver envelop letter postal postbox receive send"`

### masks

> **masks**: `string` = `"air cover covid face hospital medical pollution protection respirator sick social"`

### maximize

> **maximize**: `string` = `"application components design interface line screen shape ui ux website"`

### media\_bluetooth\_off

> **media\_bluetooth\_off**: `string` = `"connection connectivity device disabled enabled music note offline paring signal slash symbol wireless"`

### media\_bluetooth\_on

> **media\_bluetooth\_on**: `string` = `"connection connectivity device disabled enabled music note off online paring signal slash symbol wireless"`

### mediation

> **mediation**: `string` = `"alternative arrows compromise direction dots negotiation party right structure"`

### medical\_services

> **medical\_services**: `string` = `"aid bag briefcase emergency first kit medicine"`

### medication

> **medication**: `string` = `"doctor drug emergency hospital medicine pharmacy pills prescription"`

### meeting\_room

> **meeting\_room**: `string` = `"building doorway entrance home house interior logout office open places signout"`

### memory

> **memory**: `string` = `"card chip digital micro processor sd storage"`

### menu

> **menu**: `string` = `"application components hamburger interface lines playlist screen ui ux website"`

### menu\_book

> **menu\_book**: `string` = `"dining food meal page restaurant"`

### menu\_open

> **menu\_open**: `string` = `"application arrow chevron components hamburger interface left lines screen ui ux website"`

### merge

> **merge**: `string` = `"arrows directions maps navigation path route sign traffic"`

### merge\_type

> **merge\_type**: `string` = `"arrow combine direction format text"`

### message

> **message**: `string` = `"bubble chat comment communicate feedback speech talk text"`

### mic

> **mic**: `string` = `"hearing microphone noise record search sound speech voice"`

### mic\_external\_off

> **mic\_external\_off**: `string` = `"audio disabled enabled microphone slash sound voice"`

### mic\_external\_on

> **mic\_external\_on**: `string` = `"audio disabled enabled microphone off slash sound voice"`

### mic\_none

> **mic\_none**: `string` = `"hearing microphone noise record sound voice"`

### mic\_off

> **mic\_off**: `string` = `"audio disabled enabled hearing microphone noise recording slash sound voice"`

### microwave

> **microwave**: `string` = `"appliance cooking electric heat home house kitchen machine"`

### military\_tech

> **military\_tech**: `string` = `"army award badge honor medal merit order privilege prize rank reward ribbon soldier star status trophy winner"`

### minimize

> **minimize**: `string` = `"application components design interface line screen shape ui ux website"`

### missed\_video\_call

> **missed\_video\_call**: `string` = `"arrow camera filming hardware image motion picture record videography"`

### mms

> **mms**: `string` = `"bubble chat comment communicate feedback image landscape message mountains multimedia photography picture speech"`

### mobile\_friendly

> **mobile\_friendly**: `string` = `"Android approve cell check complete device done hardware iOS mark ok phone select tablet tick validate verified yes"`

### mobile\_off

> **mobile\_off**: `string` = `"Android cell device disabled enabled hardware iOS phone silence slash tablet"`

### mobile\_screen\_share

> **mobile\_screen\_share**: `string` = `"Android arrow cell device hardware iOS mirror monitor phone screencast streaming tablet tv wireless"`

### mobiledata\_off

> **mobiledata\_off**: `string` = `"arrow disabled down enabled internet network on slash speed up wifi wireless"`

### mode

> **mode**: `string` = `"compose create draft draw edit pencil write"`

### mode\_comment

> **mode\_comment**: `string` = `"bubble chat comment communicate feedback message mode speech"`

### mode\_edit

> **mode\_edit**: `string` = `"compose create draft draw pencil write"`

### mode\_edit\_outline

> **mode\_edit\_outline**: `string` = `"compose create draft draw pencil write"`

### mode\_night

> **mode\_night**: `string` = `"dark disturb moon sleep weather"`

### mode\_of\_travel

> **mode\_of\_travel**: `string` = `"arrow destination direction location maps pin place stop transportation trip"`

### mode\_standby

> **mode\_standby**: `string` = `"disturb power sleep target"`

### model\_training

> **model\_training**: `string` = `"arrow bulb idea inprogress light loading refresh renew restore reverse rotate"`

### monetization\_on

> **monetization\_on**: `string` = `"bill card cash circle coin commerce cost credit currency dollars finance money online payment price profit sale shopping symbol"`

### money

> **money**: `string` = `"100 bill card cash coin commerce cost credit currency digit dollars finance number online payment price profit shopping symbol"`

### money\_off

> **money\_off**: `string` = `"bill card cart cash coin commerce credit currency disabled dollars enabled finance money online payment price profit shopping slash symbol"`

### money\_off\_csred

> **money\_off\_csred**: `string` = `"bill card cart cash coin commerce credit currency disabled dollars enabled online payment shopping slash symbol"`

### monitor

> **monitor**: `string` = `"Android chrome device display hardware iOS mac screen web window"`

### monitor\_weight

> **monitor\_weight**: `string` = `"body device diet health scale smart"`

### monochrome\_photos

> **monochrome\_photos**: `string` = `"black camera image photography picture white"`

### mood

> **mood**: `string` = `"emoji emoticon emotions expressions face feelings glad happiness happy like person pleased smiley smiling social survey"`

### mood\_bad

> **mood\_bad**: `string` = `"disappointment dislike emoji emoticon emotions expressions face feelings person rating smiley social survey unhappiness unhappy unpleased unsmile unsmiling"`

### more

> **more**: `string` = `"3 archive badge bookmark dots etc favorite indent label remember save stamp sticker tab tag three"`

### more\_horiz

> **more\_horiz**: `string` = `"3 application components dots etc horizontal interface ios pending screen status three ui ux website"`

### more\_time

> **more\_time**: `string` = `"+ add clock date new plus schedule symbol"`

### more\_vert

> **more\_vert**: `string` = `"3 android application components dots etc interface screen three ui ux vertical website"`

### motion\_photos\_auto

> **motion\_photos\_auto**: `string` = `"A alphabet animation automatic character circle font gif letter live symbol text type video"`

### motion\_photos\_off

> **motion\_photos\_off**: `string` = `"animation circle disabled enabled slash video"`

### mouse

> **mouse**: `string` = `"click computer cursor device hardware wireless"`

### move\_down

> **move\_down**: `string` = `"arrow direction jump navigation transfer"`

### move\_to\_inbox

> **move\_to\_inbox**: `string` = `"archive arrow down email envelop incoming letter message move send to"`

### move\_up

> **move\_up**: `string` = `"arrow direction jump navigation transfer"`

### movie

> **movie**: `string` = `"cinema film media screen show slate tv video watch"`

### movie\_creation

> **movie\_creation**: `string` = `"clapperboard film movies slate video"`

### movie\_filter

> **movie\_filter**: `string` = `"clapperboard creation film movies slate stars video"`

### moving

> **moving**: `string` = `"arrow direction navigation travel up"`

### mp

> **mp**: `string` = `"alphabet character font image letter megapixel photography pixels quality resolution symbol text type"`

### multiline\_chart

> **multiline\_chart**: `string` = `"analytics bars data diagram infographic line measure metrics multiple statistics tracking"`

### multiple\_stop

> **multiple\_stop**: `string` = `"arrows directions dots left maps navigation right"`

### museum

> **museum**: `string` = `"architecture attraction building estate event exhibition explore local palces places real see shop store tour"`

### music\_note

> **music\_note**: `string` = `"audiotrack key sound"`

### music\_off

> **music\_off**: `string` = `"audiotrack disabled enabled key note on slash sound"`

### music\_video

> **music\_video**: `string` = `"band mv recording screen tv watch"`

### my\_location

> **my\_location**: `string` = `"destination direction maps navigation pin place point stop"`

### nat

> **nat**: `string` = `"communication"`

### nature

> **nature**: `string` = `"forest outdoor outside park tree wilderness"`

### nature\_people

> **nature\_people**: `string` = `"activity body forest human outdoor outside park person tree wilderness"`

### navigate\_before

> **navigate\_before**: `string` = `"arrows direction left"`

### navigate\_next

> **navigate\_next**: `string` = `"arrows direction right"`

### navigation

> **navigation**: `string` = `"arrow destination direction location maps pin place point stop"`

### near\_me

> **near\_me**: `string` = `"arrow destination direction location maps navigation pin place point stop"`

### near\_me\_disabled

> **near\_me\_disabled**: `string` = `"destination direction enabled location maps navigation off pin place point slash"`

### nearby\_error

> **nearby\_error**: `string` = `"! alert attention caution danger exclamation important mark notification symbol warning"`

### nearby\_off

> **nearby\_off**: `string` = `"disabled enabled on slash"`

### nest\_cam\_wired\_stand

> **nest\_cam\_wired\_stand**: `string` = `"camera filming hardware image motion picture videography"`

### network\_cell

> **network\_cell**: `string` = `"cellular data internet mobile phone speed wifi wireless"`

### network\_check

> **network\_check**: `string` = `"connection internet meter signal speed tick wifi wireless"`

### network\_locked

> **network\_locked**: `string` = `"alert available cellular connection data error internet mobile not privacy private protection restricted safety secure security service signal warning wifi wireless"`

### network\_wifi

> **network\_wifi**: `string` = `"cellular data internet mobile phone speed wireless"`

### new\_releases

> **new\_releases**: `string` = `"! alert announcement attention burst caution danger error exclamation important mark notification star symbol warning"`

### newspaper

> **newspaper**: `string` = `"article data document drive file folders magazine media notes page sheet slide text writing"`

### next\_plan

> **next\_plan**: `string` = `"arrow circle right"`

### next\_week

> **next\_week**: `string` = `"arrow baggage briefcase business suitcase"`

### nfc

> **nfc**: `string` = `"communication data field mobile near wireless"`

### night\_shelter

> **night\_shelter**: `string` = `"architecture bed building estate homeless house place real sleep"`

### nightlife

> **nightlife**: `string` = `"alcohol bar bottle club cocktail dance drink food glass liquor music note wine"`

### nightlight

> **nightlight**: `string` = `"dark disturb mode moon sleep weather"`

### nightlight\_round

> **nightlight\_round**: `string` = `"dark half mode moon"`

### nights\_stay

> **nights\_stay**: `string` = `"cloud crescent dark mode moon phases silence silent sky time weather"`

### no\_accounts

> **no\_accounts**: `string` = `"avatar disabled enabled face human offline people person profile slash thumbnail unavailable unidentifiable unknown user"`

### no\_backpack

> **no\_backpack**: `string` = `"accessory bookbag knapsack travel"`

### no\_drinks

> **no\_drinks**: `string` = `"alcohol beverage bottle cocktail food liquor wine"`

### no\_encryption

> **no\_encryption**: `string` = `"disabled enabled lock off password safety security slash"`

### no\_encryption\_gmailerrorred

> **no\_encryption\_gmailerrorred**: `string` = `"disabled enabled locked off slash"`

### no\_flash

> **no\_flash**: `string` = `"camera disabled enabled image lightning off on photography picture slash thunderbolt"`

### no\_food

> **no\_food**: `string` = `"disabled drink enabled fastfood hamburger meal off on slash"`

### no\_luggage

> **no\_luggage**: `string` = `"baggage carry disabled enabled off on slash suitcase travel"`

### no\_meals

> **no\_meals**: `string` = `"dining disabled eat enabled food fork knife off restaurant slash spoon utensils"`

### no\_meeting\_room

> **no\_meeting\_room**: `string` = `"building disabled doorway enabled entrance home house interior office on open places slash"`

### no\_photography

> **no\_photography**: `string` = `"camera disabled enabled image off on picture slash"`

### no\_sim

> **no\_sim**: `string` = `"camera card device eject insert memory phone storage"`

### no\_stroller

> **no\_stroller**: `string` = `"baby care carriage children disabled enabled infant kid newborn off on parents slash toddler young"`

### no\_transfer

> **no\_transfer**: `string` = `"automobile bus cars direction disabled enabled maps off public slash transportation vehicle"`

### nordic\_walking

> **nordic\_walking**: `string` = `"athlete athletic body entertainment exercise hiking hobby human people person social sports travel walker"`

### north

> **north**: `string` = `"arrow directional maps navigation up"`

### north\_east

> **north\_east**: `string` = `"arrow maps navigation noth right up"`

### north\_west

> **north\_west**: `string` = `"arrow directional left maps navigation up"`

### not\_accessible

> **not\_accessible**: `string` = `"accessibility body handicap help human person wheelchair"`

### not\_interested

> **not\_interested**: `string` = `"allowed banned cancel circle close disabled dislike exit interested not off prohibited quit remove stop"`

### not\_listed\_location

> **not\_listed\_location**: `string` = `"? assistance destination direction help information maps pin place punctuation questionmark stop support symbol"`

### not\_started

> **not\_started**: `string` = `"circle media pause play video"`

### note

> **note**: `string` = `"bookmark message paper"`

### note\_add

> **note\_add**: `string` = `"+ -doc create data document drive file folders new page paper plus sheet slide symbol writing"`

### note\_alt

> **note\_alt**: `string` = `"clipboard document file memo page paper writing"`

### notes

> **notes**: `string` = `"comment document text write writing"`

### notification\_add

> **notification\_add**: `string` = `"+ active alarm alert bell chime notifications notify plus reminder ring sound symbol"`

### notification\_important

> **notification\_important**: `string` = `"! active alarm alert announcement attention bell caution chime danger error exclamation feedback mark notifications notify problem reminder ring sound symbol warning"`

### notifications

> **notifications**: `string` = `"active alarm alert bell chime notify reminder ring sound"`

### notifications\_active

> **notifications\_active**: `string` = `"alarm alert bell chime notify reminder ringing sound"`

### notifications\_none

> **notifications\_none**: `string` = `"alarm alert bell notify reminder ring sound"`

### notifications\_off

> **notifications\_off**: `string` = `"active alarm alert bell chime disabled enabled notify offline reminder ring slash sound"`

### notifications\_paused

> **notifications\_paused**: `string` = `"--- active alarm aleet alert bell chime ignore notify pause quiet reminder ring sleep snooze sound zzz"`

### offline\_bolt

> **offline\_bolt**: `string` = `"circle electric fast flash lightning spark thunderbolt"`

### offline\_pin

> **offline\_pin**: `string` = `"approve checkmark circle complete done ok select tick validate verified yes"`

### offline\_share

> **offline\_share**: `string` = `"Android arrow cell connect device direction hardware iOS link mobile multiple phone right tablet"`

### oil\_barrel

> **oil\_barrel**: `string` = `"droplet gasoline nest water"`

### on\_device\_training

> **on\_device\_training**: `string` = `"arrow bulb call cell contact hardware idea inprogress light loading mobile model refresh renew restore reverse rotate telephone"`

### ondemand\_video

> **ondemand\_video**: `string` = `"Android chrome desktop device hardware iOS mac monitor play television tv web window"`

### online\_prediction

> **online\_prediction**: `string` = `"bulb connection idea light network signal wireless"`

### opacity

> **opacity**: `string` = `"color droplet hue inverted liquid palette tone water"`

### open\_in\_browser

> **open\_in\_browser**: `string` = `"arrow box new up website window"`

### open\_in\_full

> **open\_in\_full**: `string` = `"action arrows expand grow move"`

### open\_in\_new

> **open\_in\_new**: `string` = `"application arrow box components interface screen ui ux website window"`

### open\_in\_new\_off

> **open\_in\_new\_off**: `string` = `"arrow box disabled enabled export on slash window"`

### open\_with

> **open\_with**: `string` = `"arrows directional expand move"`

### other\_houses

> **other\_houses**: `string` = `"architecture cottage estate home maps place real residence residential stay traveling"`

### outbound

> **outbound**: `string` = `"arrow circle directional right up"`

### outbox

> **outbox**: `string` = `"mail send sent"`

### outdoor\_grill

> **outdoor\_grill**: `string` = `"barbecue barbeque bbq charcoal cooking home house outside"`

### outlet

> **outlet**: `string` = `"connecter electricity plug power"`

### outlined\_flag

> **outlined\_flag**: `string` = `"country goal mark nation report start"`

### padding

> **padding**: `string` = `"design layout margin size square"`

### pages

> **pages**: `string` = `"article gplus paper post star"`

### pageview

> **pageview**: `string` = `"document find glass magnifying paper search"`

### paid

> **paid**: `string` = `"circle currency money payment transaction"`

### palette

> **palette**: `string` = `"art colors filters paint"`

### pan\_tool

> **pan\_tool**: `string` = `"drag fingers gesture hands human move scan stop touch wait"`

### panorama

> **panorama**: `string` = `"angle image mountains photography picture view wide"`

### panorama\_fish\_eye

> **panorama\_fish\_eye**: `string` = `"angle circle image photography picture wide"`

### panorama\_horizontal

> **panorama\_horizontal**: `string` = `"angle image photography picture wide"`

### panorama\_photosphere

> **panorama\_photosphere**: `string` = `"angle horizontal image photography picture wide"`

### panorama\_vertical

> **panorama\_vertical**: `string` = `"angle image photography picture wide"`

### panorama\_wide\_angle

> **panorama\_wide\_angle**: `string` = `"image photography picture"`

### paragliding

> **paragliding**: `string` = `"athlete athletic body entertainment exercise fly hobby human parachute people person skydiving social sports travel"`

### park

> **park**: `string` = `"attraction fresh local nature outside plant tree"`

### party\_mode

> **party\_mode**: `string` = `"camera lens photography picture"`

### password

> **password**: `string` = `"key login pin security star unlock"`

### pattern

> **pattern**: `string` = `"key login password pin security star unlock"`

### pause

> **pause**: `string` = `"controls media music pending player status video wait"`

### pause\_circle

> **pause\_circle**: `string` = `"controls media music video"`

### pause\_circle\_filled

> **pause\_circle\_filled**: `string` = `"controls media music pending status video wait"`

### pause\_circle\_outline

> **pause\_circle\_outline**: `string` = `"controls media music pending status video wait"`

### pause\_presentation

> **pause\_presentation**: `string` = `"application desktop device pending screen share slides status wait website window www"`

### payment

> **payment**: `string` = `"bill cash charge coin commerce cost creditcard currency dollars finance financial information money online price shopping symbol"`

### payments

> **payments**: `string` = `"bill card cash coin commerce cost credit currency dollars finance layer money multiple online price shopping symbol"`

### pedal\_bike

> **pedal\_bike**: `string` = `"automobile bicycle cars direction human maps public route scooter transportation vehicle vespa"`

### pending

> **pending**: `string` = `"circle dots loading progress waiting"`

### pending\_actions

> **pending\_actions**: `string` = `"clipboard clock date document remember schedule time"`

### pentagon

> **pentagon**: `string` = `"five shape sides"`

### people

> **people**: `string` = `"accounts committee community face family friends group humans network persons profiles social team users"`

### people\_alt

> **people\_alt**: `string` = `"accounts committee face family friends group humans network persons profiles social team users"`

### people\_outline

> **people\_outline**: `string` = `"accounts committee face family friends group humans network persons profiles social team users"`

### percent

> **percent**: `string` = `"math number symbol"`

### perm\_camera\_mic

> **perm\_camera\_mic**: `string` = `"image microphone min photography picture speaker"`

### perm\_contact\_calendar

> **perm\_contact\_calendar**: `string` = `"account agenda date face human information people person profile schedule time user"`

### perm\_data\_setting

> **perm\_data\_setting**: `string` = `"cellular configure gear information network settings wifi wireless"`

### perm\_device\_information

> **perm\_device\_information**: `string` = `"Android alert announcement cell hardware iOS important mobile phone tablet"`

### perm\_identity

> **perm\_identity**: `string` = `"account avatar face human information people person profile save, thumbnail user"`

### perm\_media

> **perm\_media**: `string` = `"collection data directories document file folders images landscape mountains photography picture save storage"`

### perm\_phone\_msg

> **perm\_phone\_msg**: `string` = `"bubble call cell chat comment communicate contact device message mobile recording save speech telephone voice"`

### perm\_scan\_wifi

> **perm\_scan\_wifi**: `string` = `"alert announcement connection information internet network service signal wireless"`

### person

> **person**: `string` = `"account avatar face human people profile user"`

### person\_add

> **person\_add**: `string` = `"+ account avatar face friend human new people plus profile symbol user"`

### person\_add\_alt

> **person\_add\_alt**: `string` = `"+ account face human people plus profile user"`

### person\_add\_disabled

> **person\_add\_disabled**: `string` = `"+ account enabled face human new offline people plus profile slash symbol user"`

### person\_off

> **person\_off**: `string` = `"account avatar disabled enabled face human people profile slash user"`

### person\_outline

> **person\_outline**: `string` = `"account avatar face human people profile user"`

### person\_pin

> **person\_pin**: `string` = `"account avatar destination direction face gps human location maps people place profile stop user"`

### person\_pin\_circle

> **person\_pin\_circle**: `string` = `"account destination direction face gps human location maps people place profile stop user"`

### person\_remove

> **person\_remove**: `string` = `"account avatar delete face human minus people profile unfriend user"`

### person\_search

> **person\_search**: `string` = `"account avatar face find glass human look magnifying people profile user"`

### personal\_video

> **personal\_video**: `string` = `"Android cam chrome desktop device hardware iOS mac monitor television tv web window"`

### pest\_control

> **pest\_control**: `string` = `"bug exterminator insects"`

### pest\_control\_rodent

> **pest\_control\_rodent**: `string` = `"exterminator mice"`

### pets

> **pets**: `string` = `"animal cat claw dog hand paw"`

### phishing

> **phishing**: `string` = `"fishing fraud hook scam"`

### phone

> **phone**: `string` = `"call cell chat contact device hardware mobile telephone text"`

### phone\_android

> **phone\_android**: `string` = `"cell device hardware iOS mobile tablet"`

### phone\_bluetooth\_speaker

> **phone\_bluetooth\_speaker**: `string` = `"call cell connection connectivity contact device hardware mobile signal symbol telephone wireless"`

### phone\_callback

> **phone\_callback**: `string` = `"arrow cell contact device down hardware mobile telephone"`

### phone\_disabled

> **phone\_disabled**: `string` = `"call cell contact device enabled hardware mobile offline slash telephone"`

### phone\_enabled

> **phone\_enabled**: `string` = `"call cell contact device hardware mobile telephone"`

### phone\_forwarded

> **phone\_forwarded**: `string` = `"arrow call cell contact device direction hardware mobile right telephone"`

### phone\_iphone

> **phone\_iphone**: `string` = `"Android apple cell device hardware iOS mobile tablet"`

### phone\_locked

> **phone\_locked**: `string` = `"call cell contact device hardware mobile password privacy private protection safety secure security telephone"`

### phone\_missed

> **phone\_missed**: `string` = `"arrow call cell contact device hardware mobile telephone"`

### phone\_paused

> **phone\_paused**: `string` = `"call cell contact device hardware mobile telephone wait"`

### phonelink

> **phonelink**: `string` = `"Android chrome computer connect desktop device hardware iOS mac mobile sync tablet web windows"`

### phonelink\_erase

> **phonelink\_erase**: `string` = `"Android cancel cell close connection device exit hardware iOS mobile no remove stop tablet"`

### phonelink\_lock

> **phonelink\_lock**: `string` = `"Android cell connection device erase hardware iOS locked mobile password privacy private protection safety secure security tablet"`

### phonelink\_off

> **phonelink\_off**: `string` = `"Android chrome computer connect desktop device disabled enabled hardware iOS mac mobile slash sync tablet web windows"`

### phonelink\_ring

> **phonelink\_ring**: `string` = `"Android cell connection data device hardware iOS mobile network service signal tablet wireless"`

### phonelink\_setup

> **phonelink\_setup**: `string` = `"Android call chat device hardware iOS information mobile settings tablet text"`

### photo

> **photo**: `string` = `"image mountains photography picture"`

### photo\_album

> **photo\_album**: `string` = `"archive bookmark image label library mountains photography picture ribbon save tag"`

### photo\_camera

> **photo\_camera**: `string` = `"image photography picture"`

### photo\_camera\_back

> **photo\_camera\_back**: `string` = `"image landscape mountains photography picture rear"`

### photo\_camera\_front

> **photo\_camera\_front**: `string` = `"account face human image people person photography picture portrait profile user"`

### photo\_filter

> **photo\_filter**: `string` = `"filters image photography picture stars"`

### photo\_library

> **photo\_library**: `string` = `"album image mountains photography picture"`

### photo\_size\_select\_actual

> **photo\_size\_select\_actual**: `string` = `"image mountains photography picture"`

### photo\_size\_select\_large

> **photo\_size\_select\_large**: `string` = `"adjust album editing image library mountains photography picture"`

### photo\_size\_select\_small

> **photo\_size\_select\_small**: `string` = `"adjust album editing image large library mountains photography picture"`

### php

> **php**: `string` = `"alphabet brackets character code css developer engineering font html letter platform symbol text type"`

### piano

> **piano**: `string` = `"instrument keyboard keys musical social"`

### piano\_off

> **piano\_off**: `string` = `"disabled enabled instrument keyboard keys musical on slash social"`

### picture\_as\_pdf

> **picture\_as\_pdf**: `string` = `"alphabet character document file font image letter multiple photography symbol text type"`

### picture\_in\_picture

> **picture\_in\_picture**: `string` = `"cropped overlap photo position shape"`

### picture\_in\_picture\_alt

> **picture\_in\_picture\_alt**: `string` = `"cropped overlap photo position shape"`

### pie\_chart

> **pie\_chart**: `string` = `"analytics bars data diagram infographic measure metrics statistics tracking"`

### pie\_chart\_outline

> **pie\_chart\_outline**: `string` = `"analytics bars data diagram infographic measure metrics statistics tracking"`

### pin

> **pin**: `string` = `"1 2 3 digit key login logout number password pattern security star symbol unlock"`

### pin\_drop

> **pin\_drop**: `string` = `"destination direction gps location maps navigation place stop"`

### pinch

> **pinch**: `string` = `"arrows compress direction finger grasp hand navigation nip squeeze tweak"`

### pivot\_table\_chart

> **pivot\_table\_chart**: `string` = `"analytics arrows bars data diagram direction drive editing grid infographic measure metrics rotate sheet statistics tracking"`

### place

> **place**: `string` = `"destination direction location maps navigation pin point stop"`

### plagiarism

> **plagiarism**: `string` = `"document find glass look magnifying page paper search see"`

### play\_arrow

> **play\_arrow**: `string` = `"controls media music player start video"`

### play\_circle

> **play\_circle**: `string` = `"arrow controls media music video"`

### play\_disabled

> **play\_disabled**: `string` = `"controls enabled media music off slash video"`

### play\_for\_work

> **play\_for\_work**: `string` = `"arrow circle down google half"`

### play\_lesson

> **play\_lesson**: `string` = `"audio bookmark digital ebook lesson multimedia play reading ribbon"`

### playlist\_add

> **playlist\_add**: `string` = `"+ collection music new plus symbol task todo"`

### playlist\_add\_check

> **playlist\_add\_check**: `string` = `"approve checkmark collection complete done music ok select task tick todo validate verified yes"`

### playlist\_add\_check\_circle

> **playlist\_add\_check\_circle**: `string` = `"album artist audio cd collection mark music record sound track"`

### playlist\_add\_circle

> **playlist\_add\_circle**: `string` = `"album artist audio cd check collection mark music record sound track"`

### playlist\_play

> **playlist\_play**: `string` = `"arow arrow collection music"`

### playlist\_remove

> **playlist\_remove**: `string` = `"- collection minus music"`

### plumbing

> **plumbing**: `string` = `"build construction fix handyman repair tools wrench"`

### plus\_one

> **plus\_one**: `string` = `"1 add digit increase number symbol"`

### podcasts

> **podcasts**: `string` = `"broadcast casting network signal transmitting wireless"`

### point\_of\_sale

> **point\_of\_sale**: `string` = `"checkout cost machine merchant money payment pos retail system transaction"`

### policy

> **policy**: `string` = `"certified find glass legal look magnifying privacy private protection search security see shield verified"`

### poll

> **poll**: `string` = `"analytics barchart bars data diagram infographic measure metrics statistics survey tracking vote"`

### pool

> **pool**: `string` = `"athlete athletic beach body entertainment exercise hobby human ocean people person places sea sports swimming water"`

### portable\_wifi\_off

> **portable\_wifi\_off**: `string` = `"connected connection data device disabled enabled internet network offline service signal slash usage wireless"`

### portrait

> **portrait**: `string` = `"account face human people person photo picture profile user"`

### post\_add

> **post\_add**: `string` = `"+ data document drive file folders item page paper plus sheet slide text writing"`

### power

> **power**: `string` = `"charge cord electrical online outlet plug socket"`

### power\_input

> **power\_input**: `string` = `"dc lines supply"`

### power\_off

> **power\_off**: `string` = `"charge cord disabled electrical enabled on outlet plug slash"`

### power\_settings\_new

> **power\_settings\_new**: `string` = `"information off save shutdown"`

### precision\_manufacturing

> **precision\_manufacturing**: `string` = `"arm automatic chain conveyor crane factory industry machinery mechanical production repairing robot supply warehouse"`

### pregnant\_woman

> **pregnant\_woman**: `string` = `"baby birth body female human lady maternity mom mother people person user women"`

### present\_to\_all

> **present\_to\_all**: `string` = `"arrow presentation screen share slides website"`

### preview

> **preview**: `string` = `"design eye layout reveal screen see show website window www"`

### price\_change

> **price\_change**: `string` = `"arrows bill card cash coin commerce cost credit currency dollars down finance money online payment shopping symbol up"`

### price\_check

> **price\_check**: `string` = `"approve bill card cash coin commerce complete cost credit currency dollars done finance mark money ok online payment select shopping symbol tick validate verified yes"`

### print

> **print**: `string` = `"draft fax ink machine office paper printer send"`

### print\_disabled

> **print\_disabled**: `string` = `"enabled off on paper printer slash"`

### priority\_high

> **priority\_high**: `string` = `"! alert attention caution danger error exclamation important mark notification symbol warning"`

### privacy\_tip

> **privacy\_tip**: `string` = `"alert announcement announcment assistance certified details help information private protection security service shield support verified"`

### production\_quantity\_limits

> **production\_quantity\_limits**: `string` = `"! alert attention bill card cart cash caution coin commerce credit currency danger dollars error exclamation important mark money notification online payment shopping symbol warning"`

### propane

> **propane**: `string` = `"gas nest"`

### propane\_tank

> **propane\_tank**: `string` = `"bbq gas grill nest"`

### psychology

> **psychology**: `string` = `"behavior body brain cognitive function gear head human intellectual mental mind people person preferences psychiatric science settings social therapy thinking thoughts"`

### public

> **public**: `string` = `"country earth global globe language map network planet social space web world"`

### public\_off

> **public\_off**: `string` = `"disabled earth enabled global globe map network on planet slash social space web world"`

### publish

> **publish**: `string` = `"arrow cloud file import submit upload"`

### published\_with\_changes

> **published\_with\_changes**: `string` = `"approve arrows check complete done inprogress loading mark ok refresh renew replace rotate select tick validate verified yes"`

### push\_pin

> **push\_pin**: `string` = `"location marker place remember save"`

### qr\_code

> **qr\_code**: `string` = `"barcode camera media product quick response smartphone urls"`

### qr\_code\_2

> **qr\_code\_2**: `string` = `"barcode camera media product quick response smartphone urls"`

### qr\_code\_scanner

> **qr\_code\_scanner**: `string` = `"barcode camera media product quick response smartphone urls"`

### query\_builder

> **query\_builder**: `string` = `"clock date hour minute save schedule time"`

### query\_stats

> **query\_stats**: `string` = `"analytics chart data diagram find glass infographic line look magnifying measure metrics search see statistics tracking"`

### question\_answer

> **question\_answer**: `string` = `"bubble chat comment communicate conversation converse feedback message speech talk"`

### question\_mark

> **question\_mark**: `string` = `"? assistance help information mark punctuation question support symbol"`

### queue

> **queue**: `string` = `"add collection layers multiple music playlist stack stream video"`

### queue\_music

> **queue\_music**: `string` = `"add collection playlist stream"`

### queue\_play\_next

> **queue\_play\_next**: `string` = `"+ add arrow collection desktop device display hardware monitor music new playlist plus screen steam symbol tv video"`

### quickreply

> **quickreply**: `string` = `"bubble chat comment communicate fast lightning message speech thunderbolt"`

### quiz

> **quiz**: `string` = `"? assistance faq help information mark punctuation question support symbol test"`

### r\_mobiledata

> **r\_mobiledata**: `string` = `"alphabet character font letter symbol text type"`

### radar

> **radar**: `string` = `"detect military near network position scan"`

### radio

> **radio**: `string` = `"antenna audio device frequency hardware listen media music player signal tune"`

### radio\_button\_checked

> **radio\_button\_checked**: `string` = `"application bullet circle components design form interface off point record screen selected toggle ui ux website"`

### radio\_button\_unchecked

> **radio\_button\_unchecked**: `string` = `"bullet circle deselected form off point record toggle"`

### railway\_alert

> **railway\_alert**: `string` = `"! attention automobile bike cars caution danger direction error exclamation important maps mark notification public scooter subway symbol train transportation vehicle vespa warning"`

### ramen\_dining

> **ramen\_dining**: `string` = `"breakfast dinner drink fastfood lunch meal noodles restaurant"`

### ramp\_left

> **ramp\_left**: `string` = `"arrows directions maps navigation path route sign traffic"`

### ramp\_right

> **ramp\_right**: `string` = `"arrows directions maps navigation path route sign traffic"`

### rate\_review

> **rate\_review**: `string` = `"chat comment feedback message pencil stars write"`

### raw\_off

> **raw\_off**: `string` = `"alphabet character disabled enabled font image letter original photography slash symbol text type"`

### raw\_on

> **raw\_on**: `string` = `"alphabet character disabled enabled font image letter off original photography slash symbol text type"`

### read\_more

> **read\_more**: `string` = `"arrow text"`

### receipt

> **receipt**: `string` = `"bill credit invoice paper payment sale transaction"`

### receipt\_long

> **receipt\_long**: `string` = `"bill check document list paperwork record store transaction"`

### recent\_actors

> **recent\_actors**: `string` = `"account avatar cards carousel contacts face human layers list people person profile thumbnail user"`

### recommend

> **recommend**: `string` = `"approved circle confirm favorite gesture hand like reaction social support thumbs well"`

### record\_voice\_over

> **record\_voice\_over**: `string` = `"account face human people person profile recording sound speaking speech transcript user"`

### rectangle

> **rectangle**: `string` = `"four parallelograms polygons quadrilaterals recangle shape sides"`

### redeem

> **redeem**: `string` = `"bill cart cash certificate coin commerce credit currency dollars giftcard money online payment present shopping"`

### redo

> **redo**: `string` = `"arrow backward forward next repeat rotate undo"`

### reduce\_capacity

> **reduce\_capacity**: `string` = `"arrow body covid decrease down human people person social"`

### refresh

> **refresh**: `string` = `"around arrows direction inprogress loading navigation refresh renew right rotate turn"`

### remember\_me

> **remember\_me**: `string` = `"Android avatar device hardware human iOS identity mobile people person phone profile tablet user"`

### remove

> **remove**: `string` = `"can delete line minus negative substract subtract trash"`

### remove\_circle

> **remove\_circle**: `string` = `"allowed banned block can delete disable minus negative not substract trash"`

### remove\_circle\_outline

> **remove\_circle\_outline**: `string` = `"allowed banned block can delete disable minus negative not substract trash"`

### remove\_done

> **remove\_done**: `string` = `"approve check complete disabled enabled finished mark multiple off ok select slash tick yes"`

### remove\_from\_queue

> **remove\_from\_queue**: `string` = `"collection desktop device display hardware list monitor screen steam television"`

### remove\_moderator

> **remove\_moderator**: `string` = `"certified disabled enabled off privacy private protection security shield slash verified"`

### remove\_red\_eye

> **remove\_red\_eye**: `string` = `"iris looking preview see sight vision"`

### remove\_road

> **remove\_road**: `string` = `"- cancel close destination direction exit highway maps minus new no stop street symbol traffic"`

### remove\_shopping\_cart

> **remove\_shopping\_cart**: `string` = `"card cash checkout coin commerce credit currency disabled dollars enabled off online payment slash tick"`

### reorder

> **reorder**: `string` = `"format lines list stacked"`

### repeat

> **repeat**: `string` = `"arrows controls media music video"`

### repeat\_on

> **repeat\_on**: `string` = `"arrows controls media music video"`

### repeat\_one

> **repeat\_one**: `string` = `"1 arrows controls digit media music number symbol video"`

### repeat\_one\_on

> **repeat\_one\_on**: `string` = `"arrows controls digit media music number symbol video"`

### replay

> **replay**: `string` = `"arrows controls music refresh reload renew repeat retry rewind undo video"`

### replay\_10

> **replay\_10**: `string` = `"arrows controls digit music number refresh renew repeat rewind symbol ten video"`

### replay\_30

> **replay\_30**: `string` = `"arrows controls digit music number refresh renew repeat rewind symbol thirty video"`

### replay\_5

> **replay\_5**: `string` = `"arrows controls digit five music number refresh renew repeat rewind symbol video"`

### replay\_circle\_filled

> **replay\_circle\_filled**: `string` = `"arrows controls music refresh renew repeat video"`

### reply

> **reply**: `string` = `"arrow backward left mail message send share"`

### reply\_all

> **reply\_all**: `string` = `"arrows backward group left mail message multiple send share"`

### report

> **report**: `string` = `"! alert attention caution danger error exclamation important mark notification octagon symbol warning"`

### report\_gmailerrorred

> **report\_gmailerrorred**: `string` = `"! alert attention caution danger exclamation important mark notification octagon symbol warning"`

### report\_off

> **report\_off**: `string` = `"! alert attention caution danger disabled enabled error exclamation important mark notification octagon offline slash symbol warning"`

### report\_problem

> **report\_problem**: `string` = `"! alert announcement attention caution danger error exclamation feedback important mark notification symbol triangle warning"`

### request\_quote

> **request\_quote**: `string` = `"bill card cash coin commerce cost credit currency dollars finance money online payment price shopping symbol"`

### reset\_tv

> **reset\_tv**: `string` = `"arrow device hardware monitor television"`

### restart\_alt

> **restart\_alt**: `string` = `"around arrow inprogress loading reboot refresh renew repeat reset"`

### restaurant

> **restaurant**: `string` = `"breakfast cutlery dining dinner eat food fork knife local lunch meal places spoon utensils"`

### restaurant\_menu

> **restaurant\_menu**: `string` = `"book dining eat food fork knife local meal spoon"`

### restore

> **restore**: `string` = `"arrow backwards clock date history refresh renew reverse rotate schedule time turn undo"`

### restore\_from\_trash

> **restore\_from\_trash**: `string` = `"arrow backwards can clock date delete garbage history refresh remove renew reverse rotate schedule time turn up"`

### restore\_page

> **restore\_page**: `string` = `"arrow data doc file history paper refresh rotate sheet storage undo web"`

### reviews

> **reviews**: `string` = `"bubble chat comment communicate feedback message rate rating recommendation speech"`

### rice\_bowl

> **rice\_bowl**: `string` = `"dinner food lunch meal restaurant"`

### ring\_volume

> **ring\_volume**: `string` = `"calling cell contact device hardware incoming mobile ringer sound telephone"`

### rocket

> **rocket**: `string` = `"spaceship"`

### rocket\_launch

> **rocket\_launch**: `string` = `"spaceship takeoff"`

### roller\_shades

> **roller\_shades**: `string` = `"blinds cover curtains nest open shutter sunshade"`

### roller\_shades\_closed

> **roller\_shades\_closed**: `string` = `"blinds cover curtains nest shutter sunshade"`

### roofing

> **roofing**: `string` = `"architecture building chimney construction estate home house real residence residential service shelter"`

### room

> **room**: `string` = `"destination direction gps location maps marker pin place spot stop"`

### room\_preferences

> **room\_preferences**: `string` = `"building doorway entrance gear home house interior office open settings"`

### room\_service

> **room\_service**: `string` = `"alert bell concierge delivery hotel notify"`

### rotate\_90\_degrees\_ccw

> **rotate\_90\_degrees\_ccw**: `string` = `"arrows direction editing image photo turn"`

### rotate\_90\_degrees\_cw

> **rotate\_90\_degrees\_cw**: `string` = `"arrows ccw direction editing image photo turn"`

### rotate\_left

> **rotate\_left**: `string` = `"around arrow circle direction inprogress loading refresh reload renew reset turn"`

### rotate\_right

> **rotate\_right**: `string` = `"around arrow circle direction inprogress loading refresh renew turn"`

### roundabout\_left

> **roundabout\_left**: `string` = `"arrows directions maps navigation path route sign traffic"`

### roundabout\_right

> **roundabout\_right**: `string` = `"arrows directions maps navigation path route sign traffic"`

### rounded\_corner

> **rounded\_corner**: `string` = `"adjust edit shape square transform"`

### route

> **route**: `string` = `"directions maps path sign traffic"`

### router

> **router**: `string` = `"box cable connection device hardware internet network signal wifi"`

### rowing

> **rowing**: `string` = `"activity boat body canoe human people person sports water"`

### rss\_feed

> **rss\_feed**: `string` = `"application blog connection data internet network service signal website wifi wireless"`

### rsvp

> **rsvp**: `string` = `"alphabet character font invitation invite letter plaît respond répondez sil symbol text type vous"`

### rtt

> **rtt**: `string` = `"call real rrt text time"`

### rule

> **rule**: `string` = `"approve check done incomplete line mark missing no ok select tick validate verified wrong x yes"`

### rule\_folder

> **rule\_folder**: `string` = `"approve cancel check close complete data document done drive exit file mark no ok remove select sheet slide storage tick validate verified yes"`

### run\_circle

> **run\_circle**: `string` = `"body exercise human people person running"`

### running\_with\_errors

> **running\_with\_errors**: `string` = `"! alert attention caution danger duration exclamation important mark notification processing symbol time warning"`

### rv\_hookup

> **rv\_hookup**: `string` = `"arrow attach automobile automotive back cars connect direction left maps public right trailer transportation travel truck van vehicle"`

### safety\_divider

> **safety\_divider**: `string` = `"apart distance separate social space"`

### sailing

> **sailing**: `string` = `"entertainment fishing hobby ocean sailboat sea social sports travel water"`

### sanitizer

> **sanitizer**: `string` = `"bacteria bottle clean covid disinfect germs pump"`

### satellite

> **satellite**: `string` = `"bluetooth connection connectivity data device image internet landscape location maps mountains network photography picture scan service signal symbol wifi wireless--"`

### satellite\_alt

> **satellite\_alt**: `string` = `"alternative artificial communication space station television"`

### save

> **save**: `string` = `"data diskette document drive file floppy multimedia storage write"`

### save\_alt

> **save\_alt**: `string` = `"arrow diskette document down file floppy multimedia write"`

### save\_as

> **save\_as**: `string` = `"compose create data disk document draft drive editing file floppy input multimedia pencil storage write writing"`

### saved\_search

> **saved\_search**: `string` = `"find glass important look magnifying marked see star"`

### savings

> **savings**: `string` = `"bank bill card cash coin commerce cost credit currency dollars finance money online payment piggy symbol"`

### scale

> **scale**: `string` = `"measure monitor weight"`

### scanner

> **scanner**: `string` = `"copy device hardware machine"`

### scatter\_plot

> **scatter\_plot**: `string` = `"analytics bars chart circles data diagram dot infographic measure metrics statistics tracking"`

### schedule

> **schedule**: `string` = `"calendar clock date mark save time"`

### schedule\_send

> **schedule\_send**: `string` = `"calendar clock date email letter remember share time"`

### schema

> **schema**: `string` = `"analytics chart data diagram flow infographic measure metrics statistics tracking"`

### school

> **school**: `string` = `"academy achievement cap class college education graduation hat knowledge learning university"`

### science

> **science**: `string` = `"beaker chemical chemistry experiment flask glass laboratory research tube"`

### score

> **score**: `string` = `"2k alphabet analytics bars character chart data diagram digit font infographic letter measure metrics number statistics symbol text tracking type"`

### screen\_lock\_landscape

> **screen\_lock\_landscape**: `string` = `"Android device hardware iOS mobile phone rotate security tablet"`

### screen\_lock\_portrait

> **screen\_lock\_portrait**: `string` = `"Android device hardware iOS mobile phone rotate security tablet"`

### screen\_lock\_rotation

> **screen\_lock\_rotation**: `string` = `"Android arrow device hardware iOS mobile phone rotate tablet turn"`

### screen\_rotation

> **screen\_rotation**: `string` = `"Android arrow device hardware iOS mobile phone rotate tablet turn"`

### screen\_search\_desktop

> **screen\_search\_desktop**: `string` = `"Android arrow device hardware iOS lock monitor rotate web"`

### screen\_share

> **screen\_share**: `string` = `"Android arrow cast chrome device display hardware iOS laptop mac mirror monitor steam streaming web window"`

### screenshot

> **screenshot**: `string` = `"Android cell crop device hardware iOS mobile phone tablet"`

### screenshot\_monitor

> **screenshot\_monitor**: `string` = `"Android chrome desktop device display hardware iOS mac screengrab web window"`

### sd

> **sd**: `string` = `"alphabet camera card character data device digital drive flash font image letter memory photo secure symbol text type"`

### sd\_card

> **sd\_card**: `string` = `"camera digital memory photos secure storage"`

### sd\_card\_alert

> **sd\_card\_alert**: `string` = `"! attention camera caution danger digital error exclamation important mark memory notification photos secure storage symbol warning"`

### sd\_storage

> **sd\_storage**: `string` = `"camera card data digital memory microsd secure"`

### search

> **search**: `string` = `"filter find glass look magnifying see up"`

### search\_off

> **search\_off**: `string` = `"cancel close disabled enabled find glass look magnifying on see slash stop x"`

### security

> **security**: `string` = `"certified privacy private protection shield verified"`

### security\_update

> **security\_update**: `string` = `"Android arrow device download hardware iOS mobile phone tablet"`

### security\_update\_good

> **security\_update\_good**: `string` = `"Android checkmark device hardware iOS mobile ok phone tablet tick"`

### security\_update\_warning

> **security\_update\_warning**: `string` = `"! Android alert attention caution danger device download error exclamation hardware iOS important mark mobile notification phone symbol tablet"`

### segment

> **segment**: `string` = `"alignment fonts format lines list paragraph part piece rules style text"`

### select\_all

> **select\_all**: `string` = `"selection square tool"`

### self\_improvement

> **self\_improvement**: `string` = `"body calm care chi human meditate meditation people person relax sitting wellbeing yoga zen"`

### sell

> **sell**: `string` = `"bill card cart cash coin commerce credit currency dollars money online payment price shopping tag"`

### send

> **send**: `string` = `"chat email message paper plane reply right share telegram"`

### send\_and\_archive

> **send\_and\_archive**: `string` = `"arrow download email letter save share"`

### send\_time\_extension

> **send\_time\_extension**: `string` = `"deliver dispatch envelop mail message schedule"`

### send\_to\_mobile

> **send\_to\_mobile**: `string` = `"Android arrow device export forward hardware iOS phone right share tablet"`

### sensors

> **sensors**: `string` = `"connection network scan signal wireless"`

### sensors\_off

> **sensors\_off**: `string` = `"connection disabled enabled network scan signal slash wireless"`

### sentiment\_dissatisfied

> **sentiment\_dissatisfied**: `string` = `"angry disappointed dislike emoji emoticon emotions expressions face feelings frown mood person sad smiley survey unhappy unsatisfied upset"`

### sentiment\_neutral

> **sentiment\_neutral**: `string` = `"emotionless emotions expressions face feelings indifference mood okay person survey"`

### sentiment\_satisfied

> **sentiment\_satisfied**: `string` = `"emoji emoticon emotions expressions face feelings glad happiness happy like mood person pleased smiley smiling survey"`

### sentiment\_satisfied\_alt

> **sentiment\_satisfied\_alt**: `string` = `"account emoji face happy human people person profile smile user"`

### sentiment\_very\_dissatisfied

> **sentiment\_very\_dissatisfied**: `string` = `"angry disappointed dislike emoji emoticon emotions expressions face feelings mood person sad smiley sorrow survey unhappy unsatisfied upset"`

### sentiment\_very\_satisfied

> **sentiment\_very\_satisfied**: `string` = `"emoji emoticon emotions expressions face feelings glad happiness happy like mood person pleased smiley smiling survey"`

### set\_meal

> **set\_meal**: `string` = `"chopsticks dinner fish food lunch restaurant teishoku"`

### settings

> **settings**: `string` = `"application change details gear information options personal service"`

### settings\_accessibility

> **settings\_accessibility**: `string` = `"body details human information people personal preferences profile user"`

### settings\_applications

> **settings\_applications**: `string` = `"change details gear information options personal save service"`

### settings\_backup\_restore

> **settings\_backup\_restore**: `string` = `"arrow backwards history refresh reverse rotate time undo"`

### settings\_bluetooth

> **settings\_bluetooth**: `string` = `"connection connectivity device network signal symbol wifi"`

### settings\_brightness

> **settings\_brightness**: `string` = `"dark filter light mode sun"`

### settings\_cell

> **settings\_cell**: `string` = `"Android cellphone device hardware iOS mobile tablet"`

### settings\_ethernet

> **settings\_ethernet**: `string` = `"arrows brackets computer connection connectivity dots internet network parenthesis wifi"`

### settings\_input\_antenna

> **settings\_input\_antenna**: `string` = `"airplay arrows computer connection connectivity dots internet network screencast stream wifi wireless"`

### settings\_input\_component

> **settings\_input\_component**: `string` = `"audio av cables connection connectivity internet plugs points video wifi"`

### settings\_input\_composite

> **settings\_input\_composite**: `string` = `"cable component connection connectivity plugs points"`

### settings\_input\_hdmi

> **settings\_input\_hdmi**: `string` = `"cable connection connectivity definition high plugin points video wire"`

### settings\_input\_svideo

> **settings\_input\_svideo**: `string` = `"cable connection connectivity definition plugin plugs points standard svideo,"`

### settings\_overscan

> **settings\_overscan**: `string` = `"arrows expand image photo picture"`

### settings\_phone

> **settings\_phone**: `string` = `"call cell contact device hardware mobile telephone"`

### settings\_power

> **settings\_power**: `string` = `"information off save shutdown"`

### settings\_remote

> **settings\_remote**: `string` = `"bluetooth connection connectivity control device signal wifi wireless"`

### settings\_suggest

> **settings\_suggest**: `string` = `"change details gear options recommendation service suggestion system"`

### settings\_system\_daydream

> **settings\_system\_daydream**: `string` = `"backup cloud drive storage"`

### settings\_voice

> **settings\_voice**: `string` = `"microphone recorder speaker"`

### share

> **share**: `string` = `"android connect contect link multimedia multiple network options send shared sharing social"`

### share\_location

> **share\_location**: `string` = `"destination direction gps maps pin place stop tracking"`

### shield

> **shield**: `string` = `"certified privacy private protection secure security verified"`

### shop

> **shop**: `string` = `"arrow bag bill briefcase buy card cart cash coin commerce credit currency dollars google money online payment play purchase shopping store"`

### shop\_2

> **shop\_2**: `string` = `"add arrow buy cart google play purchase shopping"`

### shop\_two

> **shop\_two**: `string` = `"add arrow briefcase buy cart google play purchase shopping"`

### shopping\_bag

> **shopping\_bag**: `string` = `"bill business buy card cart cash coin commerce credit currency dollars money online payment storefront"`

### shopping\_basket

> **shopping\_basket**: `string` = `"add bill buy card cart cash checkout coin commerce credit currency dollars money online payment purchase"`

### shopping\_cart

> **shopping\_cart**: `string` = `"add bill buy card cash checkout coin commerce credit currency dollars money online payment purchase"`

### shopping\_cart\_checkout

> **shopping\_cart\_checkout**: `string` = `"arrow cash coin commerce currency dollars money online payment right"`

### short\_text

> **short\_text**: `string` = `"brief comment document lines note write writing"`

### shortcut

> **shortcut**: `string` = `"arrow direction forward right"`

### show\_chart

> **show\_chart**: `string` = `"analytics bars chart data diagram infographic line measure metrics presentation show statistics stock tracking"`

### shower

> **shower**: `string` = `"bathroom closet home house place plumbing sprinkler wash water wc"`

### shuffle

> **shuffle**: `string` = `"arrows controls music random video"`

### shuffle\_on

> **shuffle\_on**: `string` = `"arrows controls music random video"`

### shutter\_speed

> **shutter\_speed**: `string` = `"aperture camera duration image lens photography photos picture setting stop timer watch"`

### sick

> **sick**: `string` = `"covid discomfort emotions expressions face feelings fever flu ill mood pain person survey upset"`

### signal\_cellular\_0\_bar

> **signal\_cellular\_0\_bar**: `string` = `"data internet mobile network phone speed wifi wireless"`

### signal\_cellular\_4\_bar

> **signal\_cellular\_4\_bar**: `string` = `"data internet mobile network phone speed wifi wireless"`

### signal\_cellular\_alt

> **signal\_cellular\_alt**: `string` = `"analytics bar chart data diagram infographic internet measure metrics mobile network phone statistics tracking wifi wireless"`

### signal\_cellular\_connected\_no\_internet\_0\_bar

> **signal\_cellular\_connected\_no\_internet\_0\_bar**: `string` = `"! alert attention caution danger data error exclamation important mark mobile network notification phone symbol warning wifi wireless"`

### signal\_cellular\_connected\_no\_internet\_4\_bar

> **signal\_cellular\_connected\_no\_internet\_4\_bar**: `string` = `"! alert attention caution danger data error exclamation important mark mobile network notification phone symbol warning wifi wireless"`

### signal\_cellular\_no\_sim

> **signal\_cellular\_no\_sim**: `string` = `"camera card chip device disabled enabled memory network offline phone slash storage"`

### signal\_cellular\_nodata

> **signal\_cellular\_nodata**: `string` = `"internet mobile network offline phone quit wifi wireless x"`

### signal\_cellular\_null

> **signal\_cellular\_null**: `string` = `"data internet mobile network phone wifi wireless"`

### signal\_cellular\_off

> **signal\_cellular\_off**: `string` = `"data disabled enabled internet mobile network offline phone slash wifi wireless"`

### signal\_wifi\_bad

> **signal\_wifi\_bad**: `string` = `"bar cancel cellular close data exit internet mobile network no phone quit remove stop wireless"`

### signal\_wifi\_off

> **signal\_wifi\_off**: `string` = `"cellular data disabled enabled internet mobile network phone slash speed wireless"`

### signal\_wifi\_statusbar\_null

> **signal\_wifi\_statusbar\_null**: `string` = `"cellular data internet mobile network phone speed wireless"`

### signpost

> **signpost**: `string` = `"arrow direction left maps right signal signs street traffic"`

### sim\_card

> **sim\_card**: `string` = `"camera chip device memory network phone storage"`

### sim\_card\_alert

> **sim\_card\_alert**: `string` = `"! attention camera caution danger digital error exclamation important mark memory notification photos sd secure storage symbol warning"`

### sim\_card\_download

> **sim\_card\_download**: `string` = `"arrow camera chip device memory phone storage"`

### single\_bed

> **single\_bed**: `string` = `"bedroom double furniture home hotel house king night pillows queen rest sleep twin"`

### sip

> **sip**: `string` = `"alphabet call character dialer font initiation internet letter over phone protocol routing session symbol text type voice"`

### skateboarding

> **skateboarding**: `string` = `"athlete athletic body entertainment exercise hobby human people person skateboarder social sports"`

### skip\_next

> **skip\_next**: `string` = `"arrow back controls forward music play previous transport video"`

### skip\_previous

> **skip\_previous**: `string` = `"arrow backward controls forward music next play transport video"`

### sledding

> **sledding**: `string` = `"athlete athletic body entertainment exercise hobby human people person sledge snow social sports travel winter"`

### slideshow

> **slideshow**: `string` = `"movie photos play presentation square video view"`

### slow\_motion\_video

> **slow\_motion\_video**: `string` = `"arrow circle controls music play speed time"`

### smart\_button

> **smart\_button**: `string` = `"action auto components composer function interface special stars ui ux website"`

### smart\_display

> **smart\_display**: `string` = `"airplay chrome connect device screencast stream television tv video wireless"`

### smart\_screen

> **smart\_screen**: `string` = `"Android airplay cell connect device hardware iOS mobile phone screencast stream tablet video"`

### smart\_toy

> **smart\_toy**: `string` = `"games robot"`

### smartphone

> **smartphone**: `string` = `"Android call cell chat device hardware iOS mobile tablet text"`

### smoke\_free

> **smoke\_free**: `string` = `"cigarette disabled enabled never no off places prohibited slash smoking tobacco warning zone"`

### smoking\_rooms

> **smoking\_rooms**: `string` = `"allowed cigarette places smoke tobacco zone"`

### sms

> **sms**: `string` = `"3 bubble chat comment communication conversation dots message more service speech three"`

### sms\_failed

> **sms\_failed**: `string` = `"! alert attention bubbles caution chat comment communication conversation danger error exclamation important mark message notification service speech symbol warning"`

### snippet\_folder

> **snippet\_folder**: `string` = `"data document drive file sheet slide storage"`

### snooze

> **snooze**: `string` = `"alarm bell clock duration notification set timer watch"`

### snowboarding

> **snowboarding**: `string` = `"athlete athletic body entertainment exercise hobby human people person social sports travel winter"`

### snowmobile

> **snowmobile**: `string` = `"automobile car direction skimobile social sports transportation travel vehicle winter"`

### snowshoeing

> **snowshoeing**: `string` = `"body human people person sports travel walking winter"`

### soap

> **soap**: `string` = `"bathroom clean fingers gesture hand wash wc"`

### social\_distance

> **social\_distance**: `string` = `"6 apart body ft human people person space"`

### solar\_power

> **solar\_power**: `string` = `"eco energy heat nest sunny"`

### sort

> **sort**: `string` = `"filter find lines list organize"`

### sort\_by\_alpha

> **sort\_by\_alpha**: `string` = `"alphabetize az by character font letters list order organize symbol text type"`

### soup\_kitchen

> **soup\_kitchen**: `string` = `"breakfast brunch dining food lunch meal"`

### source

> **source**: `string` = `"code composer content creation data document file folder mode storage view"`

### south

> **south**: `string` = `"arrow directional down maps navigation"`

### south\_america

> **south\_america**: `string` = `"america continent landscape place region south"`

### south\_east

> **south\_east**: `string` = `"arrow directional down maps navigation right"`

### south\_west

> **south\_west**: `string` = `"arrow directional down left maps navigation"`

### spa

> **spa**: `string` = `"aromatherapy flower healthcare leaf massage meditation nature petals places relax wellbeing wellness"`

### space\_bar

> **space\_bar**: `string` = `"keyboard line"`

### speaker

> **speaker**: `string` = `"audio box electronic loud music sound stereo system video"`

### speaker\_group

> **speaker\_group**: `string` = `"audio box electronic loud multiple music sound stereo system video"`

### speaker\_notes

> **speaker\_notes**: `string` = `"bubble cards chat comment communicate format list message speech text"`

### speaker\_notes\_off

> **speaker\_notes\_off**: `string` = `"bubble cards chat comment communicate disabled enabled format list message on slash speech text"`

### speaker\_phone

> **speaker\_phone**: `string` = `"Android cell device hardware iOS mobile sound tablet volume"`

### speed

> **speed**: `string` = `"arrow clock controls dial fast gauge measure motion music slow speedometer test velocity video"`

### spellcheck

> **spellcheck**: `string` = `"alphabet approve character checkmark edit font letter ok processor select symbol text tick type word write yes"`

### splitscreen

> **splitscreen**: `string` = `"grid layout multitasking two"`

### spoke

> **spoke**: `string` = `"connection network radius"`

### sports

> **sports**: `string` = `"athlete athletic basketball blowing coach entertainment exercise game hobby instrument live referee soccer social sound trophy warning whistle"`

### sports\_bar

> **sports\_bar**: `string` = `"alcohol beer drink liquor pint places pub"`

### sports\_baseball

> **sports\_baseball**: `string` = `"athlete athletic entertainment exercise game hobby social"`

### sports\_basketball

> **sports\_basketball**: `string` = `"athlete athletic entertainment exercise game hobby social"`

### sports\_cricket

> **sports\_cricket**: `string` = `"athlete athletic ball bat entertainment exercise game hobby social"`

### sports\_esports

> **sports\_esports**: `string` = `"controller entertainment gamepad gaming hobby online playstation social video xbox"`

### sports\_football

> **sports\_football**: `string` = `"american athlete athletic entertainment exercise game hobby social"`

### sports\_golf

> **sports\_golf**: `string` = `"athlete athletic ball club entertainment exercise game golfer golfing hobby social"`

### sports\_handball

> **sports\_handball**: `string` = `"athlete athletic body entertainment exercise game hobby human people person social"`

### sports\_hockey

> **sports\_hockey**: `string` = `"athlete athletic entertainment exercise game hobby ice social sticks"`

### sports\_kabaddi

> **sports\_kabaddi**: `string` = `"athlete athletic body combat entertainment exercise fighting game hobby human judo martial people person social wrestle wrestling"`

### sports\_martial\_arts

> **sports\_martial\_arts**: `string` = `"athlete athletic entertainment exercise hobby human karate people person social"`

### sports\_mma

> **sports\_mma**: `string` = `"arts athlete athletic boxing combat entertainment exercise fighting game glove hobby martial mixed social"`

### sports\_motorsports

> **sports\_motorsports**: `string` = `"athlete athletic automobile bike drive driving entertainment helmet hobby motorcycle protect social vehicle"`

### sports\_rugby

> **sports\_rugby**: `string` = `"athlete athletic ball entertainment exercise game hobby social"`

### sports\_score

> **sports\_score**: `string` = `"destination flag goal"`

### sports\_soccer

> **sports\_soccer**: `string` = `"athlete athletic entertainment exercise football game hobby social"`

### sports\_tennis

> **sports\_tennis**: `string` = `"athlete athletic ball bat entertainment exercise game hobby racket social"`

### sports\_volleyball

> **sports\_volleyball**: `string` = `"athlete athletic entertainment exercise game hobby social"`

### square

> **square**: `string` = `"draw four quadrangle shape sides"`

### square\_foot

> **square\_foot**: `string` = `"construction feet inches length measurement ruler school set tools"`

### ssid\_chart

> **ssid\_chart**: `string` = `"graph lines network wifi"`

### stacked\_bar\_chart

> **stacked\_bar\_chart**: `string` = `"analytics chart-chart data diagram infographic measure metrics statistics tracking"`

### stacked\_line\_chart

> **stacked\_line\_chart**: `string` = `"analytics data diagram infographic measure metrics statistics tracking"`

### stadium

> **stadium**: `string` = `"activity amphitheater arena coliseum event local star things ticket"`

### stairs

> **stairs**: `string` = `"down staircase up"`

### star

> **star**: `string` = `"best bookmark favorite highlight ranking rate rating save toggle"`

### star\_border

> **star\_border**: `string` = `"best bookmark favorite highlight outline ranking rate rating save toggle"`

### star\_half

> **star\_half**: `string` = `"0.5 1/2 achievement bookmark favorite highlight important marked ranking rate rating reward saved shape special toggle"`

### star\_outline

> **star\_outline**: `string` = `"bookmark favorite half highlight ranking rate rating save toggle"`

### star\_rate

> **star\_rate**: `string` = `"achievement bookmark favorite highlight important marked ranking rating reward saved shape special"`

### stars

> **stars**: `string` = `"achievement bookmark circle favorite highlight important like love marked ranking rate rating reward saved shape special"`

### start

> **start**: `string` = `"arrow keyboard next right"`

### stay\_current\_landscape

> **stay\_current\_landscape**: `string` = `"Android device hardware iOS mobile phone tablet"`

### stay\_current\_portrait

> **stay\_current\_portrait**: `string` = `"Android device hardware iOS mobile phone tablet"`

### stay\_primary\_landscape

> **stay\_primary\_landscape**: `string` = `"Android current device hardware iOS mobile phone tablet"`

### stay\_primary\_portrait

> **stay\_primary\_portrait**: `string` = `"Android current device hardware iOS mobile phone tablet"`

### sticky\_note\_2

> **sticky\_note\_2**: `string` = `"bookmark message paper text writing"`

### stop

> **stop**: `string` = `"arrow controls music pause player square video"`

### stop\_circle

> **stop\_circle**: `string` = `"controls music pause play square video"`

### stop\_screen\_share

> **stop\_screen\_share**: `string` = `"Android arrow cast chrome device disabled display enabled hardware iOS laptop mac mirror monitor offline slash steam streaming web window"`

### storage

> **storage**: `string` = `"computer database drive memory network server"`

### store

> **store**: `string` = `"bill building business buy card cash coin company credit currency dollars e-commerce market money online payment purchase shopping storefront"`

### store\_mall\_directory

> **store\_mall\_directory**: `string` = `"building"`

### storefront

> **storefront**: `string` = `"business buy cafe commerce market merchant places restaurant retail sell shopping stall"`

### storm

> **storm**: `string` = `"forecast hurricane temperature twister weather wind"`

### straight

> **straight**: `string` = `"arrows directions maps navigation path route sign traffic up"`

### straighten

> **straighten**: `string` = `"length measurement piano ruler size"`

### stream

> **stream**: `string` = `"cast connected feed live network signal wireless"`

### streetview

> **streetview**: `string` = `"gps location maps"`

### strikethrough\_s

> **strikethrough\_s**: `string` = `"alphabet character cross doc editing editor font letter out spreadsheet styles symbol text type writing"`

### stroller

> **stroller**: `string` = `"baby care carriage children infant kid newborn toddler young"`

### style

> **style**: `string` = `"booklet cards filters options tags"`

### subdirectory\_arrow\_left

> **subdirectory\_arrow\_left**: `string` = `"arrow down navigation"`

### subdirectory\_arrow\_right

> **subdirectory\_arrow\_right**: `string` = `"arrow down navigation"`

### subject

> **subject**: `string` = `"alignment document email full justify lines list note text writing"`

### subscript

> **subscript**: `string` = `"2 doc editing editor gmail novitas spreadsheet style symbol text writing"`

### subscriptions

> **subscriptions**: `string` = `"enroll media order playlist queue signup subscribe youtube"`

### subtitles

> **subtitles**: `string` = `"accessibility accessible captions character closed decoder language media movies translate tv"`

### subtitles\_off

> **subtitles\_off**: `string` = `"accessibility accessible caption closed disabled enabled language slash translate video"`

### subway

> **subway**: `string` = `"automobile bike cars maps metro rail scooter train transportation travel tunnel underground vehicle vespa"`

### summarize

> **summarize**: `string` = `"document list menu note report summary"`

### superscript

> **superscript**: `string` = `"2 doc editing editor gmail novitas spreadsheet style symbol text writing"`

### supervised\_user\_circle

> **supervised\_user\_circle**: `string` = `"account avatar control face human parental parents people person profile supervisor"`

### supervisor\_account

> **supervisor\_account**: `string` = `"administrator avatar control face human parental parents people person profile supervised user"`

### support

> **support**: `string` = `"assist help lifebuoy rescue safety"`

### support\_agent

> **support\_agent**: `string` = `"care customer face headphone person representative service"`

### surfing

> **surfing**: `string` = `"athlete athletic beach body entertainment exercise hobby human people person sea social sports summer water"`

### surround\_sound

> **surround\_sound**: `string` = `"audio circle signal speaker system volume volumn wireless"`

### swap\_calls

> **swap\_calls**: `string` = `"arrows device direction mobile share"`

### swap\_horiz

> **swap\_horiz**: `string` = `"arrows back direction forward horizontal"`

### swap\_horizontal\_circle

> **swap\_horizontal\_circle**: `string` = `"arrows back direction forward"`

### swap\_vert

> **swap\_vert**: `string` = `"arrows back direction down navigation up vertical"`

### swap\_vertical\_circle

> **swap\_vertical\_circle**: `string` = `"arrows back direction down horizontal up"`

### swipe

> **swipe**: `string` = `"arrows fingers gesture hands touch"`

### swipe\_down

> **swipe\_down**: `string` = `"arrows direction disable enable finger hands hit navigation strike swing swpie take"`

### swipe\_down\_alt

> **swipe\_down\_alt**: `string` = `"arrows direction disable enable finger hands hit navigation strike swing swpie take"`

### swipe\_left

> **swipe\_left**: `string` = `"arrows finger hand hit navigation reject strike swing take"`

### swipe\_left\_alt

> **swipe\_left\_alt**: `string` = `"arrows finger hand hit navigation reject strike swing take"`

### swipe\_right

> **swipe\_right**: `string` = `"accept arrows direction finger hands hit navigation strike swing swpie take"`

### swipe\_right\_alt

> **swipe\_right\_alt**: `string` = `"accept arrows direction finger hands hit navigation strike swing swpie take"`

### swipe\_up

> **swipe\_up**: `string` = `"arrows direction disable enable finger hands hit navigation strike swing swpie take"`

### swipe\_up\_alt

> **swipe\_up\_alt**: `string` = `"arrows direction disable enable finger hands hit navigation strike swing swpie take"`

### swipe\_vertical

> **swipe\_vertical**: `string` = `"arrows direction finger hands hit navigation strike swing swpie take verticle"`

### switch\_access\_shortcut

> **switch\_access\_shortcut**: `string` = `"arrows direction navigation new north star symbol up"`

### switch\_access\_shortcut\_add

> **switch\_access\_shortcut\_add**: `string` = `"+ arrows direction navigation new north plus star symbol up"`

### switch\_account

> **switch\_account**: `string` = `"choices face human multiple options people person profile social user"`

### switch\_camera

> **switch\_camera**: `string` = `"arrows photography picture"`

### switch\_left

> **switch\_left**: `string` = `"arrows directional navigation toggle"`

### switch\_right

> **switch\_right**: `string` = `"arrows directional navigation toggle"`

### switch\_video

> **switch\_video**: `string` = `"arrows camera photography videos"`

### sync

> **sync**: `string` = `"360 around arrows direction inprogress loading refresh renew rotate turn"`

### sync\_alt

> **sync\_alt**: `string` = `"arrows horizontal internet technology update wifi"`

### sync\_disabled

> **sync\_disabled**: `string` = `"360 around arrows direction enabled inprogress loading off refresh renew rotate slash turn"`

### sync\_lock

> **sync\_lock**: `string` = `"around arrows locked password privacy private protection renew rotate safety secure security turn"`

### sync\_problem

> **sync\_problem**: `string` = `"! 360 alert around arrows attention caution danger direction error exclamation important inprogress loading mark notification refresh renew rotate symbol turn warning"`

### system\_security\_update

> **system\_security\_update**: `string` = `"Android arrow cell device down hardware iOS mobile phone tablet"`

### system\_security\_update\_good

> **system\_security\_update\_good**: `string` = `"Android approve cell check complete device done hardware iOS mark mobile ok phone select tablet tick validate verified yes"`

### system\_security\_update\_warning

> **system\_security\_update\_warning**: `string` = `"! Android alert attention caution cell danger device error exclamation hardware iOS important mark mobile notification phone symbol tablet"`

### system\_update

> **system\_update**: `string` = `"Android arrows cell device direction download hardware iOS install mobile phone tablet"`

### system\_update\_alt

> **system\_update\_alt**: `string` = `"arrow download export"`

### tab

> **tab**: `string` = `"browser computer documents folder internet tabs website windows"`

### tab\_unselected

> **tab\_unselected**: `string` = `"browser computer documents folder internet tabs website windows"`

### table\_chart

> **table\_chart**: `string` = `"analytics bars data diagram grid infographic measure metrics statistics tracking"`

### table\_rows

> **table\_rows**: `string` = `"grid layout lines stacked"`

### table\_view

> **table\_view**: `string` = `"format grid group layout multiple"`

### tablet

> **tablet**: `string` = `"Android device hardware iOS ipad mobile web"`

### tablet\_android

> **tablet\_android**: `string` = `"device hardware iOS ipad mobile web"`

### tablet\_mac

> **tablet\_mac**: `string` = `"Android apple device hardware iOS ipad mac mobile tablet web"`

### tag

> **tag**: `string` = `"hashtag key media number pound social trend"`

### tag\_faces

> **tag\_faces**: `string` = `"emoji emotion happy satisfied smile"`

### takeout\_dining

> **takeout\_dining**: `string` = `"box container delivery food meal restaurant"`

### tap\_and\_play

> **tap\_and\_play**: `string` = `"Android cell connection device hardware iOS internet mobile network nfc phone signal tablet to wifi wireless"`

### tapas

> **tapas**: `string` = `"appetizer brunch dinner food lunch restaurant snack"`

### task

> **task**: `string` = `"approve check complete data document done drive file folders mark ok page paper select sheet slide tick validate verified writing yes"`

### task\_alt

> **task\_alt**: `string` = `"approve check circle complete done mark ok select tick validate verified yes"`

### taxi\_alert

> **taxi\_alert**: `string` = `"! attention automobile cab cars caution danger direction error exclamation important lyft maps mark notification public symbol transportation uber vehicle warning yellow"`

### terminal

> **terminal**: `string` = `"application code emulator program software"`

### terrain

> **terrain**: `string` = `"geography landscape mountain"`

### text\_decrease

> **text\_decrease**: `string` = `"- alphabet character font letter minus remove resize subtract symbol type"`

### text\_fields

> **text\_fields**: `string` = `"T add alphabet character font input letter symbol type"`

### text\_format

> **text\_format**: `string` = `"A alphabet character font letter square style symbol type"`

### text\_increase

> **text\_increase**: `string` = `"+ add alphabet character font letter new plus resize symbol type"`

### text\_rotate\_up

> **text\_rotate\_up**: `string` = `"A alphabet arrow character field font letter move symbol type"`

### text\_rotate\_vertical

> **text\_rotate\_vertical**: `string` = `"A alphabet arrow character down field font letter move symbol type verticle"`

### text\_rotation\_angledown

> **text\_rotation\_angledown**: `string` = `"A alphabet arrow character field font letter move rotate symbol type"`

### text\_rotation\_angleup

> **text\_rotation\_angleup**: `string` = `"A alphabet arrow character field font letter move rotate symbol type"`

### text\_rotation\_down

> **text\_rotation\_down**: `string` = `"A alphabet arrow character field font letter move rotate symbol type"`

### text\_rotation\_none

> **text\_rotation\_none**: `string` = `"A alphabet arrow character field font letter move rotate symbol type"`

### text\_snippet

> **text\_snippet**: `string` = `"data document file notes storage writing"`

### textsms

> **textsms**: `string` = `"bubble chat comment communicate dots feedback message speech"`

### texture

> **texture**: `string` = `"diagonal lines pattern stripes"`

### theater\_comedy

> **theater\_comedy**: `string` = `"broadway event movie musical places show standup tour watch"`

### theaters

> **theaters**: `string` = `"film media movies photography showtimes video watch"`

### thermostat

> **thermostat**: `string` = `"forecast temperature weather"`

### thermostat\_auto

> **thermostat\_auto**: `string` = `"A celsius fahrenheit temperature thermometer"`

### thumb\_down

> **thumb\_down**: `string` = `"dislike downvote favorite fingers gesture hands ranking rate rating reject up"`

### thumb\_down\_alt

> **thumb\_down\_alt**: `string` = `"bad decline disapprove dislike feedback hand hate negative no reject social veto vote"`

### thumb\_down\_off\_alt

> **thumb\_down\_off\_alt**: `string` = `"bad decline disapprove dislike favorite feedback filled fingers gesture hands hate negative no ranking rate rating reject sad social veto vote"`

### thumb\_up

> **thumb\_up**: `string` = `"approve dislike down favorite fingers gesture hands ranking rate rating success upvote"`

### thumb\_up\_alt

> **thumb\_up\_alt**: `string` = `"agreed approved confirm correct favorite feedback good hand happy like okay positive satisfaction social success vote yes"`

### thumb\_up\_off\_alt

> **thumb\_up\_off\_alt**: `string` = `"agreed approved confirm correct favorite feedback fingers gesture good hands happy like okay positive ranking rate rating satisfaction social vote yes"`

### thumbs\_up\_down

> **thumbs\_up\_down**: `string` = `"dislike favorite fingers gesture hands rate rating vote"`

### time\_to\_leave

> **time\_to\_leave**: `string` = `"automobile cars destination direction drive estimate eta maps public transportation travel trip vehicle"`

### timelapse

> **timelapse**: `string` = `"duration motion photo timer video"`

### timeline

> **timeline**: `string` = `"analytics chart data graph history line movement points tracking trending zigzag zigzap"`

### timer

> **timer**: `string` = `"alarm alart bell clock disabled duration enabled notification off slash stopwatch wait"`

### timer\_10

> **timer\_10**: `string` = `"digits duration numbers seconds"`

### timer\_10\_select

> **timer\_10\_select**: `string` = `"alphabet camera character digit font letter number seconds symbol text type"`

### timer\_3

> **timer\_3**: `string` = `"digits duration numbers seconds"`

### timer\_3\_select

> **timer\_3\_select**: `string` = `"alphabet camera character digit font letter number seconds symbol text type"`

### timer\_off

> **timer\_off**: `string` = `"alarm alart bell clock disabled duration enabled notification slash stopwatch"`

### tips\_and\_updates

> **tips\_and\_updates**: `string` = `"alert announcement electricity idea information lamp lightbulb stars"`

### title

> **title**: `string` = `"T alphabet character font header letter subject symbol text type"`

### toc

> **toc**: `string` = `"content format lines list reorder stacked table text titles"`

### today

> **today**: `string` = `"agenda calendar date event mark month range remember reminder schedule time week"`

### toggle\_off

> **toggle\_off**: `string` = `"application components configuration control design disable inable inactive interface selection settings slider switch ui ux website"`

### toggle\_on

> **toggle\_on**: `string` = `"application components configuration control design disable inable inactive interface off selection settings slider switch ui ux website"`

### token

> **token**: `string` = `"badge hexagon mark shield sign symbol"`

### toll

> **toll**: `string` = `"bill booth card cash circles coin commerce credit currency dollars highway money online payment ticket"`

### tonality

> **tonality**: `string` = `"circle editing filter image photography picture"`

### topic

> **topic**: `string` = `"data document drive file folder sheet slide storage"`

### tornado

> **tornado**: `string` = `"crisis disaster natural rain storm weather wind"`

### touch\_app

> **touch\_app**: `string` = `"arrow command fingers gesture hand press swipe tap"`

### tour

> **tour**: `string` = `"destination flag places travel visit"`

### toys

> **toys**: `string` = `"car fan games kids windmill"`

### track\_changes

> **track\_changes**: `string` = `"bullseye circle evolve lines movement radar rotate shift target"`

### traffic

> **traffic**: `string` = `"direction light maps signal street"`

### train

> **train**: `string` = `"automobile cars direction maps public rail subway transportation vehicle"`

### tram

> **tram**: `string` = `"automobile cars direction maps public rail subway train transportation vehicle"`

### transfer\_within\_a\_station

> **transfer\_within\_a\_station**: `string` = `"arrows body direction human left maps people person public right route stop transit transportation vehicle walk"`

### transform

> **transform**: `string` = `"adjust crop editing image photo picture"`

### transgender

> **transgender**: `string` = `"female lgbt neutral neutrual social symbol"`

### transit\_enterexit

> **transit\_enterexit**: `string` = `"arrow direction maps navigation route transportation"`

### translate

> **translate**: `string` = `"alphabet language letter speaking speech text translator words"`

### travel\_explore

> **travel\_explore**: `string` = `"earth find glass global globe look magnifying map network planet search see social space web world"`

### trending\_down

> **trending\_down**: `string` = `"analytics arrow change chart data diagram infographic measure metrics movement rate rating sale statistics tracking"`

### trending\_flat

> **trending\_flat**: `string` = `"arrow change chart data graph metric movement rate right tracking"`

### trending\_up

> **trending\_up**: `string` = `"analytics arrow change chart data diagram infographic measure metrics movement rate rating statistics tracking"`

### trip\_origin

> **trip\_origin**: `string` = `"circle departure"`

### try

> **try**: `string` = `"bookmark bubble chat comment communicate favorite feedback highlight important marked message saved shape special speech star"`

### tty

> **tty**: `string` = `"call cell contact deaf device hardware impaired mobile speech talk telephone text"`

### tune

> **tune**: `string` = `"adjust editing options settings sliders"`

### tungsten

> **tungsten**: `string` = `"electricity indoor lamp lightbulb setting"`

### turn\_left

> **turn\_left**: `string` = `"arrows directions maps navigation path route sign traffic"`

### turn\_right

> **turn\_right**: `string` = `"arrows directions maps navigation path route sign traffic"`

### turn\_sharp\_left

> **turn\_sharp\_left**: `string` = `"arrows directions maps navigation path route sign traffic"`

### turn\_sharp\_right

> **turn\_sharp\_right**: `string` = `"arrows directions maps navigation path route sign traffic"`

### turn\_slight\_left

> **turn\_slight\_left**: `string` = `"arrows directions maps navigation path right route sign traffic"`

### turn\_slight\_right

> **turn\_slight\_right**: `string` = `"arrows directions maps navigation path route sharp sign traffic"`

### turned\_in

> **turned\_in**: `string` = `"archive bookmark favorite item label library reading remember ribbon save submit tag"`

### turned\_in\_not

> **turned\_in\_not**: `string` = `"archive bookmark favorite item label library outline reading remember ribbon save submit tag"`

### tv

> **tv**: `string` = `"device display linear living monitor room screencast stream television video wireless"`

### tv\_off

> **tv\_off**: `string` = `"Android chrome desktop device disabled enabled hardware iOS mac monitor slash television web window"`

### two\_wheeler

> **two\_wheeler**: `string` = `"automobile bicycle cars direction maps moped motorbike motorcycle public ride riding scooter transportation travel twom vehicle wheeler wheels"`

### u\_turn\_left

> **u\_turn\_left**: `string` = `"arrows directions maps navigation path route sign traffic u-turn"`

### u\_turn\_right

> **u\_turn\_right**: `string` = `"arrows directions maps navigation path route sign traffic u-turn"`

### umbrella

> **umbrella**: `string` = `"beach protection rain sunny"`

### unarchive

> **unarchive**: `string` = `"arrow inbox mail store undo up"`

### undo

> **undo**: `string` = `"arrow backward mail previous redo repeat rotate"`

### unfold\_less

> **unfold\_less**: `string` = `"arrows chevron collapse direction expandable inward list navigation up"`

### unfold\_more

> **unfold\_more**: `string` = `"arrows chevron collapse direction down expandable list navigation"`

### unpublished

> **unpublished**: `string` = `"approve check circle complete disabled done enabled mark off ok select slash tick validate verified yes"`

### unsubscribe

> **unsubscribe**: `string` = `"cancel close email envelop esubscribe message newsletter off remove send"`

### upcoming

> **upcoming**: `string` = `"alarm calendar mail message notification"`

### update

> **update**: `string` = `"arrow backwards clock forward future history load refresh reverse rotate schedule time"`

### update\_disabled

> **update\_disabled**: `string` = `"arrow backwards clock enabled forward history load off on refresh reverse rotate schedule slash time"`

### upgrade

> **upgrade**: `string` = `"arrow export instal line replace update"`

### upload

> **upload**: `string` = `"arrows download drive"`

### upload\_file

> **upload\_file**: `string` = `"arrow data document download drive folders page paper sheet slide writing"`

### usb

> **usb**: `string` = `"cable connection device wire"`

### usb\_off

> **usb\_off**: `string` = `"cable connection device wire"`

### vaccines

> **vaccines**: `string` = `"aid covid doctor drug emergency hospital immunity injection medical medication medicine needle pharmacy sick syringe vaccination vial"`

### verified

> **verified**: `string` = `"approve badge burst check complete done mark ok select star tick validate yes"`

### verified\_user

> **verified\_user**: `string` = `"approve audit certified checkmark complete done ok privacy private protection security select shield tick validate yes"`

### vertical\_align\_bottom

> **vertical\_align\_bottom**: `string` = `"alignment arrow doc down editing editor spreadsheet text type writing"`

### vertical\_align\_center

> **vertical\_align\_center**: `string` = `"alignment arrow doc down editing editor spreadsheet text type up writing"`

### vertical\_align\_top

> **vertical\_align\_top**: `string` = `"alignment arrow doc editing editor spreadsheet text type up writing"`

### vertical\_shades

> **vertical\_shades**: `string` = `"blinds cover curtains nest open shutter sunshade"`

### vertical\_shades\_closed

> **vertical\_shades\_closed**: `string` = `"blinds cover curtains nest roller shutter sunshade"`

### vertical\_split

> **vertical\_split**: `string` = `"design format grid layout paragraph text website writing"`

### vibration

> **vibration**: `string` = `"Android alert cell device hardware iOS mobile mode motion notification phone silence silent tablet vibrate"`

### video\_call

> **video\_call**: `string` = `"+ add camera chat conference filming hardware image motion new picture plus screen symbol videography"`

### video\_camera\_back

> **video\_camera\_back**: `string` = `"image landscape mountains photography picture rear"`

### video\_camera\_front

> **video\_camera\_front**: `string` = `"account face human image people person photography picture profile user"`

### video\_file

> **video\_file**: `string` = `"camera document filming hardware image motion picture videography"`

### video\_label

> **video\_label**: `string` = `"device item screen window"`

### video\_library

> **video\_library**: `string` = `"arrow collection play"`

### video\_settings

> **video\_settings**: `string` = `"change details gear information options play screen service window"`

### video\_stable

> **video\_stable**: `string` = `"filming recording setting stability taping"`

### videocam

> **videocam**: `string` = `"camera chat conference filming hardware image motion picture screen videography"`

### videocam\_off

> **videocam\_off**: `string` = `"camera chat conference disabled enabled filming hardware image motion offline picture screen slash videography"`

### videogame\_asset

> **videogame\_asset**: `string` = `"console controller device gamepad gaming nintendo playstation xbox"`

### videogame\_asset\_off

> **videogame\_asset\_off**: `string` = `"console controller device disabled enabled gamepad gaming playstation slash"`

### view\_agenda

> **view\_agenda**: `string` = `"blocks cards design format grid layout website,stacked"`

### view\_array

> **view\_array**: `string` = `"blocks design format grid layout website"`

### view\_carousel

> **view\_carousel**: `string` = `"banner blocks cards design format grid images layout website"`

### view\_column

> **view\_column**: `string` = `"blocks design format grid layout vertical website"`

### view\_comfy

> **view\_comfy**: `string` = `"grid layout pattern squares"`

### view\_comfy\_alt

> **view\_comfy\_alt**: `string` = `"cozy design format layout web"`

### view\_compact

> **view\_compact**: `string` = `"grid layout pattern squares"`

### view\_compact\_alt

> **view\_compact\_alt**: `string` = `"dense design format layout web"`

### view\_cozy

> **view\_cozy**: `string` = `"comfy design format layout web"`

### view\_day

> **view\_day**: `string` = `"blocks calendar cards carousel design format grid layout website week"`

### view\_headline

> **view\_headline**: `string` = `"blocks design format grid layout paragraph text website"`

### view\_in\_ar

> **view\_in\_ar**: `string` = `"3d augmented cube daydream headset reality square vr"`

### view\_kanban

> **view\_kanban**: `string` = `"grid layout pattern squares"`

### view\_list

> **view\_list**: `string` = `"blocks design format grid layout lines reorder stacked title website"`

### view\_module

> **view\_module**: `string` = `"blocks design format grid layout reorder squares stacked title website"`

### view\_quilt

> **view\_quilt**: `string` = `"blocks design format grid layout reorder squares stacked title website"`

### view\_sidebar

> **view\_sidebar**: `string` = `"design format grid layout web"`

### view\_stream

> **view\_stream**: `string` = `"blocks design format grid layout lines list reorder stacked title website"`

### view\_timeline

> **view\_timeline**: `string` = `"grid layout pattern squares"`

### view\_week

> **view\_week**: `string` = `"bars blocks columns day design format grid layout website"`

### vignette

> **vignette**: `string` = `"border editing effect filter gradient image photography setting"`

### villa

> **villa**: `string` = `"architecture beach estate home house maps place real residence residential stay traveling vacation"`

### visibility

> **visibility**: `string` = `"eye on password preview reveal see shown visability"`

### visibility\_off

> **visibility\_off**: `string` = `"disabled enabled eye hidden invisible on password reveal see show slash view visability"`

### voice\_chat

> **voice\_chat**: `string` = `"bubble camera comment communicate facetime feedback message speech video"`

### voice\_over\_off

> **voice\_over\_off**: `string` = `"account disabled enabled face human people person profile recording slash speaking speech transcript user"`

### voicemail

> **voicemail**: `string` = `"call device message missed mobile phone recording"`

### volume\_down

> **volume\_down**: `string` = `"audio av control music quieter shh soft sound speaker tv"`

### volume\_mute

> **volume\_mute**: `string` = `"audio control music sound speaker tv"`

### volume\_off

> **volume\_off**: `string` = `"audio av control disabled enabled low music mute slash sound speaker tv"`

### volume\_up

> **volume\_up**: `string` = `"audio control music sound speaker tv"`

### volunteer\_activism

> **volunteer\_activism**: `string` = `"donation fingers gesture giving hands heart love sharing"`

### vpn\_key

> **vpn\_key**: `string` = `"login network passcode password register security signin signup unlock"`

### vpn\_key\_off

> **vpn\_key\_off**: `string` = `"[offline] disabled enabled network on passcode password slash unlock"`

### vpn\_lock

> **vpn\_lock**: `string` = `"earth globe locked network password privacy private protection safety secure security virtual world"`

### vrpano

> **vrpano**: `string` = `"angle image landscape mountains panorama photography picture view wide"`

### wallpaper

> **wallpaper**: `string` = `"background image landscape photography picture"`

### warehouse

> **warehouse**: `string` = `"garage industry manufacturing storage"`

### warning

> **warning**: `string` = `"! alert announcement attention caution danger error exclamation feedback important mark notification problem symbol triangle"`

### warning\_amber

> **warning\_amber**: `string` = `"! alert attention caution danger error exclamation important mark notification symbol triangle"`

### wash

> **wash**: `string` = `"bathroom clean fingers gesture hand wc"`

### watch

> **watch**: `string` = `"Android clock gadget iOS smartwatch time vr wearables web wristwatch"`

### watch\_later

> **watch\_later**: `string` = `"clock date hour minute schedule time"`

### watch\_off

> **watch\_off**: `string` = `"Android clock close gadget iOS shut time vr wearables web wristwatch"`

### water

> **water**: `string` = `"aqua beach lake ocean river waves weather"`

### water\_damage

> **water\_damage**: `string` = `"architecture building droplet estate house leak plumbing real residence residential shelter"`

### waterfall\_chart

> **waterfall\_chart**: `string` = `"analytics bar data diagram infographic measure metrics statistics tracking"`

### waves

> **waves**: `string` = `"beach lake ocean pool river sea swim water"`

### wb\_auto

> **wb\_auto**: `string` = `"A W alphabet automatic balance character editing font image letter photography symbol text type white wp"`

### wb\_cloudy

> **wb\_cloudy**: `string` = `"balance editing white wp"`

### wb\_incandescent

> **wb\_incandescent**: `string` = `"balance bright editing lamp lightbulb lighting settings white wp"`

### wb\_iridescent

> **wb\_iridescent**: `string` = `"balance bright editing lighting settings white wp"`

### wb\_shade

> **wb\_shade**: `string` = `"balance house lighting white"`

### wb\_sunny

> **wb\_sunny**: `string` = `"balance bright lighting weather white"`

### wc

> **wc**: `string` = `"bathroom closet female gender man person restroom toilet unisex wash water women"`

### web

> **web**: `string` = `"blocks browser internet page screen website www"`

### web\_asset

> **web\_asset**: `string` = `"-website application browser design desktop download image interface internet layout screen ui ux video window www"`

### web\_asset\_off

> **web\_asset\_off**: `string` = `"browser disabled enabled internet on screen slash webpage website windows www"`

### webhook

> **webhook**: `string` = `"api developer development enterprise software"`

### weekend

> **weekend**: `string` = `"chair couch furniture home living lounge relax room seat"`

### west

> **west**: `string` = `"arrow directional left maps navigation"`

### whatshot

> **whatshot**: `string` = `"arrow circle direction fire frames round trending"`

### wheelchair\_pickup

> **wheelchair\_pickup**: `string` = `"accessibility accessible body handicap help human person"`

### where\_to\_vote

> **where\_to\_vote**: `string` = `"approve ballot check complete destination direction done election location maps mark ok pin place poll select stop tick validate verified yes"`

### widgets

> **widgets**: `string` = `"app blocks box menu setting squares ui"`

### wifi

> **wifi**: `string` = `"connection data internet network scan service signal wireless"`

### wifi\_calling

> **wifi\_calling**: `string` = `"cell connection connectivity contact device hardware mobile signal telephone wireless"`

### wifi\_calling\_3

> **wifi\_calling\_3**: `string` = `"cellular data internet mobile network phone speed wireless"`

### wifi\_channel

> **wifi\_channel**: `string` = `"(scan) [cellular connection data internet mobile] network service signal wireless"`

### wifi\_find

> **wifi\_find**: `string` = `"(scan) [cellular connection data detect discover glass internet look magnifying mobile] network notice search service signal wireless"`

### wifi\_lock

> **wifi\_lock**: `string` = `"cellular connection data internet locked mobile network password privacy private protection safety secure security service signal wireless"`

### wifi\_off

> **wifi\_off**: `string` = `"connection data disabled enabled internet network offline scan service signal slash wireless"`

### wifi\_password

> **wifi\_password**: `string` = `"(scan) [cellular connection data internet lock mobile] network secure service signal wireless"`

### wifi\_protected\_setup

> **wifi\_protected\_setup**: `string` = `"around arrows rotate"`

### wifi\_tethering

> **wifi\_tethering**: `string` = `"cellular connection data internet mobile network phone scan service signal speed wireless"`

### wifi\_tethering\_off

> **wifi\_tethering\_off**: `string` = `"cellular connection data disabled enabled internet mobile network offline phone scan service signal slash speed wireless"`

### wind\_power

> **wind\_power**: `string` = `"eco energy nest windy"`

### window

> **window**: `string` = `"close glass grid home house interior layout outside"`

### wine\_bar

> **wine\_bar**: `string` = `"alcohol cocktail cup drink glass liquor"`

### work

> **work**: `string` = `"-briefcase baggage business job suitcase"`

### work\_history

> **work\_history**: `string` = `"arrow backwards baggage briefcase business clock date job refresh renew reverse rotate schedule suitcase time turn"`

### work\_off

> **work\_off**: `string` = `"baggage briefcase business disabled enabled job on slash suitcase"`

### work\_outline

> **work\_outline**: `string` = `"baggage briefcase business job suitcase"`

### workspace\_premium

> **workspace\_premium**: `string` = `"certification degree ecommerce guarantee medal permit ribbon verification"`

### workspaces

> **workspaces**: `string` = `"circles collaboration dot filled group team"`

### wrap\_text

> **wrap\_text**: `string` = `"arrow doc editing editor spreadsheet type write writing"`

### wrong\_location

> **wrong\_location**: `string` = `"cancel close destination direction exit maps no pin place quit remove stop"`

### wysiwyg

> **wysiwyg**: `string` = `"composer mode screen software system text view visibility website window"`

### yard

> **yard**: `string` = `"backyard flower garden home house nature pettle plants"`

### youtube\_searched\_for

> **youtube\_searched\_for**: `string` = `"arrow backwards find glass history inprogress loading look magnifying refresh renew restore reverse rotate see yt"`

### zoom\_in

> **zoom\_in**: `string` = `"bigger find glass grow look magnifier magnifying plus scale search see size"`

### zoom\_in\_map

> **zoom\_in\_map**: `string` = `"arrows destination location maps move place stop"`

### zoom\_out

> **zoom\_out**: `string` = `"find glass look magnifier magnifying minus negative scale search see size smaller"`

### zoom\_out\_map

> **zoom\_out\_map**: `string` = `"arrows destination location maps move place stop"`

---
import Layout from "../../layouts/Layout.astro";
import DevelopersContent from "../../components/pages/DevelopersContent.astro";
import { languages } from '../../i18n/ui';
import { useTranslations } from '../../i18n/utils';

export function getStaticPaths() {
    const supportedLangs = Object.keys(languages).filter(l => l !== 'en');
    return supportedLangs.map(lang => ({ params: { lang } }));
}

const { lang } = Astro.params;
const t = useTranslations(lang as keyof typeof languages);

const breadcrumbs = [
    { name: t('nav.home' as any) || "Home", url: `https://firecms.co/${lang}` },
    { name: t('nav.developers' as any) || "Developers", url: `https://firecms.co/${lang}/developers` }
];
---

<Layout
        title={t('seo.developers.title')}
        description={t('seo.developers.description')}
        breadcrumbs={breadcrumbs}
        lang={lang}
>
    <DevelopersContent lang={lang} t={t} />
</Layout>

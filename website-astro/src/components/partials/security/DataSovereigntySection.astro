---
import Panel from "../../Panel.astro";
import { defaultBorderMixin } from "../../../migration/partials/styles";

const DataSovereigntyImage = "/img/demo-botanic-plant-firestore.png";
---

<Panel color={"gray"}>
    <div class="py-12 md:py-16 max-w-6xl mx-auto px-4">
        <div class="grid lg:grid-cols-2 gap-12 items-center">
            <div>
                <div class="flex items-center mb-4">
                    <StorageIcon class="w-8 h-8 mr-2 mt-2 text-blue-400 flex-shrink-0"/>
                    <span class="material-icons w-8 h-8 mr-2 mt-2 text-blue-400 flex-shrink-0">storage</span>
                </div>
                <p class="text-xl text-gray-100 mb-4">
                    Unlike traditional SaaS CMS platforms that ingest and control your data,
                    FireCMS operates as a sophisticated tool that connects directly to <b>your own</b> database.
                </p>

                <div class="space-y-8 my-16">
                    <div class="flex items-start space-x-3">
                        <CheckCircleIcon class="w-6 h-6 text-green-400 mt-2 flex-shrink-0"/>
                        <span class="material-icons w-6 h-6 text-green-400 mt-2 flex-shrink-0">check_circle</span>
                            <h4 class="font-semibold text-white">No Data Migration Required</h4>
                            <p class="text-gray-200 text-sm m-1">
                                FireCMS adapts to your existing data structure, not the other way around.
                            </p>
                        </div>
                    </div>
                    <div class="flex items-start space-x-3">
                        <CheckCircleIcon class="w-6 h-6 text-green-400 mt-2 flex-shrink-0"/>
                        <span class="material-icons w-6 h-6 text-green-400 mt-2 flex-shrink-0">check_circle</span>
                            <h4 class="font-semibold text-white">Zero Vendor Lock-in</h4>
                            <p class="text-gray-200 text-sm m-1">
                                Stop using FireCMS anytime - your data stays exactly where it is.
                            </p>
                        </div>
                    </div>
                    <div class="flex items-start space-x-3">
                        <CheckCircleIcon class="w-6 h-6 text-green-400 mt-2 flex-shrink-0"/>
                        <span class="material-icons w-6 h-6 text-green-400 mt-2 flex-shrink-0">check_circle</span>
                            <h4 class="font-semibold text-white">Direct Infrastructure Access</h4>
                            <p class="text-gray-200 text-sm m-1">
                                Full control over your Google Cloud or MongoDB Atlas project.
                            </p>
                        </div>
                    </div>
                </div>

            </div>

            <div class:list={["rounded-xl border overflow-hidden", defaultBorderMixin]}>
                <img
                    src={DataSovereigntyImage}
                    alt="Diagram showing FireCMS connecting to customer's own Firebase/MongoDB project with data remaining under customer control"
                    class="h-full w-full object-cover"
                />
            </div>
        </div>
    </div>
</Panel>

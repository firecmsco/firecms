---
import { InfoOutlineIcon } from "@firecms/ui";

interface Props {
    tip: string;
}

const { tip } = Astro.props;
---

<span title={tip}>
    <slot /> <InfoOutlineIcon className={"inline align-middle"} size={"smallest"}/>
</span>

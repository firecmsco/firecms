---
import { ContainerMixin, ContainerPaddingMixin, defaultBorderMixin } from "../../../migration/partials/styles";

const schemaEditorVideo = "/img/schema_editor.webm";
---

<section class="relative bg-gray-900">
    <div class:list={[
        "px-4 sm:px-6 text-white dark:text-white",
        "border-0 border-y bg-gray-950",
        defaultBorderMixin
    ]}>
        <div class:list={[
            ContainerMixin,
            ContainerPaddingMixin,
            defaultBorderMixin,
            "border-x border-y-0",
            "flex flex-col py-20"
        ]}>
            <div class="relative max-w-6xl mx-auto pb-16 my-16">
                <h3 class="typography-h3 mb-4 uppercase">
                    Automatic schema detection
                </h3>

                <div class="mt-4 mx-auto text-xl">
                    <p>
                        Build your content model visually with FireCMS's powerful schema editor.
                        Create collections, define fields, and establish relationships with an
                        intuitive drag-and-drop UI.
                    </p>
                    <p>
                        With <b>20+ field types</b> and advanced validation options, you can build
                        sophisticated data models in minutes instead of hours. The schema editor
                        handles the complexity, letting you focus on <b>creating the perfect admin
                        experience</b> for your team.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <div class:list={[ContainerMixin, "border-0 border-x", defaultBorderMixin]}>
        <div class:list={[ContainerMixin, "px-4 sm:px-6 -translate-y-44"]}>
            <video
                style="pointer-events: none;"
                class:list={["rounded-xl border", defaultBorderMixin]}
                width="100%"
                loop
                autoplay
                muted
                playsinline
            >
                <source src={schemaEditorVideo} type="video/webm" />
            </video>
        </div>
    </div>
</section>


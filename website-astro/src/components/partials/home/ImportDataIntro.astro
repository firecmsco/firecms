---
import { ContainerMixin, ContainerPaddingMixin, defaultBorderMixin } from "../../../migration/partials/styles";

const dataImportVideo = "/img/import.webm";
---

<section class="relative bg-white">
    <div class:list={[
        "px-4 sm:px-6 text-gray-800",
        "border-0 border-y bg-gray-200",
        defaultBorderMixin
    ]}>
        <div class:list={[
            ContainerMixin,
            ContainerPaddingMixin,
            defaultBorderMixin,
            "border-x border-y-0",
            "flex flex-col py-20"
        ]}>
            <div class="relative max-w-6xl mx-auto pb-16 my-16">
                <h3 class="mb-4 uppercase">
                    Simple <b>Data Import</b>, from file to CMS
                </h3>

                <div class="mt-4 mx-auto text-xl">
                    <p>
                        FireCMS offers a streamlined data import feature designed to make your life easier.
                        Whether you're working with extensive <b>CSV, JSON or Excel</b> files,
                        our intuitive tools enable you to transfer and integrate your datasets
                        into FireCMS. No more tedious manual data entry or convoluted import processes—just
                        quick, efficient, and reliable data importation.
                    </p>
                    <p>
                        Our data import tool will <b>detect your data types automatically</b>, and if you
                        override a mapping, it will do the data conversion for you.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <div class:list={[ContainerMixin, "border-0 border-x", defaultBorderMixin]}>
        <div class:list={[ContainerMixin, "px-4 sm:px-6 -translate-y-44"]}>
            <video
                style="pointer-events: none;"
                class:list={["rounded-xl border", defaultBorderMixin]}
                width="100%"
                loop
                autoplay
                muted
                playsinline
            >
                <source src={dataImportVideo} type="video/webm" />
            </video>
        </div>
    </div>
</section>


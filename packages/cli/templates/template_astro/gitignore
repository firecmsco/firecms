# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# local env files. `.env` is not ignored: it holds the FireCMS license key, which is
# a PUBLIC_ variable and ends up in the client bundle anyway, so a clone of this repo
# needs it. Keep private values in .env.local.
.env.local
.env.production

# macOS
.DS_Store

# typescript
*.tsbuildinfo

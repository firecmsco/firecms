"use client";
import React from "react";
import { Icon, IconProps } from "@firecms/ui";

/**
 * @group Icons
 */
export function ClientIcon(props: IconProps & { iconKey: string }) {
    return <Icon {...props}/>
}
